# OPS-UI-THEME-DIAGNOSIS — تشخيص ثيم الشاشات التشغيلية

التاريخ: 2026-09-01 · الجولة: **Operations UI Theme Audit — تشخيص فقط** (بأمر المالك) · صفر تعديل كود/بيانات.

## 1) النطاق والمنهج

- الأداة: `scripts/_ops-theme-audit.js` — Chrome headless عبر CDP على **الإنتاج** (`emsoperations.online`)، دخول بحساب قراءة (operator)، فتح النوافذ بنفس مسارات القائمة الجانبية، قياس computed style لكل عنصر مرئي + لقطات.
- فئتا الكشف: **(A)** أسطح فاتحة صريحة (خلفية luminance > 180، alpha > 0.8) داخل أجسام داكنة · **(B)** نصوص منخفضة التباين (فاتح-على-فاتح / داكن-على-داكن) مقابل الخلفية الفعلية للأب.
- النتائج الخام: `test-output/ops-theme-audit-1788261678307/scan-results.json` + 8 لقطات.

## 2) النتيجة لكل شاشة

| الشاشة | أسطح فاتحة | نصوص منخفضة التباين | الحكم |
|---|---|---|---|
| النماذج `formsModal` (نموذج كبار المسعفين) | 2 | 12 | ❌ بقايا مؤثرة |
| لوحة التحكم `analyticsModal` (الإحصائيات والرسوم) | 3 | 10 | ❌ بقايا مؤثرة |
| تنسيق الإجازة `controlModal` | 28 | 0 | ⚠️ عدم اتساق بصري (مقروء) |
| إحصائيات `chartsModal` | 0 | 0 | ✅ سليم |
| توزيع البلاغات `distributionModal` | 0 | 0 | ✅ سليم (1100 عنصر مفحوص) |
| وقت الذروة `peakTimeModal` | 0 | 0 | ✅ سليم |
| سجل العمليات `auditLogModal` | 0 | 0 | ✅ سليم |
| **التقارير / مركز العمليات** `operations-dashboard.html` | 0 | 0 | ✅ سليم تمامًا (داكن أصيل) |

## 3) الأسباب الجذرية (مثبتة بالدليل)

### ① حشرة تحليل CSS — تعليق يبتلع قاعدة (analyticsModal)
- `public/css/executive-theme.css:2334` داخل تعليق: «أصناف `peak-*/analytics-*`» — التسلسل `*/` في `peak-*/` **يُغلق التعليق مبكرًا**، فيبتلع المحلل القاعدة التالية مباشرة ويسقطها:
  `2341: #analyticsModal .analytics-section { background: var(--ex-surface) !important; ... }`
- **الإثبات الحي:** `document.styleSheets[executive-theme].cssRules` على الإنتاج = 947 قاعدة، ولا توجد أي قاعدة بمحدد `.analytics-section`؛ القاعدة التي تليها (`h3` — 2342) موجودة. أي أن المحلل أسقط قاعدة واحدة بالضبط — التوقيع الكلاسيكي لابتلاع ما بعد تعليق مكسور.
- **الأثر:** أقسام «الخريطة الحرارية» و«توقع الذروة» بيضاء (`app.css:3298` يكسب بلا منازع)، وعناوينها حوّلتها قواعد أخرى للألوان الفاتحة ⇒ نص فاتح شبه خفي على أبيض (10 نصوص).
- نفس فئة الخلل الذي عولج في المرحلة B (قوس زائد في smart-toolbar.css) — يحتاج حارسًا اختباريًا.

### ② محدد واسع جدًا يفرض الأبيض (formsModal)
- `public/css/app.css:2047`: `#formContent div[style*="grid-template-columns"] { background: var(--white) !important; grid-template-columns: repeat(auto-fill, minmax(160px,1fr)) !important; ... }`
- صُمم لشبكات مربعات الاختيار، لكن محدد الخاصية يلتقط **أي** div بأعمدة مضمّنة — منها شبكة التسليم والاستلام الثلاثية في `public/forms/form-senior.html:75` (div بلا صنف).
- **الإثبات الحي:** العنصر المقاس (867×351، `rgb(255,255,255)`) هو هذا الغلاف نفسه؛ والصناديق الداخلية (`background:var(--gray-50)` مضمّن) داكنة فعلًا لأن صائدي `[style*=]` في executive-theme (1825-1826) التقطوها — الغلاف وحده هو الأبيض.
- أثر جانبي موثق: نفس القاعدة تكسر تخطيط «3 أعمدة» المقصود للتواقيع وتحوّله auto-fill (تخطيطي، خارج نطاق الثيم — يُذكر للعلم).
- النصوص الـ12: عناوين/تسميات حوّلتها قواعد الثيم التنفيذي للألوان الفاتحة (مصممة لخلفية داكنة) فأصبحت فاتحة-على-أبيض.
- زر 🗑️ حذف (`background:var(--coral-50)` مضمّن — app.js:6114 و5 مواضع مشابهة) = شريحة فاتحة صغيرة؛ لون دلالي ثانوي، يُصنّف ملاحظة اختيارية لا خللًا.

### ③ فجوات تغطية — لا قواعد داكنة أصلًا (analyticsModal + controlModal)
- `.analytics-header` (`app.css:3181` — أبيض/gray-200): لا توجد له أي قاعدة مضادة في أي ورقة ⇒ ترويسة بيضاء 1200×72 بعنوان شبه خفي.
- `controlModal`: `.control-person` (`app.css:4691` أبيض) + `.display` (gray-50) + `.control-role-badge` (primary-50) — 28 سطحًا فاتحًا بلا أي تغطية داكنة. النص بقي داكنًا على الأبيض (مقروء) لأن قواعد النص التنفيذية لم تصلها أيضًا ⇒ **بطاقات فاتحة متسقة داخل نافذة داكنة** — مشكلة اتساق هوية لا قابلية قراءة.

## 4) نطاق الإصلاح المقترح (للاعتماد — لم يُنفذ شيء)

| # | الإصلاح | الملف | الحجم |
|---|---|---|---|
| 1 | تصحيح التعليق 2334 (`peak-*/` ← `peak-* /`) فيستعيد المحلل القاعدة 2341 | `executive-theme.css` | حرف واحد تقريبًا |
| 2 | تغليب داكن بنفس المحدد حرفيًا `#formContent div[style*="grid-template-columns"]` (نفس السبقية (1,1,1) + التحميل اللاحق يكسب) — خلفية زجاجية داكنة للغلاف | `executive-theme.css` | قاعدة واحدة |
| 3 | تغطية داكنة لـ`.analytics-header` و`.control-person`/`.display`/`.control-role-badge` بنفس نمط الجولات السابقة الموثق | `executive-theme.css` | 3 مجموعات |
| 4 | حارس اختباري (اختبار صرف): (أ) فحص ساكن يمنع `*/` داخل التعليقات في كل `public/css/*.css`؛ (ب) تحويل ماسح هذه الجولة إلى اختبار انحدار «صفر أسطح فاتحة» في النوافذ الثلاث | `scripts/` | اختبار فقط |

- **المبادئ:** الإصلاح كله في طبقة executive-theme + اختبارات؛ **صفر تعديل على app.css (محمي) وapp.js وindex.html وملفات forms/**؛ لا مساس بالخريطة/RC-6/PI-10؛ لا Redesign — نفس لغة الزجاج الداكن المعتمدة.
- **خارج النطاق:** أثر auto-fill التخطيطي على شبكة التواقيع (يُحسم بقرار منفصل إن رغب المالك) · شريحة الحذف coral-50 (دلالية) · لوحة المساعد التلقائية على الجوال (تذكرة مستقلة مؤجلة سلفًا).

## 5) أدلة

- اللقطات: `test-output/ops-theme-audit-1788261678307/` (formsModal · analyticsModal · controlModal تظهر البقايا بصريًا؛ البقية سليمة).
- مسابر الإثبات: `scripts/_ops-theme-audit.js` · `_cascade-probe.js` (getMatchedStylesForNode) · `_rules-probe.js` / `_rules-around.js` (cssRules الحية) · `_forms-white-probe.js`.

**الخلاصة:** 5 من 8 شاشات سليمة تمامًا (ومنها التقارير/مركز العمليات)، و3 نوافذ تحمل بقايا جذورها محسومة: تعليق يبتلع قاعدة ①، محدد واسع ②، فجوات تغطية ③ — كلها قابلة للإصلاح في طبقة الثيم التنفيذي وحدها بلا أي مساس بالكود المحمي.
