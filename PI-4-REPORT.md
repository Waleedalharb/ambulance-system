# تقرير PI-4 — واجهة «الجهات والمواقع» في أرشيف المناوبة (العرض التنفيذي)

**التاريخ:** 2026-08-29
**الحالة:** منفذ محليًا + مختبر + أدلة كاملة — **بلا Commit / Push / Deploy**
**المرجعية:** `pi-4-directive-draft.md` (النسخة المعتمدة بتعديلات المالك الأربعة) · اعتماد المالك 2026-08-29

---

## ① ما بُني (Before → After)

| | قبل PI-4 | بعد PI-4 |
|---|---|---|
| قسم `snapshot.places` | يُختم مع اللقطة (PI-3) لكن **لا تعرضه أي واجهة** | تبويب عاشر «🗺️ الجهات والمواقع» في سجل المناوبة يعرضه بأربعة مستويات حفر حتى Evidence |
| مسار البيانات | — | `shift_snapshots (sealed.places)` → `/api/shifts/:id` (`response.places`) → `renderPlacesTab` — **قراءة صِرفة من اللقطة** |

## ② الملفات المعدّلة (بالأدلة)

| الملف | التغيير | الحجم |
|---|---|---|
| `public/operations-dashboard.html` | زر التبويب العاشر + أنماط `.pl-*` الداكنة + `renderPlacesTab` + `placesTabToggle` (بين علامتَي `PI-4 BEGIN/END`) + وصلة في `viewShift` | +168 سطرًا، صفر حذف |
| `server.js` | **سطر واحد additive**: `response.places = sealed.places || null;` (بجانب سطر hospital المعتمد — نفس النمط) — لا استعلام حي ولا منطق | ضمن +31 (منها PI-3) |
| `services/place-intelligence-service.js` | **حقل واحد additive** في مدخل القسم: `decision: r.decision` — حتى تعرض الواجهة الحالة **المختومة** بدل استنتاجها من القاعدة (تقوية للحاجز ②، لا منطق جديد) | سطر واحد (الملف جديد أصلًا من PI-1 — untracked) |
| `scripts/place-archive-view-test.js` | Golden Set للواجهة V1–V10 | جديد |
| `scripts/pi4-preview-gen.js` | مولّد صفحات المعاينة للقطات الشاشة | جديد |

**لم يُمس:** `shift-archive-engine.js` في هذه الجولة (تعديله الظاهر في git من PI-3 المعتمد) · R1–R5 · placesVersion · المخازن · مسارات CAD · report_times والاحتساب.

## ③ Golden Set — 16/16 ✅ (حالة بحالة)

### حراس نصية/صياغية
| # | الفحص | الدليل |
|---|---|---|
| G1 | زر التبويب العاشر موجود | `data-mtab="places"` + أيقونة `fa-map-marked-alt` ✅ |
| G2 | كود PI-4 خالٍ من أي fetch/XHR — عرض فقط | فحص نصي على القسم المعلَّم ✅ |
| G3 | موصول بـviewShift (نفس استجابة `/api/shifts/:id`) | `renderPlacesTab(currentShiftDetail)` ✅ |
| G4 | الخادم يعرّض المختوم فقط | `response.places = sealed.places || null;` ✅ |
| G5 | تهريب HTML | `esc` على كل قيمة من البيانات ✅ |
| G6 | صياغة JS سليمة | `node --check` على الكتلة المضمّنة ✅ |

### اختبارات العرض الفعلية (الكود المستخلص من الصفحة يعمل في Node بـDOM مصطنع — الأرقام مقروءة من HTML المولّد)

اللقطة الاختبارية (حقيقية عبر الخدمة الفعلية): `incidents=6 · confirmed=3 · likely=1 · unknown=1 · noResolution=1` · مدارس 2 مؤكد + 1 محتمل · مستشفيات 1 مؤكد.

| # | الفحص | الدليل الرقمي |
|---|---|---|
| V1 | **مطابقة الأرقام حرفيًا** | كل قيمة معروضة = قيمة اللقطة (`hk-val`=6/3/1/1 · `pt-count`=2 للمدارس و1 للمستشفيات) ✅ |
| V2 | **الحفر الرباعي** | نوع (`pl-type-card`) ← جهة (`pl-place-row`: «مدرسة الملك خالد») ← بلاغ (`pl-inc-row`: SC1) ← بطاقة Evidence (`pl-detail` + `pe-item`) ✅ |
| V3 | **فصل Confirmed/Likely** | العنوان يعرض **2** فقط (لا 3)؛ المحتمل مطوي: `▸ محتمل — يحتاج تحقق: 1` ✅ |
| V4 | **Unknown** | سطر معلومة مستقل «غير محسوم: 1 بلاغًا»؛ لا «مدارس غير محسومة» ولا نوع unknown ✅ |
| V5 | **اللقطة القديمة** | بلا `places` + `places:null` + مناوبة نشطة: صفر crash + رسالتان صادقتان هادئتان ✅ |
| V6 | **المقام موثق نصًا** | `denominatorNote` تظهر كما خُتمت؛ كود الواجهة خالٍ من أي قسمة/Math.round/parseFloat ✅ |
| V7 | **عدم المساس بالقائم** | تبويب المستشفيات وبنية `hosp-*` والتبويبات التسعة كما هي ✅ |
| V8 | **صفر استعلام حي — بعدادات فعلية** | `live-store requests during render: 0` (عدّادات على `_loadResolutions`/`_loadRegistry`) ✅ |
| V9 | **Historical Integrity** | بعد تغيير المخزن الحي (اسم مغيّر/likely/70/v5): الشاشة بقيت «مدرسة الملك خالد · مؤكد · 97% · v2» بالضبط — لا أثر للحي في العرض ✅ |
| V10 | **No UI Mutation** | اللقطة بعد العرض مطابقة 100% (`sealedUnchanged=true`) · `resolve=0 record=0` · الطي يقلب `style.display` فقط ✅ |

## ④ الأدلة البصرية (لقطات شاشة فعلية — Edge headless)

ولّدتها `scripts/pi4-preview-gen.js` من **كود PI-4 المستخلص من الصفحة الفعلية + CSS الصفحة نفسها + لقطة مختومة حقيقية عبر الخدمة** (لا شيء مختلق):

| اللقطة | المسار | المحتوى |
|---|---|---|
| المستوى ① | `pi4-previews/preview-level1.png` | المؤشرات + الختم + المقام + شبكة البطاقات (مدارس 3 · مستشفيات 2 · شرطة 1) + سطر Likely المطوي + سطر غير المحسوم |
| المستويات ②③④ | `pi4-previews/preview-expanded.png` | الجهة ← البلاغات ← بطاقة التدقيق بكل الحقول + Evidence (`rule/distance/confidence` كما خُتمت) |
| اللقطة القديمة | `pi4-previews/preview-old.png` | «قسم الجهات غير متوفر لهذه المناوبة — أُرشفت قبل نظام الجهات والمواقع» — هادئة بلا مظهر خطأ |

> ملاحظة شفافية: أثناء التوليد انكشف خلل بنيوي في النسخة الأولى (حاوية الشبكة `.pl-type-grid` لم تكن تُفتتح/تُغلق، فتمددت البطاقات بعرض كامل) — أُصلح بإعادة هيكلة: شبكة للبطاقات فقط + منطقة تفصيل بعرض كامل تحتها، وأُعيدت كل الاختبارات واللقطات بعد الإصلاح.

## ⑤ عدم الانحدار — بالأرقام

| الحزمة | النتيجة |
|---|---|
| place-intelligence (PI-1) | **26/26 ✅** |
| place-discovery (PI-2) | **13/13 ✅** |
| place-archive (PI-3) | **13/13 ✅** |
| place-archive-proof2 (إغلاق مراجعة PI-3) | **6/6 ✅** |
| place-archive-view (PI-4) | **16/16 ✅** |
| **الانحدار الشامل** | **201/213 — نفس الـ12 فشل البيئية المعروفة من baseline** |

## ⑥ حالة المستودع

```
 M public/operations-dashboard.html   (+168 — قسم العرض الجديد)
 M server.js                          (+31 — منها سطر PI-4 additive؛ الباقي PI-3 المعتمد)
 M shift-archive-engine.js            (PI-3 المعتمد — لم يُمس في هذه الجولة)
?? scripts/place-archive-view-test.js  scripts/pi4-preview-gen.js
```

- آخر commit: **`ba8bc29`** — لم يتغير.
- **لا Commit / لا Push / لا Deploy تم.**

## ⑦ قيود معلنة بصدق

1. **التبويب يعرض المناوبات المؤرشفة فقط** — النشطة ترى «يُختم قسم الجهات والمواقع عند أرشفة المناوبة» (قرار تصميمي: القسم أرشيفي تعريفًا؛ أي عرض حي = مرحلة مستقلة).
2. **وقت/تاريخ البلاغ نفسه** ليس ضمن مدخل القسم المختوم (اللقطة تحفظ `resolvedAt` وقت الحسم) — المعروض هو وقت الحسم بصدق؛ إن رغب المالك بوقت البلاغ في البطاقة فهو توسعة على بنية القسم تحتاج أمرًا منفصلًا.
3. لقطات الشاشة من صفحات معاينة معزولة (نفس الكود والـCSS والبيانات المختومة)، وليست من الخادم الحي — لقطة الخادم الحي تتطلب نشرًا، وهو ممنوع حاليًا.

## ⑧ الخلاصة

PI-4 مكتمل محليًا وفق النسخة المعتمدة بتعديلاتها الأربعة: العرض من اللقطة حصرًا، صفر منطق في الواجهة، Likely مطوي منفصل، Unknown سطر معلومة، المستوى الرابع بطاقة تدقيق داخلية بلا انتقال للبلاغ الحي، وV9/V10 مغلقتان بالأدلة. الأرشيف الآن يحوّل التعرف على الجهات إلى إحصائية قابلة للحفر والتدقيق.

**الحالة: بانتظار مراجعة المالك واعتماده. لا Commit / Push / Deploy إلا بأمر صريح.**
