# وثيقة قرار — مؤشر المساهمة التشغيلية: المرحلة الأولى (واجهة الإحصائيات)
**الحالة: معتمدة من المالك** | تاريخ الاعتماد: 2026-08-14 | النطاق: تنفيذ المرحلة الأولى فقط

## 1. الاعتماد
اعتماد المالك لمواصفة الفحص والتصميم (`employee-contribution-indicator-data-map.md` + `contribution-indicator-design.md` في مساحة العمل) مع قرار التصنيف الإداري: مطابقة تامة على `employees.job_title` — القيادة الميدانية (`كبير مسعفين`، `مساعد كبير مسعفين`)، العمليات (`تحكم عملياتي`، `تنسيق استجابة`). لا يُستخدم `users.role` لهذا الغرض.

## 2. نطاق المرحلة الأولى (ملزم)
- **واجهة إحصائيات فقط**: أعداد خام لكل موظف مصنَّف، شهريًا — بلا نقاط، بلا ترتيب، بلا مقارنة بين الفئتين.
- المرحلة الثانية (أوزان/مؤشر مركّب) تُقرر بعد رؤية أول نتيجة شهرية.
- تسجيل الدخول/مدة الصفحة/الرسائل مستبعدة من الإنجاز (قرار المالك — بند سابعًا).

## 3. القرارات المعمارية
| القرار | التفصيل | المسوّغ |
|---|---|---|
| المالك | توسيع `IndicatorService` (قراءة فقط) بمنهج `getEmployeeContribution(year, month)` — لا خدمة جديدة | المؤشرات مملوكة أصلًا لـ Indicator Service؛ إضافة قرائية لا تمس مصادر الحقيقة |
| العقد الجديد | `GET /api/indicators/contribution?year=&month=` — `authenticate` + `authorize(['admin','director'])` | بيانات أداء موظفين = امتياز إداري |
| الساعات/المناوبات | إعادة استخدام محلل الرموز المركزي في `ScheduleMetricsService` (حقن متأخر — نمط StaffingEventsService) | ممنوع نسخ منطق مدة الرموز («مركزي هنا فقط») |
| مفاتيح الفاعل | لكل موظف: `{ String(users.id), users.username(=الكود) }`؛ `shift_forms.created_by` = الكود؛ `shift_alerts.acknowledged_by` = الاسم | اختلاف صيغ الختم بين الجداول (موثق في تقرير الفحص) |
| حدود الشهر | `substr(created_at,1,7)` على الختم المخزن (UTC) | تحفظ موثق: حد الشهر قد يزيح ±3 ساعات عن الرياض — مقبول للمرحلة الأولى |
| الواجهة | صفحة جديدة `public/contribution-stats.html` — عرض فقط، بلا منطق تشغيلي (قاعدة Zero Business Logic) | لا تعديل على أي صفحة قائمة (Zero Visual Regression) |
| الترتيب في العرض | أبجدي بالاسم داخل كل فئة — ترتيب محايد وليس ترتيب أداء | «بدون ترتيب» |

## 4. سلال الأعمال المعروضة (أعداد خام، كلها من مصادر الفحص الموثقة)
- التوظيف: الساعات المجدولة، عدد المناوبات (رموز دوام فعلي)، أيام بلا ساعات قابلة للاشتقاق.
- التكميل (`audit_log: completion_saved`) · توزيع البلاغات = إجراءات (`report_created`) · تراجع (`report_undone`) · بلاغات تفصيلية (`report_entry_added`).
- التمركزات (`positioning_events`: created/updated/ended) · تسجيل خروج الفرق (`shift_signout_events`).
- النماذج (`shift_forms`، تفصيل بالنوع).
- أحداث القوى البشرية / المركبات / اللوجستيات / المراكز (`operational_events` حسب domain).
- سير العمل (`workflow_audit_log`: create/approve/pdf/reissue/edit_fields) · تعديلات الجدول (`audit_log`: shift_cell_update/employee_transfer_*/roster_*) · ملفات (`audit_log`: doc_uploaded/ops_files_uploaded) · تنبيهات معالجة (`shift_alerts.acknowledged_by`).

## 5. تحفظات معروضة بصراحة في الواجهة
- توزيع البلاغات يعدّ **الإجراءات** لا البلاغات المفردة.
- القيادة الميدانية تُقاس بأعمالها **المسجلة** فقط؛ المتابعة بلا كتابة غير موجودة بنيويًا.
- ملاحظات المناوبة مستبعدة (لا ختم مؤلف).
- `فيصل احمد شريف الغامدي (101886)` بلا حساب دخول — لن تظهر له أعمال.

## 6. خارج النطاق (المرحلة الثانية)
نقاط/أوزان، ترتيب، مقارنة بين الفئتين، مؤشر نتيجة الجاهزية لنطاق القائد، فئة دعم لوجستي/مساعد صحي، مؤشرات النشاط (جلسات/دردشة).
