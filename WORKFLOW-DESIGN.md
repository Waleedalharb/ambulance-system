# تصميم منظومة «سير العمل الرسمي» (Official Shift Workflow)

**الحالة:** مسودة تصميم v2 — تتضمن ملاحظات مراجعة المالك الثماني — بانتظار الاعتماد النهائي. لا يُكتب أي كود قبل الاعتماد.
**التاريخ:** 2026-07-24
**المرجع:** مواصفة المالك + مراجعته للتصميم v1 (معتمدان كمرجع ملزم).

---

## 0. المبادئ الحاكمة (غير قابلة للتفاوض)

1. **الاستقلالية التامة:** المنظومة جديدة بالكامل — خدمة، جداول، مسارات، شاشات — ولا تُعدّل أي سطر في أنظمة التكميل/المركبات/الأرشيف المستقرة.
2. **قراءة فقط من التكميل:** كل بيانات المناوبة تُقرأ من المصادر القائمة ولا تُكتب فيها أبدًا.
3. **الاتجاه الآخر ممنوع أيضًا:** اعتماد سير العمل ثم تعديل التكميل لا يغيّر النسخة المعتمدة؛ إعادة الإرسال = نسخة جديدة.
4. **Append Only:** لا حذف لأي نسخة أو سجل تدقيق — الإلغاء حالة موثقة وليس حذفًا.
5. **الختم سيرفري:** المناوبة، الوقت، الفاعل، رقم المرجع — كلها تُختم من الخادم.
6. **النسخة المعتمدة مستقلة 100%:** لقطة كاملة للمناوبة تُخزن مع النسخة؛ لو تغيرت بيانات التكميل بعد سنة، تُفتح النسخة كما كانت يوم اعتمادها حرفيًا.
7. **لا برمجة قبل الموافقة البصرية:** مرحلة W-0 (نموذج واجهة تجريبي) تُعتمد قبل W-1.

---

## 1. تسلسل سير العمل خطوة بخطوة

```
المشرف ينهي التكميل ويراجع الفرق
        │
        ▼
(1) «إعداد سير العمل»  ──►  POST /api/workflow/prepare
        │   الخادم يسحب «اللقطة الكاملة» (§7) قراءة فقط + ينشئ مسودة V1
        │   حالة المناوبة: بانتظار الإعداد ──► تم إعداده
        ▼
(2) شاشة سير العمل: بيانات مسحوبة + حقول المشرف + خانة «تمت المراجعة بواسطة»
        │   (حفظ تلقائي في المسودة)
        ▼
(3) «اعتماد سير العمل»  ──►  POST /api/workflow/version/:id/approve (ذرّي):
        │   قفل + ختم المعتمِد + منح رقم المرجع الرسمي + حساب Hash النسخة
        │   + توليد PDF الحكومي (بالمرجع وQR والتوقيع) + أرشفة
        │   حالة المناوبة ──► تم اعتماده
        ▼
(4) «إرسال» (لاحقًا عند ربط البريد) — approved فقط
        │   حالة المناوبة ──► تم إرساله ──► تم استلامه (تأكيد موثق)
        ▼
(5) تعديل بالتكميل بعد الاعتماد؟ النسخة لا تتغير.
        «إعادة إصدار» ──► V2 مسودة جديدة + سبب إلزامي.
```

---

## 2. الشاشات المطلوبة

### 2.1 مرحلة W-0 — النموذج التجريبي (Prototype) أولًا
- ملف HTML مستقل ببيانات وهمية (لا خادم ولا قاعدة) يعرض **كل** الشاشات وحالاتها:
  الشاشة الرئيسية بكل الأقسام، أماكن الأزرار، تدفق الاعتماد، شكل PDF الحكومي (صفحة HTML بتنسيق الطباعة)، شاشة الأرشيف، سجل النسخ، حالات المناوبة الخمس.
- **يعرض على المالك ويُعتمد بصريًا قبل كتابة أي خدمة أو جدول.**

### 2.2 نقطة الدخول (لمسة واحدة فقط على الشاشات القائمة)
- زر جديد **«إعداد سير العمل»** يُضاف في شاشة التكميل بجانب «عرض الملخص» — إضافة فقط، بلا تغيير أي سلوك قائم. (يُحسم مكانه نهائيًا في مراجعة W-0.)

### 2.3 شاشة سير العمل `public/workflow.html` (جديدة — قلب المنظومة)
| القسم | المحتوى | المصدر |
|---|---|---|
| رأس الوثيقة | شعار المنصة، اسم القطاع، التاريخ، رقم المناوبة ونوعها، بدايتها/نهايتها، **رقم المرجع** (بعد الاعتماد)، رقم الإصدار وحالته | اللقطة + النسخة |
| شريط حالة المناوبة | بانتظار الإعداد ← تم إعداده ← تم اعتماده ← تم إرساله ← تم استلامه | مشتق من النسخ |
| جدول الفرق | كل فرقة: الحالة، الكادر، الغائب/المتأخر بسببه، الداعمون، المكلَّفون، المركبة ورقمها وحالتها | اللقطة الكاملة |
| ملخص القوى والمؤشرات | الكادر/الحاضر/الغائب/المتأخر/الدعم + جاهزية المركبات + مؤشرات المناوبة | اللقطة الكاملة |
| الملاحظات | ملاحظات التكميل العامة كما سجّلها المشرف أثناء المناوبة | اللقطة الكاملة |
| حقول المشرف | ملخص المناوبة، الملاحظات التشغيلية، أبرز الأحداث، المشاكل التشغيلية، التوصيات | إدخال (مسودة فقط) |
| خانة «تمت المراجعة بواسطة» | ☐ مشرف العمليات ☐ مدير القطاع ☐ المناوبة الإداري (تظهر في الـ PDF حتى لو غير مفعّلة تشغيليًا الآن) | إدخال (مسودة فقط) |
| شريط الإجراءات | حفظ تلقائي • اعتماد • تحميل PDF • إرسال (معطّل حتى الاعتماد) • إعادة إصدار • إلغاء مسودة | حسب حالة النسخة |
| سجل النسخ | V1..Vn: المرجع، الحالة، من أنشأ/اعتمد/أرسل، الأوقات، سبب إعادة الإصدار | الخادم |

### 2.4 أرشيف سير العمل (تبويب داخل `workflow.html`)
- قائمة المناوبات التي لها نسخ + حالة كل مناوبة + فتح أي نسخة للعرض (المعتمدة قراءة فقط) وتحميل PDFها.

---

## 3. حالات النظام

### 3.1 حالات النسخة (State Machine)
| الحالة | الوصف | الإجراءات المسموحة |
|---|---|---|
| `draft` | مسودة مفتوحة | تحرير الحقول وخانة المراجعة، اعتماد، إلغاء |
| `approved` | معتمدة ومقفلة + مرجع + PDF | عرض، تحميل PDF، إرسال (لاحقًا)، إعادة إصدار، تعديل استثنائي admin |
| `sent` | أُرسلت (لاحقًا) | عرض، PDF، إعادة إصدار، تأكيد استلام، تعديل استثنائي admin |
| `cancelled` | مسودة أُلغيت (لا تُحذف) | عرض فقط |

- اتجاه واحد: `draft → approved → sent`. لا رجوع إلا بتعديل استثنائي موثق (admin) أو إعادة إصدار.
- مسودة واحدة مفتوحة لكل مناوبة كحد أقصى.

### 3.2 حالة سير العمل على مستوى المناوبة (للمتابعة الإدارية)
`بانتظار إعداد سير العمل ← تم إعداده ← تم اعتماده ← تم إرساله ← تم استلامه`
- تُشتق من حالات النسخ (لا تُخزن منفصلة ولا تُحسب محليًا في الواجهة).
- «تم استلامه»: حاليًا تأكيد يدوي موثق من admin بعد الإرسال؛ لاحقًا قد تكون تلقائية من مزود البريد.

## 4. قواعد الاعتماد

1. الصلاحية: `admin` أو `director`.
2. الاعتماد **عملية ذرّية واحدة** تنجح كاملة أو تفشل كاملة: قفل + ختم المعتمِد والوقت + **منح رقم المرجع** (عدّاد ذرّي) + **حساب Hash النسخة** + توليد PDF + أرشفة.
3. لا PDF رسمي ولا إرسال ولا رقم مرجع قبل الاعتماد.
4. معاينة المسودة: HTML فقط بعلامة «مسودة — غير معتمدة».

## 5. قواعد القفل بعد الاعتماد

1. النسخة المعتمدة/المرسلة قراءة فقط للجميع — الخادم يرفض التحرير (403).
2. الاستثناء الوحيد: **admin** عبر «تعديل استثنائي» صريح — يوثق بالقيمة قبل/بعد وسبب إلزامي، ولا يغيّر رقم الإصدار ولا المرجع ولا ختم الاعتماد الأصلي، و**يستلزم إعادة توليد PDF وHash جديدين موثقين في السجل**.
3. أي إعادة إرسال = إعادة إصدار (نسخة جديدة)، لا تعديل على المرسلة.

## 6. رقم المرجع الرسمي

- الصيغة: `SRCA-SR-OPS-YYYY-NNNNNN` (مثال: `SRCA-SR-OPS-2026-000145`).
- يُمنح **عند الاعتماد فقط** (وثيقة رسمية = مرجع رسمي)، من عدّاد سنوي ذرّي (`workflow_counters`) داخل معاملة الاعتماد — لا يتكرر ولا يُعاد استخدامه ولو أُلغيت نسخة.
- يظهر في: الشاشة، الـ PDF (الرأس والـ Footer)، الـ QR، أرشيف سير العمل، وسطر الموضوع عند الإرسال لاحقًا.

## 7. اللقطة الكاملة (Snapshot) — استقلالية النسخة 100%

`snapshot_json` يحفظ **كل** ما يشكّل المناوبة يومها:

| العنصر | المصدر (قراءة فقط) |
|---|---|
| هوية المناوبة (id/نوع/تاريخ/بداية/نهاية/حالة) | المناوبة النشطة (ختم سيرفري) |
| الفرق وحالاتها وكادرها (أسماء + أدوار) | `/api/staffing/state` |
| الغياب والتأخير (الاسم + السبب + الوقت) | نفس المصدر + سجل الأحداث |
| الدعم (الداعم/الفريق/من متى/مؤقت) | نفس المصدر |
| التكليف | نفس المصدر |
| المركبات: التعيينات، الحالات، أرقامها، غير المعيّنة | `/api/vehicles/board` |
| ملخص القوى البشرية + المؤشرات | `/api/staffing/state` + `/api/staffing/indicators` |
| ملاحظات التكميل العامة | `/api/completion/latest` (قراءة) |
| المشرفون المتفاعلون (من سجّل أحداث المناوبة) | سجل الأحداث (أسماء الفاعلين) |
| **وقت إنشاء اللقطة** (UTC + عرض السعودية) | الخادم |
| `snapshot_schema` (رقم إصدار بنية اللقطة) | ثابت الخدمة — لقراءة النسخ القديمة مستقبلًا |

العرض لاحقًا يعتمد على اللقطة المخزنة فقط — لا يعيد قراءة التكميل أبدًا.

## 8. آلية إنشاء PDF — وثيقة حكومية وليست نسخة شاشة

**قرار التقنية (محسوم بمتطلب المالك «ليست Screenshot»):** `pdfkit` + `arabic-reshaper` + خط عربي TTF مضمّن (Amiri) — توليد برمجي كامل التحكم. + مكتبة `qrcode` للـ QR. (3 تبعيات خفيفة تُثبَّت عند W-4.)

**تخطيط الوثيقة:**
```
┌──────────────────────────────────────────┐
│ [الشعار]   هيئة الهلال الأحمر — قطاع الجنوب │  ← رأس رسمي
│         سير العمل الرسمي للمناوبة          │
│  المرجع: SRCA-SR-OPS-2026-000145  V2      │
├──────────────────────────────────────────┤
│ بيانات المناوبة (تاريخ/نوع/بداية/نهاية)    │
│ جدول الفرق والكادر والحالات                │
│ الغياب والتأخير والدعم والتكليف            │
│ المركبات وحالاتها                          │
│ ملخص القوى والمؤشرات + الملاحظات           │
│ حقول المشرف الخمسة                         │
├──────────────────────────────────────────┤
│ تمت المراجعة بواسطة: ☐ ☐ ☐                │
│ أعدّه: ______   اعتمده: وليد الحربي ______ │
│ التوقيع: ______   [مكان التوقيع الإلكتروني]│
│ [الختم]                          [QR]      │
├──────────────────────────────────────────┤
│ Footer: المرجع • رقم الصفحة • وقت الإصدار  │
└──────────────────────────────────────────┘
```
- **التوقيع الإلكتروني:** منطقة مخصصة من الآن (اسم المعتمِد مطبوع + سطر توقيع + حقل `signature_meta` في قاعدة البيانات جاهز للربط المستقبلي دون تغيير التصميم).
- **التخزين:** `data/archives/workflows/SRCA-SR-OPS-YYYY-NNNNNN.pdf` + المسار في قاعدة البيانات.

## 9. QR التحقق من الأصالة

- أسفل كل PDF — المسح يعرض: رقم المرجع، رقم الإصدار، الحالة، المعتمِد، تاريخ الاعتماد، وأول 16 خانة من `content_hash`.
- `content_hash` = SHA-256 على (اللقطة + المعتمِد + وقت الاعتماد + المرجع) — يُخزن مع النسخة عند الاعتماد.
- محتوى الـ QR: نص التحقق ذاته (يعمل بلا شبكة) + رابط `GET /api/workflow/verify/:ref` الذي يطابق المرجع/الـ Hash ويعرض «أصلية ✓» (يعرض بيانات التحقق فقط، بلا كشف بيانات المناوبة؛ متاح بلا مصادقة ليعمل المسح من أي جهاز — قابل للتشديد بقرار).
- بعد أي «تعديل استثنائي» (§5) يُعاد حساب الـ Hash ويُوثق القديم والجديد في سجل التدقيق.

## 10. آلية الأرشفة

1. كل نسخة (بأي حالة) في `shift_workflows` بلقطتها الكاملة — الاستعراض لا يعتمد على بقاء بيانات التكميل.
2. ملفات PDF على القرص (§8) + المسار في القاعدة.
3. تبويب «أرشيف سير العمل»: تصفح حسب المناوبة/التاريخ/الحالة/المرجع.
4. لا تكامل مع محرك أرشيف المناوبات الحالي في هذه النسخة (قرار مستقبلي).

## 11. آلية الإرسال (مؤجلة التفعيل — مصممة الآن)

1. **سجل مزودين (Provider Registry):** عقد واحد `send({ to[], subject, body, attachments[] })` وثلاثة مزودين:
   - `NullMailProvider` (الحالي): لا يرسل؛ دورة الإرسال تعمل كاملة وتسجل `pending_provider`.
   - `SmtpMailProvider` (جاهز البنية، يُفعَّل بإعدادات).
   - `GoogleMailProvider` (جاهز البنية، يُفعَّل بإعدادات).
2. التبديل لاحقًا = إعداد واحد (`mail_provider` + بيانات الاعتماد + `recipients[]`) دون لمس أي كود آخر.
3. **الاعتماد شرط الإرسال** (400 لغير approved). سطر الموضوع: «سير العمل الرسمي — <المرجع> — مناوبة <النوع> <التاريخ>».

## 12. آلية إعادة الإصدار (Versioning)

1. `version_no` تسلسلي لكل مناوبة — لا يُعاد استخدام رقم أبدًا.
2. إعادة الإصدار من `approved/sent` فقط + **سبب إلزامي**.
3. النسخة الجديدة: لقطة كاملة **جديدة** من التكميل الحالي + نسخ حقول المشرف من السابقة قابلة للتحرير + `draft` بدورة اعتماد كاملة (مرجع جديد وHash جديد عند اعتمادها).
4. سجل النسخ يعرض: الرقم، المرجع، الحالة، من أنشأ/اعتمد/أرسل، الأوقات، سبب إعادة الإصدار.

## 13. سجل التدقيق (Audit Log)

- جدول `workflow_audit_log` — **Append Only، لا UPDATE/DELETE له إطلاقًا**.
- يُسجَّل: إنشاء مسودة، تحرير حقول/خانة مراجعة، اعتماد (مع المرجع والـ Hash)، توليد PDF، إرسال/محاولة، تأكيد استلام، إعادة إصدار، إلغاء، تعديل استثنائي (قبل/بعد + السبب + Hash قديم/جديد).
- كل سجل: الفاعل (id + اسم من الجلسة)، الوقت السيرفري، تفاصيل JSON.

## 14. نقاط التكامل مع نظام التكميل (قراءة فقط — القائمة الكاملة)

| يقرأ من | ماذا | كتابة؟ |
|---|---|---|
| `/api/staffing/state` | الفرق/الحالات/الكادر/الغياب/التأخير/الدعم/التكليف/ملخص القوى | ❌ أبدًا |
| `/api/staffing/indicators` | مؤشرات المناوبة | ❌ أبدًا |
| `/api/vehicles/board` | تعيينات وحالات المركبات وغير المعيّنة | ❌ أبدًا |
| `/api/completion/latest` | ملاحظات التكميل العامة | ❌ أبدًا |
| `/api/staffing/timeline` | أسماء المشرفين المتفاعلين (فاعلو الأحداث) | ❌ أبدًا |
| `/api/current-shift` | هوية المناوبة النشطة | ❌ أبدًا |
| شاشة التكميل | **زر دخول واحد مضاف** | لا تغيير سلوك |

- لا triggers ولا كتابة في أي جدول قائم. **ضمانة آلية:** البوابة (192/192) تبقى خضراء بعد كل مرحلة دون تعديل اختبار واحد.

## 15. الـ APIs الجديدة المقترحة (جديدة بالكامل)

| المسار | الوظيفة | الصلاحية |
|---|---|---|
| `POST /api/workflow/prepare` | إنشاء/إرجاع مسودة بسحب اللقطة الكاملة | admin, director |
| `GET /api/workflow/shift/:shiftId` | نسخ المناوبة + حالة سير العمل | admin, director |
| `GET /api/workflow/version/:id` | نسخة كاملة من لقطتها المخزنة | admin, director |
| `PUT /api/workflow/version/:id` | حفظ حقول المشرف + خانة المراجعة (draft فقط) | admin, director |
| `POST /api/workflow/version/:id/approve` | اعتماد ذرّي: قفل+مرجع+Hash+PDF+أرشفة | admin, director |
| `POST /api/workflow/version/:id/reissue` | نسخة جديدة (سبب إلزامي) | admin, director |
| `POST /api/workflow/version/:id/send` | دورة إرسال عبر المزود الحالي | admin, director |
| `POST /api/workflow/version/:id/acknowledge` | تأكيد استلام موثق | admin |
| `POST /api/workflow/version/:id/cancel` | إلغاء مسودة موثق | admin, director |
| `POST /api/workflow/version/:id/override` | تعديل استثنائي بعد القفل (سبب إلزامي + Hash جديد) | **admin فقط** |
| `GET /api/workflow/version/:id/pdf` | تحميل الـ PDF | admin, director |
| `GET /api/workflow/version/:id/audit` | سجل تدقيق النسخة | admin |
| `GET /api/workflow/verify/:ref` | تحقق أصالة بالمرجع/الـ Hash (للـ QR) | عام (بيانات تحقق فقط) |

## 16. الجداول الجديدة المقترحة (جديدة بالكامل)

**`shift_workflows`** — `id` • `shift_id` • `version_no` • `status` • `ref_no` (NULL حتى الاعتماد، فريد) • `content_hash` • `snapshot_json` (اللقطة الكاملة §7) • `fields_json` (الحقول الخمسة + `reviewed_by[]`) • `signature_meta` (جاهز للتوقيع الإلكتروني) • `reissue_reason` • `pdf_path` • `send_status` • `recipients_json` • `created_by/at` • `approved_by/at` • `sent_at` • `acknowledged_by/at` — فريد `(shift_id, version_no)` + قيد مسودة واحدة مفتوحة لكل مناوبة.

**`workflow_audit_log`** — `id` • `workflow_id` • `version_no` • `action` • `actor_id` • `actor_name` • `at` • `details_json` — بلا UPDATE/DELETE.

**`workflow_counters`** — `year` • `next_seq` — عدّاد رقم المرجع الذرّي.

## 17. مراحل التنفيذ (توقف ومراجعة بعد كل مرحلة)

| المرحلة | النطاق | معيار الإغلاق |
|---|---|---|
| **W-0** | **نموذج واجهة تجريبي (HTML ببيانات وهمية): كل الشاشات + شكل PDF الحكومي + الأرشيف + الحالات** | **اعتماد المالك البصري** |
| **W-1** | الجداول الثلاثة + `workflow-service.js` + اللقطة الكاملة + prepare/list/get/put | اختبارات بوابة جديدة + 192/192 خضراء |
| **W-2** | شاشة `workflow.html` الحقيقية (حسب W-0 المعتمد) + زر الدخول | تجربة المالك من الواجهة |
| **W-3** | الاعتماد/القفل/الإلغاء/التعديل الاستثنائي + Audit Log + Versioning + رقم المرجع | بوابة + تجربة المالك |
| **W-4** | PDF الحكومي (pdfkit) + QR + التوقيع + الأرشيف + التحميل + مسار التحقق | PDF فعلي بيد المالك + مسح QR |
| **W-5** | سجل المزودين الثلاثة + دورة send + send_status + تأكيد الاستلام | بوابة + عرض «جاهز للإرسال» |

## 18. قرارات حُسمت في مراجعة المالك (v2)

1. ✅ W-0 نموذج تجريبي قبل أي برمجة.
2. ✅ PDF حكومي برمجي (pdfkit) وليس نسخة شاشة.
3. ✅ رقم مرجع رسمي `SRCA-SR-OPS-YYYY-NNNNNN` عند الاعتماد.
4. ✅ QR تحقق + content_hash.
5. ✅ اللقطة الكاملة (استقلالية 100%).
6. ✅ حالات المناوبة الخمس (حتى «تم استلامه»).
7. ✅ مكان التوقيع الإلكتروني محجوز من البداية.
8. ✅ خانة «تمت المراجعة بواسطة».
9. ✅ مزودو البريد الثلاثة (Null/SMTP/Google).

**بند واحد يُحسم بصريًا في W-0:** مكان زر الدخول النهائي وشكل الأرشيف (داخل الشاشة أم مستقل).
