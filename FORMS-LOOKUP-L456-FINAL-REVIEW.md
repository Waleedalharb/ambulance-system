# FORMS-LOOKUP — التقرير الشامل L-4 / L-5 / L-6 (تنفيذ محلي فقط)

**التاريخ:** 2026-09-02 · **النطاق:** دفعة واحدة محلية بأمر المالك (21:05) — L-4 نقطة تحقق، ثم L-5 ثم L-6 بلا توقف · **بلا Commit / Push / Deploy**

---

## 1) الخلاصة التنفيذية

| المرحلة | النطاق | الاختبار | النتيجة | الحالة |
|---|---|---|---|---|
| L-4 | اليومي + كبار المسعفين (تعبئة من المصادر الداخلية) | `daily-senior-autofill-test.js` | **13/13 ✅** (أُعيدت ضمن هذه الدفعة) | منفذ — نقطة تحقق ناجحة |
| L-5 | الإسعاف الجوي — قائمة الفرقة من المصدر الحي | `air-linked-forms-test.js` (فحوص 1–5) | **5/5 ✅** | منفذ مع نقطة موقوفة (§3) |
| L-6 | «النماذج المرتبطة بالبلاغ» في بطاقة البلاغ الحية | `air-linked-forms-test.js` (فحوص 6–10) | **5/5 ✅** | منفذ |

**انحدار كامل بعد انتهاء الدفعة (كلها خضراء):**

| الاختبار | النتيجة |
|---|---|
| L-1 API — `incident-lookup-api-test.js` | 14/14 ✅ |
| L-1 UI — `incident-lookup-ui-test.js` | 13/13 ✅ |
| L-2 — `e-form-lookup-test.js` | 16/16 ✅ (شمل انحدارين داخليين) |
| L-3 — `incident-esc-lookup-test.js` | 16/16 ✅ |
| L-4 — `daily-senior-autofill-test.js` | 13/13 ✅ |
| L-5/L-6 — `air-linked-forms-test.js` | 10/10 ✅ |
| الثيم — `forms-theme-test.js` | 47/47 ✅ |
| أخطاء Console في كل الجلسات | **صفر** |

---

## 2) L-4 — اليومي + كبار المسعفين (نقطة تحقق)

نُفّذ سابقًا ضمن هذه الدفعة وأعيد اختباره اليوم: **13/13 ✅** — التفاصيل الكاملة في `FORMS-LOOKUP-L4-EXECUTION.md`. القرارات المعتمدة المطبقة: رقم اليومي `DAILY-YYYY-MM-DD` read-only · `dailyAir` read-only من `GET /api/air-ambulance` · الفرق المستجيبة يدوية · قائمتا كبار المسعفين من `GET /api/employees` بالمطابقة التامة (`job_title` + `is_active`) مع خيار «أخرى» · المركبات يدوية · `senCmdrName` يدوي.

## 3) L-5 — الإسعاف الجوي

### النقطة الموقوفة (لم تُنفَّذ — بانتظار حسم المالك)
**ربط `airReportNumber` بـIncident Lookup / CAD** — السؤال التشغيلي «هل طلبات الجوي بلاغات CAD؟» غير محسوم من التشخيص، والـplaceholder الحالي (`2025-AIR-001`) لا يشبه أرقام CAD. حسب الأمر الحاكم: **أُوقفت هذه النقطة وحدها** ولم يُخترع أي قرار لها — حقل رقم البلاغ يبقى يدويًا كما هو حرفيًا (بما فيه الـplaceholder).

### الجزء المنفذ (لا يعتمد على النقطة الموقوفة)
- قائمة الفرقة `airUnit` تُعبَّأ ديناميكيًا من **`GET /api/cad-reports/south-teams`** (فرق الجنوب الفعلية من تكميل المناوبة النشطة).
- **Fallback موثق:** عند فشل الجلب أو غياب مناوبة نشطة (`teams:[]`) تبقى القائمة الثابتة الموجودة في `form-air.html` كما هي — لا يُحجب الحفظ أبدًا.
- اختيار سابق يُستعاد إن بقي موجودًا في القائمة الحية.
- بقية حقول الجوي كلها يدوية كما هي — **صفر تغيير في منطق الحفظ أو الواتساب أو العرض**.

### نتائج فحوص L-5
1. ✅ `south-teams` ← 200 + `teams:[]` عند أرشفة كل المناوبات (temp).
2. ✅ فتح الجوي بلا مناوبة نشطة ← القائمة الثابتة (23 فرقة + placeholder) تبقى.
3. ✅ فشل الجلب (!ok) ← القائمة الثابتة تبقى.
4. ✅ نجاح الجلب ← القائمة تُستبدل بالحية (placeholder + الفرق الفعلية فقط).
5. ✅ حفظ طلب جوي بفرقة من القائمة الحية يمر (انحدار `saveAirAmbulance`).

## 4) L-6 — «النماذج المرتبطة بالبلاغ» في بطاقة البلاغ الحية

- قسم **عرض فقط** جديد أسفل بطاقة البلاغ الحية (`incidentCardHtml` في `smart-map.js`): «النماذج المرتبطة بهذا البلاغ».
- **جلب كسول** عند فتح البطاقة فقط (لا جلب مسبق لكل بلاغات الخريطة) + كاش لكل رقم بلاغ.
- يقرأ `GET /api/e-cases` + `/api/incidents` + `/api/escalations` (كلها `authenticate` فقط — **صفر تعديل خادم**) ويرشّح بمطابقة `incidentNumber` **التامة**.
- شارة لكل نموذج مرتبط (نموذج E / حادث / تصعيد) مع تاريخ الإنشاء، أو «لا توجد نماذج مرتبطة بهذا البلاغ».
- حارسان: لا يُكتب في بطاقة استُبدلت/أُغلقت أثناء الجلب، وغياب `AuthManager` يعرض «—» بلا كسر.
- **متوقع وموثق:** السجلات القديمة بلا `incidentNumber` لن تطابق — هذا صحيح وليس خللًا.
- بطاقة ذاكرة الخريطة التاريخية **خارج النطاق** (RC-6 مغلق — لم تُمسّ).

### نتائج فحوص L-6
6. ✅ بطاقة 1310627: شارتا «نموذج E» و«حادث» — ولا تصعيد (تصعيد temp مربوط ببلاغ آخر 77777777).
7. ✅ سجل e_case قديم بلا `incidentNumber` لا يظهر (المطابقة التامة فقط).
8. ✅ بطاقة 1310628 (بلا نماذج) ← «لا توجد نماذج مرتبطة بهذا البلاغ».
9. ✅ صفر أخطاء console/استثناءات.
10. ✅ اللقطات الأربع محفوظة.

**اللقطات:** `test-output/air-linked-forms-1788373310602/` — `shot-1-air-static` (القائمة الثابتة) · `shot-2-air-live` (الحية) · `shot-3-card-linked` (الشارتان ظاهرتان) · `shot-4-card-none`.

---

## 5) الملفات والـDiff المفصول حسب المرحلة

> ملفات الـpatch الجاهزة للمراجعة في **`review-l456/`**.

| المرحلة | الملفات | الحجم |
|---|---|---|
| **L-4** | `public/forms/form-daily.html` · `public/forms/form-senior.html` · hunks اليومي/كبار في `public/js/app.js` · `scripts/daily-senior-autofill-test.js` | `review-l456/L4-forms.patch` (48 سطرًا) + `review-l456/L4-app.patch` (159 سطرًا) |
| **L-5** | hunk `initForm_air` + دالة `loadAirUnitList` الجديدة في `public/js/app.js` · سطر رفع `app.js?v=53` في `public/index.html` | `review-l456/L5-app.patch` (45 سطرًا — إضافة صِرفة) |
| **L-6** | `public/js/smart-map.js` (+63/−1: حاوية القسم + `loadLinkedForms` + استدعاء الجلب الكسول في `showCard`) · `public/css/executive-theme.css` (+37: أنماط `.smap-linked-forms` داكنة + Light Ops) · سطرا رفع `smart-map.js?v=8` و`executive-theme.css?v=7` في `index.html` | `review-l456/L6-map-card.patch` (319 سطرًا) |
| مشترك L-5/L-6 | `scripts/air-linked-forms-test.js` (جديد، 273 سطرًا) | `review-l456/air-linked-forms-test.js` |
| كل hunks app.js (L-2→L-5) للمراجعة الشاملة | — | `review-l456/app-all-hunks.patch` (740 سطرًا) |
| رفع النسخ | `public/index.html` | `review-l456/index-version-bumps.patch` |

**تغييرات هذه الجلسة تحديدًا (L-5/L-6):** `public/js/app.js` (hunk واحد إضافي) · `public/js/smart-map.js` · `public/css/executive-theme.css` · `public/index.html` (3 سطور نسخ) · اختبار جديد. **صفر** حذف أو تعديل لأي منطق قائم.

## 6) تأكيد الـNo-Go (مُتحقق منه عبر `git status`)

- ✅ **صفر** تعديل على `server.js` / قاعدة البيانات / migrations في هذه الدفعة.
- ✅ **صفر** تعديل على L-1 المعتمدة (`incident-lookup.js/css` + نقطة lookup) وعلى منطق L-2/L-3 المعتمدين.
- ✅ **PI-10 معلّق وخارج النطاق تمامًا** — ملفاته المحلية (`server.js`, `place-*`, `storage-adapter.js`, `report-service.js`) لم تُلمس في هذه الجلسة وتبقى غير منشورة.
- ✅ **صفر** تعديل على الثيم خارج أنماط قسم L-6 الجديدة (إضافة صِرفة، وانحدار الثيم 47/47 أخضر).
- ✅ لا `incident_registry` ولا `report_times` ولا إعادة حساب أزمنة — L-6 قراءة عرض فقط من نقاط GET القائمة.
- ✅ لا Commit / Push / Deploy.

## 7) ما ينتظر قرار المالك

1. **مراجعة هذه النتيجة** ثم قرار نشر مفصول لكل مرحلة (L-4 / L-5 / L-6) كما فعلنا مع RC-6 والمرحلة C.
2. **النقطة الموقوفة في L-5:** هل طلبات الإسعاف الجوي بلاغات CAD (تُربط بـIncident Lookup) أم مسار مستقل؟ عند الحسم تُجهَّز مواصفة صغيرة مستقلة.
