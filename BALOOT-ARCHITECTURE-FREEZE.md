# منصة البلوت — Architecture Freeze (قرار معماري مجمّد)

> **الحالة:** Architecture Freeze مطروح لمراجعة المالك — قرار شامل واحد.
> **المرجع الملزم:** `BALOOT-PLATFORM-VISION.md` (معتمد كتابيًا 2026-09-25).
> **القاعدة:** لا يُكتب أي كود بلوت قبل اعتماد هذه الوثيقة كاملة.
> أي انحراف لاحق عن أي بند هنا = تعديل معماري موثّق ومراجع، لا «تفصيلة تنفيذ».
> **D1/C1 كما هي — لا تتوسع ولا تتعدل** إلا في نقاط التكامل الموثقة بالبند 15.

---

## 1) القرارات المعمارية المجمّدة (ملخص تنفيذي)

| # | القرار | الخيار المجمّد | البديل المرفوض ولماذا |
|---|---|---|---|
| A1 | شكل المحرك | وحدة نقية (Pure Engine) بلا DB/IO: `state + action → state' + events` | منطق مبعثر داخل Routes — مستحيل الاختبار والإثبات |
| A2 | مصدر الحقيقة | Action Log تسلسلي append-only + Snapshots مشتقة | حالة JSON وحيدة تُحدَّث — يفقد التتبع والإثبات والـReplay |
| A3 | القناة اللحظية | WebSocket على نفس الخادم (`/ws` القائم، JWT auth) مع topics `baloot:*` | SSE (اتجاه واحد ولا يلبي نية اللاعب)، Polling أساسي (بطيء ومكلف) |
| A4 | التزامن | طابور أوامر متسلسل لكل طاولة (per-table mutex) داخل العملية | أقفال DB خشنة أو منافسة حرة — سباقات حالة |
| A5 | Idempotency | `action_id` (UUID) من العميل + UNIQUE في القاعدة | الاعتماد على منع الواجهة للضغط المزدوج — غير كافٍ |
| A6 | المؤقتات | خادمية فقط + `baloot_deadlines` مُدامجة تُعاد تعبئتها بعد Restart | setTimeout في الذاكرة فقط — ينهار عند إعادة التشغيل |
| A7 | رؤية المشاهد | Projection لكل مشاهد (Player/Spectator serialization مختلف) | بث حالة واحدة للجميع — يكشف كروت اللاعبين |
| A8 | البوابة التشغيلية | تُستدعى **مرة واحدة عند الجلوس فقط**؛ أثناء المباراة: صفر استعلامات تشغيلية | فحص متكرر أثناء اللعب — يكسر العزل وإحساس المجلس |
| A9 | سوالف الطاولة | `community_room` بنوع `table` — إعادة استخدام D1 كاملًا | دردشة موازية جديدة — ازدواج فلترة/إشراف/حظر |
| A10 | القواعد | `ruleset_version` ثابت لكل مباراة + وثيقة Ruleset Freeze تُعتمد قبل التنفيذ | قواعد ضمنية في الكود — اختلاف بلا مرجع |
| A11 | البطولات | لا محرك بطولات — جولة البطولة = طاولات بالمحرك نفسه | محرك موازٍ — ضعف سطح الأخطاء |
| A12 | قاعدة البيانات | SQLite نفسها، جداول `baloot_*` فقط، لا تقرأ أي جدول تشغيلي | قاعدة منفصلة — تعقيد نشر بلا مبرر للحجم |

---

## 2) Game State Machine

### 2.1 دورة حياة الطاولة (Table)

```
OPEN ──(المقعد الرابع يكتمل)──▶ READY_CHECK ──(الأربعة أكدوا)──▶ IN_MATCH
  │                                  │
  │◀──(مغادرة/مهلة تأكيد)────────────┘ (المقعد يعود فارغًا)
  │
  ├──(صاحب الطاولة يغلقها / مهلة خمول)──▶ CLOSED
  └──(الكل غادر)──▶ CLOSED

IN_MATCH ──(نهاية المباراة)──▶ POST_MATCH ──(ريماچ بالأربعة)──▶ READY_CHECK
       │                            └──(لا ريماچ/مغادرة)──▶ OPEN (مقاعد فارغة) أو CLOSED
       └──(إنهاء ودّي/استحالة استكمال)──▶ CLOSED (المباراة: aborted)
```

### 2.2 دورة حياة المباراة (Match — داخل IN_MATCH)

```
DEALING ──▶ BIDDING ──(اكتمال السوق: صن أو حكم + مشتري محدد)──▶ PLAYING
   │            │
   │            └──(الكل بَسّ في الجولة الثانية)──▶ REDEAL (إعادة توزيع — يُسجَّل)
   │
PLAYING = حلقة: TURN_OPEN ──▶ (4 كروت نزلت) ──▶ TRICK_SCORING ──▶ TURN_OPEN ...
   │
   └──(8 لفات اكتملت)──▶ HAND_SCORING ──(بنود الحساب مفصلة ومعلنة)──▶
        ├──(لم يبلغ أحد الهدف)──▶ DEALING (صفقة جديدة)
        └──(بلغ الهدف)──▶ MATCH_END
```

حالات عرضية تقطع أي طور سابق:

- `PAUSED_WAIT_RECONNECT` — انقطاع لاعب أثناء دوره أو بين اللفات: اللعب يتجمد بأناقة، مؤقت العودة يعمل.
- `RESUME_VOTE` — خروج نهائي: خيارا «استبدال» أو «إنهاء ودّي» بموافقة الجالسين (تصويت بسيط: الأغلبية تحسم خلال مهلة).
- `ABORTED` — انتهاء ودّي بلا فائز، تُسجَّل «غير مكتملة».

**قاعدة حاكمة:** كل انتقال طور يتم **حصريًا** داخل المحرك النقي عبر Action مسجَّل. لا يوجد أي مسار في الكود يعدّل الحالة خارج هذا المسار — وهذا ما يجعل «الإثبات والتتبع» ممكنًا بنيويًا.

---

## 3) قواعد البلوت والمحرك الخادمي

### 3.1 متطلب مانع: Ruleset Freeze

هذه الوثيقة تجمّد **المعمارية**، لكن نص القواعد الدقيق (النسخة المعتمدة من البلوت) يحتاج وثيقة مرافقة: **`BALOOT-RULESET.md`** تُكتب وتُعتمد **قبل بدء تنفيذ المحرك**، وتجاوب حرفيًا على قائمة «الأسئلة المفتوحة» في البند 18. المحرك يُبنى ليخدم `ruleset_version` كإعداد بيانات، لا كقيم محروقة في الكود.

### 3.2 نطاق القواعد الذي يغطيه المحرك (v1)

- **التوزيع:** 32 ورقة، 4 لاعبين، 8 لفات لكل صفقة، اتجاه اللعب والتوزيع وفق النسخة المعتمدة.
- **السوق (Bidding):** صن / حكم / بس / مشترى / أشكل — بالترتيب والجولات والشروط التي تثبتها Ruleset Freeze.
- **الدبل:** دبل / رد الدبل / سلسلة التصعيد المعتمدة، وأثرها على حساب الصفقة.
- **المشاريع (الإعلانات):** سرى / خمسين / مية / أربعمية / بلوت — شروط كل مشروع، زمن إعلانه، ترتيب الحسم عند تنافس المشاريع، وقيمه في الصن والحكم.
- **اللعب:** إلزام الرد بالزات، إلزام الحكم عند ثبوته في النسخة المعتمدة، أولوية الورق، حكم اللفة.
- **الحساب:** نقاط الكروت، مشتري الصن والحكم، حساب البنود، الكبّة/القهرة إن اعتُمدت، كسور الحساب، هدف المباراة (152/252 أو ما يعتمد)، حالات التعادل.
- **الحركة الآلية عند التأخر:** كرت قانوني وفق سياسة محايدة موثقة (السياسة نفسها جزء من Ruleset Freeze — لا «ذكاء» ولا عقوبة، مجرد استمرارية عادلة).

### 3.3 بنية المحرك النقي

```
services/baloot/engine/
  ├── cards.js        // نموذج الورق والترتيب والمقارنات (صن/حكم)
  ├── rules.js        // القواعد النقية القابلة للنسخ بـ ruleset_version
  ├── bidding.js      // آلة السوق (شرعية كل عرض ومرحلته)
  ├── declarations.js // المشاريع: كشف/تحقق/حسم التنافس
  ├── scoring.js      // حساب اللفة والصفقة والمباراة — بتفصيل البنود
  ├── state-machine.js// الانتقالات الشرعية للأطوار (§2)
  └── index.js        // applyAction(state, action, ctx) → { state', events[] }
```

- **كل دالة نقية:** لا DB، لا `Date.now()` عشوائي (الزمن يُمرَّر في `ctx`)، لا random داخلي (مولّد العشوائية مُحقون ومُبذَّر — Seeded RNG).
- **النتيجة قابلة لإعادة الإنتاج حرفيًا:** نفس seed + نفس تسلسل الأفعال = نفس المباراة بالضبط. هذا أساس الاختبار والتدقيق وفض النزاع.

---

## 4) Card/Move Model

### 4.1 نموذج الورق

- ترميز ثابت: `{suit}∈{S,H,D,C}` × `{rank}∈{7,8,9,10,J,Q,K,A}` — مثلًا `"H10"`.
- ترتيب القوة يُشتق من `rules.js` حسب طور الصفقة (صن/حكم) — لا يوجد «ترتيب عام» محروق.
- اليد والأرض واللفة كلها قوائم مرتبة من الترميز نفسه — لا كائنات خاصة.

### 4.2 نموذج الحركة (Action)

كل إدخال للمحرك Action موحّد:

```json
{
  "action_id": "uuid-from-client",
  "match_id": "…",
  "actor_user_id": "…",
  "seq_expected": 137,
  "type": "PLAY_CARD | BID | PASS | DECLARE | DOUBLE | RESPOND_DOUBLE | CONFIRM_READY | SIT | LEAVE | REPLACE_ACCEPT | ABORT_VOTE | REMATCH_ACCEPT | AUTO_PLAY | TIMEOUT",
  "payload": { "card": "H10" }
}
```

- أنواع `AUTO_PLAY` و`TIMEOUT` **يولّدها الخادم** (المؤقتات) — تمر عبر نفس القناة والسجل تمامًا كأفعال اللاعبين، فيظهر في السجل «لعب آليًا بسبب التأخر» بلا إخفاء.
- خط أنابيب المعالجة ثابت الترتيب:
  `مصادقة ← ربط المقعد ← فحص الطور ← فحص الدور ← توليد الحركات الشرعية ← مطابقة الحركة ← تطبيق ← أحداث`
- **العميل لا يستطيع إرسال حركة غير قانونية بنيويًا** (واجهته تعرض الشرعي فقط عبر endpoint الحركات الممكنة)، والخادم يتحقق مرة أخرى على أي حال. المحاولة غير النظامية تُرد بود: `{ ok:false, code:"MUST_FOLLOW_SUIT", message:"لازم ترد بالزات" }`.

---

## 5) Server Authority

- **الخادم هو الحكم الوحيد والمطلق:** التوزيع، الشرعية، الدور، الحساب، الفائز — كلها خادمية. العميل: يرسم + يرسل نيّة + يعرض نتيجة.
- **التوزيع يحدث خادميًا** بمولّد عشوائي آمن؛ ترتيب الطاقم كاملًا يُخزَّن في الحالة الخادمية و**لا يغادر الخادم أبدًا** إلا كروت صاحب اليد لصاحبها فقط.
- **الزمن خادمي:** كل المهل والطوابع من ساعة الخادم؛ ساعة العميل لا تُصدَّق في شيء.
- **كل نتيجة قابلة للتتبع:** أي رقم يظهر للمستخدم (نقاط صفقة، مشروع، فوز) يمكن تفكيكه إلى سلسلة الأفعال المسجلة التي أنتجته — `baloot_actions` هو الدليل.
- **إعادة التشغيل لا تفقد شيئًا:** الحالة تُعاد بناؤها من آخر Snapshot + إعادة تشغيل الأفعال اللاحقة (§7, §9).

---

## 6) Concurrency + Idempotency

- **طابور لكل طاولة:** أوامر كل Match تدخل طابورًا متسلسلًا في الذاكرة (mutex لكل `match_id`). لا يوجد أي تنفيذ متوازٍ لأمرين على نفس المباراة — إطلاقًا. الضغط المزدوج والطلبان المتزامنان يُحسمان هنا: أول أمر صحيح يُقبل، والثاني يصل فيجد الحالة تقدّمت فيُرفض بود أو يُجاب كمكرر.
- **Idempotency حقيقي:** `action_id` من العميل محفوظ بقيد UNIQUE في `baloot_actions`. إعادة الإرسال (شبكة/ضغطة مزدوجة/Retry) ترجع **نفس النتيجة الأصلية** ولا تُطبَّق مرتين.
- **Optimistic version:** كل حالة تحمل `state_version` متزايد؛ العميل يعلن `seq_expected`، وأي فجوة تعني «أنت متأخر — خذ Snapshot».
- **DB:** تحديثات المباراة داخل معاملة واحدة لكل أمر (action + snapshot + أحداث مُدامجة)، مع `busy_timeout` وWAL كما هو معمول في المشروع.
- **نطاق القرار:** عملية Node واحدة (الواقع الحالي للنشر). لو تعددت العمليات مستقبلًا يُنتقل الطابور إلى قفل موزع — موثق كحدّ معماري صريح، لا كلامًا عامًا.

---

## 7) Reconnect / Disconnect / Timeout

### 7.1 الانقطاع والعودة

- الاتصال اللحظي ينقطع ← الطاولة ترى «انقطع مؤقتًا»، ويبدأ مؤقت عودة مُعلن.
- العودة: المصادقة على `/ws` ← الاشتراك في topic المباراة ← طلب Snapshot منذ آخر `seq` يملكه العميل:
  - يملك كل الأحداث: يُرسل له الفرق فقط.
  - فاته الكثير/انتهت صلاحية الجلسة: Snapshot كامل (projection الخاص بمقعده) + استئناف.
- **المقعد والكروت والحالة كما تركها** — العودة استئناف لا بدء من جديد، لأن مصدر الحقيقة خادمي أصلًا.
- انتهاء مهلة العودة: اللاعب يُعلَّم «خارج»، وتُفتح `RESUME_VOTE` (استبدال/إنهاء ودّي). المستبدِل يجلس على المقعد **بكروت من سبقه** — المحرك يدعم تبديل هوية المقعد كفعل مسجَّل.

### 7.2 المؤقتات (كلها خادمية ومُدامجة)

| المؤقت | القيمة الافتراضية* | عند الانتهاء |
|---|---|---|
| تأكيد الجاهزية | 60 ث | المقعد يعود فارغًا بلا حرج |
| مهلة اللفة | 30 ث | `AUTO_PLAY` — حركة قانونية محايدة معلنة |
| مهلة العودة من انقطاع | 120 ث | خروج نهائي ← `RESUME_VOTE` |
| مهلة تصويت الاستبدال/الإنهاء | 60 ث | إنهاء ودّي |
| خمول الطاولة المفتوحة | 30 د | إغلاق هادئ |

\* كل القيم جزء من إعداد Ruleset/Platform Freeze وتُعرض للمستخدم قبل أن تفاجئه.

- كل مؤقت = سجل في `baloot_deadlines` (kind, fire_at, payload, consumed).
- **إقلاع الخادم:** يقرأ المهل غير المستهلكة ويعيد تسليحها؛ المهل الفائتة أثناء التوقف تُستهلك فورًا بترتيبها — كأن التوقف لم يحدث.

---

## 8) Realtime Architecture

- **القرار:** WebSocket — على نفس خادم HTTP القائم ومساره `/ws` مع نفس مصادقة JWT الموجودة (إعادة استخدام، لا بنية موازية). Render يدعم WS على الخادم نفسه كما هو الوضع الحالي.
- **Topics:** `baloot:council:{id}` (قائمة الطاولات)، `baloot:table:{id}` (مقاعد/أحداث اللوبي)، `baloot:match:{id}` (أحداث المباراة).
- **كل رسالة تحمل `match_id + seq`** — العميل يكشف الفجوات بنفسه ويطلب Resync (§7.1).
- **الاشتراك ليس تفويضًا:** الانضمام لـ `baloot:match:{id}` يتطلب تحقق خادمي: جالس على الطاولة أو مشاهد مسموح. المشاهد يستلم **Spectator Projection** فقط (§A7).
- **Payloads صغيرة:** أحداث دلتا («نزلت H10 من المقعد 2») لا حالات كاملة؛ Snapshot الكامل عند الطلب فقط.
- **Fallback قراءة فقط:** عميل لا يستطيع WS (شبكة مقيدة) يعمل بوضع منخفض: Snapshot + Polling بطيء — يشاهد ويلعب بزمن أبطأ، بلا كسر للمنطق. لا SSE.
- **حجر الأساس:** القناة مجرد **نقل**. لو انقطعت القناة كليًا، المباراة تستمر صحيحة خادميًا (المؤقتات والأفعال المسجلة)، ويعود العميل فيستأنف — اللحظية رفاهية إحساس، وليست جزءًا من صحة اللعبة.

---

## 9) Storage Model

جداول جديدة فقط، كلها بادئة `baloot_*`، في قاعدة SQLite نفسها. **لا جدول منها يقرأ أو يشير إلى أي جدول تشغيلي.** المراجع الوحيدة للخارج: `user_id` (هوية Community) و`council_id` و`room_id`.

```
baloot_tables          (id, council_id, room_id, status, ruleset_version,
                        target_score, created_by, created_at, closed_at)
baloot_table_seats     (table_id, seat 0..3, user_id, seat_status, ready_at,
                        replaced_by, …)   // الفريق = parity المقعد (0,2)/(1,3)
baloot_matches         (id, table_id, ruleset_version, phase, status,
                        state_version, snapshot_json, snapshot_seq,
                        started_at, ended_at, outcome)
baloot_match_players   (match_id, user_id, seat, team, final_score, outcome,
                        joined_via)       // original | replacement
baloot_hands           (match_id, hand_no, contract, buyer_seat, double_state,
                        score_detail_json)   // بنود الحساب كاملة لكل صفقة
baloot_actions         (match_id, seq, actor_user_id, type, payload_json,
                        idempotency_key UNIQUE, created_at)  // append-only
baloot_deadlines       (id, match_id, kind, fire_at, payload_json, consumed)
baloot_ratings         (user_id PK, matches, wins, losses, honor_points,
                        updated_at)        // read model — يُشتق ولا يُكتب يدويًا
```

**مبادئ:**
- `baloot_actions` هو **مصدر الحقيقة**؛ `snapshot_json` مجرد Checkpoint للأداء (كل صفقة أو كل N فعلًا) ويمكن حذفه وإعادة بنائه من الصفر.
- `baloot_hands.score_detail_json` يخزن **تفصيل البنود** لا الرقم النهائي فقط — «لا رقم يهبط بلا تفسير».
- `baloot_ratings` مشتق بالكامل من النتائج — إعادة بنائه في أي وقت من `baloot_match_players`.

---

## 10) Match/Hand/Trick/Score/Result History

- **اللفة (Trick)** ليست جدولًا مستقلًا: هي 4 أفعال `PLAY_CARD` متتالية في `baloot_actions` + نتيجتها مشتقة. هذا يمنع ازدواج الحقيقة.
- **الصفقة (Hand):** ملخص + تفصيل بنود الحساب في `baloot_hands` لعرض «من أخذ كم ولماذا» وشاشة نهاية الصفقة.
- **المباراة:** ملخص في `baloot_matches` + اللاعبون والنتائج في `baloot_match_players`.
- **سجل المجلس:** استعلامات قراءة فقط فوق هذه الجداول — «دفتر المجلس»: من لعب، متى، النتيجة، وإمكان استعراض أي مباراة قديمة **كإعادة عرض (Replay)** من سجل الأفعال.
- **سجل اللاعب:** مباراته ونتائجه ومشاريعه — يظهر في سياق Community فقط، ولا يظهر في أي سياق تشغيلي إطلاقًا.

---

## 11) Anti-Tamper

1. **الطاقم لا يغادر الخادم:** التوزيع خادمي بعشوائية آمنة؛ كل عميل يستلم يده فقط عبر Projection خاص به.
2. **المشاهد منهيّ الغش بنيويًا:** Spectator Projection لا تحتوي كروت الأيدي — لا «وضع مطور» ولا كشف.
3. **الخادم يعيد التحقق من كل شيء:** حتى لو العميل عُدّل، الحركة غير الشرعية تُرفض — الواجهة التي تخفي الممنوع راحة، وليست خط الدفاع.
4. **ساعة الخادم وحدها:** لا يُقبل أي زمن من العميل.
5. **Idempotency + الطابور:** لا ازدواج ولا سباق يصنع حالة مستحيلة.
6. **سجل أفعال append-only:** أي محاولة تلاعب (وصلت الخادم) تُسجَّل بفاعلها — التدقيق ممكن على أي مباراة.
7. **Rate limiting** على أفعال اللعب والدعوات والرسائل (نفس نمط حماية Community القائم).
8. **لا «وضع مشرف يغيّر النتيجة»:** صلاحيات الإشراف (`community.moderate/admin`) تستطيع إنهاء طاولة أو تقييد مستخدمًا، و**لا تستطيع** تعديل كروت أو نقاط أو نتيجة — وكل فعل إشرافي يُسجَّل في `community_audit_log` كما هو معمول.

---

## 12) Testing Strategy

| الطبقة | ماذا تختبر | كيف |
|---|---|---|
| **محرك نقي** | كل قاعدة في Ruleset Freeze: سوق، مشاريع، حساب، رد بالزات، دبل، حالات حدّية | Unit tests بـ seeded RNG — حتمية 100% وقابلة للتكرار |
| **إعادة الإنتاج** | أي مباراة = seed + أفعالها | Property test: Replay سجل الأفعال يعيد Snapshot الحالي حرفيًا لكل مباراة اختبارية |
| **آلة الحالات** | كل انتقال شرعي وغير شرعي في §2 | مصفوفة انتقالات مغطاة بالكامل |
| **التزامن** | ضغط مزدوج، طلبان متزامنان، إعادة إرسال | اختبارات تزاحم مقصودة على الطابور وidempotency |
| **الانقطاع** | قطع/عودة في كل طور، انتهاء مهلة العودة، استبدال لاعب | Integration عبر WS حقيقي على خادم اختبار |
| **المؤقتات** | AUTO_PLAY، إعادة التسليح بعد restart | خادم يُقتل ويُقلع أثناء مباراة ← تستأنف صحيحة |
| **الأمان** | محاولة كشف يد غيرك، حركة غير شرعية من عميل معدّل، تجاوز صلاحية المشاهد | اختبارات tampering صريحة |
| **الانحدار** | Community كاملة | `community-full-test` 207/207 + `community-c1-test` 57/57 + التكليف 16/16 + TL1 27/27 يجب أن تبقى خضراء — بلا استثناء |
| **جناح بلوت** | سكربت `scripts/baloot-full-test.js` بنفس نمط جناح Community | يُشغَّل قبل أي عرض على المالك |
| **حمل دخاني** | عشرات الطاولات المتزامنة | قياس زمن apply والبث (§13) |

**بوابة القبول:** لا يُعرض أي شيء على المالك إلا والجناحان (بلوت + انحدار Community) أخضران كاملَين.

---

## 13) Performance

- **الحجم المتوقع صغير:** عشرات الطاولات المتزامنة حدًّا أقصى واقعيًا — عملية واحدة + SQLite (WAL) كافية بهامش كبير، ولا يُبرَّر أي تعقيد توزيع.
- **حالة المباريات النشطة في الذاكرة** (cache) مبنية من Snapshot+Log عند أول لمسة؛ الكتابة للقاعدة مرة لكل أمر (معاملة واحدة) لا لكل حدث بث.
- **أهداف قياس:** زمن تطبيق الحركة خادميًا p95 < 150ms؛ Snapshot projection < 50KB؛ رسالة البث < 2KB.
- **الرسائل التاريخية والنتائج** تُقدَّم بصفحات (pagination) كما في D1 — لا استعلامات مفتوحة.
- **تنظيف دوري:** مهل مستهلكة، طاولات مغلقة قديمة — أرشفة/حذف وفق سياسة بسيطة توثق في التنفيذ.

---

## 14) Security

- **المصادقة:** JWT نفسه على REST وعلى WS handshake (البنية القائمة) — لا جلسة موازية.
- **التفويض مقعدي:** كل فعل يتحقق «هل هذا المستخدم على هذا المقعد في هذه المباراة؟». المشاهد read-only بـ Projection مخصصة.
- **الجلوس يتطلب** `community.join_activity` (+ البوابة §15)؛ فتح طاولة يتطلب صلاحية إنشاء المعتمدة؛ الإشراف على الطاولات يتبع `community.moderate/admin` كما هي — **لا صلاحيات تشغيلية تُمنح أو تُقرأ هنا**.
- **المحتوى النصي** (أسماء الطاولات، سوالف الطاولة) يمر بنفس فلتر المحتوى وخدمة الإشراف القائمين.
- **PII:** لا يُعرض سوى الاسم العرضي لـ Community — لا أرقام وظيفية ولا أي بيانات تشغيلية على الطاولة.
- **المدخلات:** تحقق صارم من كل payload (نوع/طول/نطاق) — نفس انضباط routes Community الحالية.

---

## 15) تكامل الطاولة مع Community — بلا كسر للعزل التشغيلي

**ما يُعاد استخدامه كما هو (لا نسخ ولا تعديل بنيوي):**

| القدرة | الخدمة القائمة | كيف تستخدمها الطاولة |
|---|---|---|
| الهوية والاسم العرضي | `community-identity-service` | أسماء المقاعد والمشاهدين |
| البوابة التشغيلية | `community-operational-gate-service` | **استدعاء واحد عند «اجلس» فقط** — allowParticipation=false ⇒ رسالة هادئة ولا جلوس. **بعد الجلوس: صفر استعلامات تشغيلية حتى نهاية المباراة** (قرار الرؤية §0) |
| الحظر المتبادل | `community-moderation-service` (`isBlockedEitherWay`) | من حظر/حُظر جالسًا لا يجلس معه ولا يُدعى لطاولته |
| سوالف الطاولة | `community-room-service` + `community-chat-service` (D1) | غرفة بنوع `table` مرتبطة بالطاولة — فلترة/إشراف/حظر/بلاغات كما هي |
| الشارات | `community-badge-service` | منح تلقائي عند أحداث النتائج (أول فوز، 10 انتصارات…) |
| التدقيق | `community_audit_log` | الأفعال الإشرافية على الطاولات |
| مجلس البلوت | `community_councils` | الطاولات تعيش داخل مجلس البلوت القائم |

**خطوط العزل الحمراء (ثابتة):**
- لا قراءة من `shifts / team_assignments / operational_* / team_live_locations` من أي ملف `baloot_*` — الاستعلام التشغيلي الوحيد يحدث داخل خدمة البوابة القائمة نفسها، في لحظة الجلوس فقط.
- لا كتابة في أي جدول تشغيلي. لا أحداث تشغيلية تُولَّد من اللعب.
- لا تعارض مع `presence`: إعلان الحالة الاجتماعية يبقى خارج البوابة كما قرر المالك؛ اللعب لا يغيّر الحضور ولا يقرأه.
- اختبار انحدار إلزامي: جناح Community كاملًا أخضر بعد كل مرحلة بلوت.

---

## 16) Challenges / Rankings / Tournaments — كيف تُبنى لاحقًا فوق المحرك

مصممة الآن كي تُبنى **بلا أي تعديل في المحرك**:

- **Rankings:** read model فوق `baloot_match_players` — نقاط شرفية بسيطة (فوز/خسارة/عدد مباريات) تُحدَّث عند `MATCH_END`. ترتيب فخر واضح أنه للمتعة، لا تقييم، ولا يخرج من Community.
- **Challenges:** سجل `baloot_challenges` (ثنائي × ثنائي + حالة) ينشئ طاولة عادية بمحرك اللعب نفسه ويربط نتيجتها بالتحدي. التحدي «غلاف» حول طاولة — لا وضع لعب جديد.
- **Tournaments (أبعد مرحلة):** امتداد لـ `community_competitions` القائمة: سجل البطولة + جدول مواجهات؛ **كل مواجهة = مباراة بالمحرك نفسه** على طاولة عادية موسومة بـ `competition_id`. لا محرك بطولات، لا قواعد خاصة، لا حالات جديدة (A11).
- **الشارات:** أحداث نتائج المباريات تغذي `community-badge-service` بقواعد منح جديدة — بلا تغيير في خدمة الشارات نفسها.

**قاعدة:** أي من هذه يبدأ بقرار مستقل بعد نضج اللعب الفردي — لا شيء منها في نطاق التنفيذ الأول.

---

## 17) خريطة الملفات والوحدات (خطة — لا كود الآن)

```
services/baloot/engine/…              (§3.3 — نقي)
services/baloot/baloot-table-service.js     // طاولات/مقاعد/دعوات/جاهزية
services/baloot/baloot-match-service.js     // تزامن المحرك + الطابور + المؤقتات + البث
services/baloot/baloot-projection.js        // Serializers: player/spectator/lobby
services/baloot/baloot-timer-service.js     // المهل المُدامجة + إعادة التسليح
server.js   // + routes /api/baloot/* و + topics baloot:* على /ws القائم
public/community.html  // + قسم الطاولات في مجلس البلوت + شاشة الطاولة
scripts/baloot-full-test.js                 // الجناح الإلزامي
db.js       // + جداول baloot_* فقط (§9)
BALOOT-RULESET.md  // يُعتمد قبل كتابة أي سطر محرك (§3.1)
```

---

## 18) أسئلة مفتوحة تحتاج قرار المالك قبل التنفيذ

هذه **الأسئلة الوحيدة** المتبقية — إجاباتها تثبّت Ruleset Freeze:

1. **نسخة القواعد المعتمدة للقطاع:** تفاصيل السوق (مشترى/أشكل وشروطهما)، سلسلة الدبل ورده، قيم المشاريع في الصن والحكم، هل تُحسب الكبّة/قهر، وهدف المباراة (152 أم 252) — أحتاج تأكيدك النهائي لنسخة واحدة مكتوبة.
2. **قيم المهل** في §7.2 — مقبولة كقيم افتراضية أم تريد تعديلها؟
3. **من يفتح طاولة؟** أي حامل `community.create_activity` أم إتاحة أوسع/أضيق؟
4. **سقف المشاهدين** لكل طاولة (اقتراح: 20) ومشاهدة من خارج أعضاء مجلس البلوت: مسموحة أم لا؟
5. **الريماچ:** تلقائي بموافقة الأربعة أم يدوي بإعادة فتح طاولة؟
6. **الترتيب الشرفي:** ظاهر لكل أعضاء المجلس أم لمن لعب فقط؟

---

## 19) Negative Scope (ملزم — موروث من الرؤية ومؤكد)

- لا مال/رهان/جوائز/كوينز/متجر — أبدًا.
- لا قراءة أو استخدام لأي بيانات تشغيلية داخل اللعب.
- لا نتائج بلوت في أي سياق خارج Community، ولا تقييم أداء وظيفي منها.
- لا إشعارات ملاحقة خارج المنصة.
- لا نسخ هوية/تصميم/أصول أي تطبيق قائم.
- لا محرك بطولات موازٍ.
- لا WebSocket جديد خارج بنية `/ws` القائمة، ولا SSE، ولا Polling أساسي.
- لا iOS في النطاق الأول — Web فقط حتى يثبت.
- لا تعديل على D1/C1 إلا نقاط التكامل الموثقة في §15.
