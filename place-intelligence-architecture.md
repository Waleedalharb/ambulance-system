# دراسة معمارية — Place Intelligence Engine
## منصة الجنوب | 2026-08-29 | تشخيص وتصميم فقط — لا تنفيذ

> **نطاق هذه الوثيقة:** دراسة معمارية لتحويل مفهوم Hospital Monitor إلى محرك عام للتعرف على نوع الموقع/الجهة المرتبطة بالبلاغ. لا يوجد أي تعديل على الكود، ولا Commit/Push/Deploy قبل اعتماد المالك للتصميم.

---

## أولًا: ما الذي نملكه الآن؟ (جرد مثبت من الكود والبيانات)

### 1. بيانات مكانية على البلاغ — `incident_registry`

| الحقل | الحالة | ملاحظة |
|---|---|---|
| `lat` / `lng` | ✅ موجود | إحداثيات البلاغ من CAD |
| `address` | ✅ موجود | غالبًا Plus Code + حي: «9W95+8W ام الشعال، الرياض السعودية» |
| `district` / `street` / `city` | ✅ موجود | مشتقة عبر `_parseLocationParts`/`_parseDistrict` في report-service |
| `description` | ✅ موجود | وصف حر من المشغل |
| `region` | ✅ موجود | قطاع (South Riyadh city Sector) |
| `caller_number` | ✅ موجود | مطبَّع منذ Duplicate 2.0 |

**الإثراء تزايدي:** `updateIncidentLocation` يملأ الناقص فقط — لا يستبدل الموجود.

### 2. بيانات CAD الخام (event-dispatched/detail)

فُحصت عينات حقيقية (بلاغان 1306462 و1306598). الحقول المتاحة:

- `address`, `zoneName`, `proQACode`, `chiefComplaint`, `callerName`, `phoneNumber/Secondary`
- `notes[]` — ملاحظات نصية حرة (رقم جوال، موقع البلاغ كنص، ProQA، ملاحظات المشغل مثل «رفض النقل إلى أقرب منشأة طبية»)
- `lastJourneys[].hospitalName` — مصدر المستشفى المعتمد في 1B

**النتيجة الحاسمة: لا يوجد في CAD حقل «اسم الموقع» صريح.** أقرب مصادر لاسم المكان: نص العنوان + الملاحظات الحرة + الوصف. وهذا يعني أن التعرف على نوع الموقع لا يمكن أن يعتمد على حقل CAD جاهز — بل على **قاعدة مواقع + إحداثيات + أدلة نصية**.

### 3. قاعدة مواقع موجودة فعلًا

| المصدر | المحتوى | الملاحظة |
|---|---|---|
| `data/map-locations.json` | **12 مستشفى** `{name, lat, lng, type: عام/متخصص/مجمع/طوارئ}` | نواة بذرة — لكنها مستشفيات حصرًا |
| `operationalCenters` (جدول) | مراكز تشغيلية بإحداثيات | مواقع داخلية، ليست جهات بلاغات |
| `incident_registry` نفسها | آلاف البلاغات المؤرشفة بإحداثيات وعناوين | **أثمن مصدر للتعلم التدريجي** |

### 4. رياضيات جاهزة

- `haversineMeters` في `services/duplicate-detection.js` — **مصدَّرة وجاهزة** (module.exports). حساب المسافة بين البلاغ والموقع المعروف لا يُعاد كتابته.

### 5. نموذج Hospital Monitor القابل للتعميم

`services/hospital-monitor-service.js` يقدم القالب المعماري المثبت:

- مفاتيح هوية ثابتة: `eventId:runUnitId` — تنجو من تغيّر رمز الفرقة
- Mutable State: Current State + History (append-only)
- `episodeClass`: active / completed / monitoring-lost
- تخزين JSON معزول (`data/hospital-monitor.json`) — fs/path فقط، لا SQL
- فصل «العرض الحالي» عن «سجل الأحداث»
- حقن (injection) في الأرشيف: `this.hospitalMonitor` + `this.hospitalDwellProvider` في `_getHospital()` بمحرك الأرشيف (السطر 628-650)

### 6. طبقة العرض الموحدة

`public/js/map-adapter.js` — واجهة موحدة (Mapbox/Leaflet قابلة للاستبدال): marker / divIcon / popup. أي شارة نوع موقع تدخل عبر هذه الطبقة فقط، وليس عبر تعديل مكتبة الخرائط.

---

## ثانيًا: ما الذي ينقصنا؟

| الفجوة | الأثر | الحل المقترح |
|---|---|---|
| لا يوجد حقل `placeName` صريح في CAD | لا يمكن «قراءة» نوع الموقع — يجب **استنتاجه** | قاعدة مواقع + تطابق إحداثي + أدلة نصية |
| قاعدة المواقع = 12 مستشفى فقط | لا مدارس/شرطة/مساجد/سجون معروفة | **تعلم تدريجي** من البلاغات المؤرشفة + إدخال يدوي مراجَع |
| لا يوجد سجل مواقع موحّد | كل نوع سيحتاج نظامًا منفصلًا لو لم نصمم صح | جدول/سجل `places` واحد بـ `placeType` |
| لا يوجد محرك ثقة | خطر التخمين | نظام Confirmed / Likely / Unknown بقواعد صارمة |
| الملاحظات النصية غير مستغلة | فيها أدلة («أقرب منشأة طبية»، «مدرسة...») | دليل نصي مؤيد — ليس حاسمًا وحده |

---

## ثالثًا: أفضل بنية — التصميم المقترح

### 1. سجل المواقع الموحّد `places` (المصدر الوحيد للحقيقة)

سجل واحد لكل الأنواع — **لا نظام لكل نوع**:

```text
placeId        معرف ثابت (internal)
placeType      hospital | school | university | health_center |
               police | prison | government | mosque | mall |
               sports | public_facility | construction | other
name           الاسم الرسمي
nameVariants[] أسماء بديلة تظهر في العناوين/الملاحظات
lat, lng       نقطة مرجعية
radiusM        نصف قطر التطابق (افتراضي 150م، أوسع للمجمعات)
district       الحي (دليل مؤيد)
source         seed | manual | learned   (مصدر إدخال الموقع)
status         active | candidate | retired
evidence[]     سجل: من أين جاء هذا الموقع ومتى
createdAt / updatedAt / createdBy
```

**البذرة الأولى:**
1. الـ12 مستشفى من `map-locations.json` (source: seed) — **تبقى متوافقة**، لا مصدر حقيقة ثاني للمستشفيات (الإجابة على سؤال 9).
2. المراكز التشغيلية من `operationalCenters` (للاستبعاد: بلاغ عند مركز إسعاف ليس «جهة خارجية»).

**التوسع التدريجي (بدل بناء قاعدة ضخمة يدويًا — جواب «الأهم» في التوجيه):**

> بلاغات تتكرر عند نفس الإحداثيات (ضمن ~150م) بعدد ≥ عتبة خلال نافذة زمنية، ونصوصها تتفق على اسم/نوع → يُنشأ **موقع مرشح** (`status: candidate`) يظهر للمراجعة اليدوية. **لا تصنيف تلقائي نهائي أبدًا** — الموقع لا يصبح `active` إلا باعتماد بشري.

هذا يستغل `incident_registry` التاريخية: لدينا آلاف البلاغات بإحداثيات وعناوين، والتجميع الجغرافي (clustering) عليها يكشف المواقع الساخنة الحقيقية دون إدخال يدوي مسبق.

### 2. محرك التعرف — `services/place-intelligence-service.js`

خدمة معزولة جديدة (نفس نمط hospital-monitor: fs/path، تخزين JSON معزول، لا تعديل مصادر أصلية).

**المدخلات (لكل بلاغ):** `lat/lng + address + district + description + notes[] + proQACode/chiefComplaint`

**قواعد التعرف مرتبة (من الأقوى للأضعف):**

| # | القاعدة | الناتج | الثقة |
|---|---|---|---|
| R1 | تطابق إحداثي: البلاغ داخل `radiusM` لموقع `active` | `placeType + placeName` من السجل | **Confirmed** |
| R2 | اسم معروف مطابق (`nameVariants` بحدود كلمة كاملة في address/description/notes) **+ توافق مكاني داخل `radiusM`** للموقع المعتمد | نفس الشيء | **Confirmed** |
| R3 | اسم معروف مطابق لكن **خارج `radiusM`** أو بدون إحداثيات | نفس الشيء | **Likely فقط — ممنوع Confirmed** |
| R4 | أدلة نصية على النوع فقط («مدرسة»، «مسجد»، «شرطة» في الوصف/الملاحظات) بدون مطابقة موقع معروف | `placeType` فقط، `placeName: null` | **Likely** (كحد أقصى) |
| R5 | لا شيء مما سبق | `placeType: null` | **Unknown** |

**قواعد صارمة (Precision First):**
- دليل نصي منفرد **لا ينتج Confirmed أبدًا** — R4 سقفه Likely.
- تشابه نص جزئي (substring) وحده لا يكفي: المطابقة النصية على `nameVariants` تكون بحدود كلمة كاملة (word boundary) بعد تطبيع عربي (همزات/تاء مربوطة/أرقام عربية).
- الزمن وتشابه النص **لا ينتجان تصنيفًا حاسمًا** (جواب سؤال 6).
- **Unknown نتيجة محترمة ومشروعة** — تُعرض كما هي بلا تخمين.

### 3. درجات الثقة (جواب سؤال 7)

| الدرجة | المعنى | السلوك |
|---|---|---|
| **Confirmed** | R1 أو R2 — دليل إحداثي على موقع معروف، أو اسم+موقع | يظهر كاملًا: شارة على الخريطة، بطاقة موقع، يدخل التحليلات والأرشيف |
| **Likely** | R3 أو R4 — دليل نصي قوي بلا إحداثي كافٍ | يظهر بوسم بصري مميّز («محتمل»)، يدخل التحليلات منفصلًا، **لا يُنشئ تنبيه تكرار** |
| **Unknown** | لا أدلة كافية | لا شارة نوع، لا شيء — البلاغ يبقى «بلاغ» فقط |

**الناتج لكل بلاغ (يُخزن نتيجة المحرك، لا مصدر حقيقة جديد):**

```text
placeResolution = {
  placeType: 'school' | null,
  placeName: 'مدرسة XXX' | null,
  placeId:  'plc_...' | null,
  confidence: 'confirmed' | 'likely' | 'unknown',
  evidence: [ {rule: 'R1', detail: 'distance 87m <= radius 150m', placeId} ],
  resolvedAt, engineVersion
}
```

`evidence` إلزامي دائمًا — **كل تصنيف قابل للتتبع إلى قاعدته** (نفس قاعدة المنصة: لا رقم بلا مصدر).

### 4. العلاقة مع Hospital Monitor (جواب سؤال 1 و10)

**الآن:** Hospital Monitor **يبقى كما هو حرفيًا** — مصدر المستشفيات الوحيد يظل مخزن 1B (`hospitalName` من incident-list). محرك المواقع **لا يلمس دورة المستشفيات** ولا ينشئ لها مصدرًا ثانيًا. عندما يصنّف محرك المواقع بلاغًا بأنه في مستشفى، فهذا تصنيف «موقع البلاغ» — أما رحلة النقل والتسليم وزمن البقاء فمصدرها الحصري يبقى 1B.

**مستقبلًا (خارطة طريق، ليس الآن):** hospital يصبح `placeType` واحدًا ضمن قالب عام:
- القالب المشترك المثبت: هوية ثابتة + Mutable State + episodeClass + حقن في الأرشيف.
- كل `placeType` يضيف مقاييسه الخاصة (المستشفى: زمن بقاء/تسليم · المدرسة: تكرار بلاغي · السجن: حساسية).
- الانتقال يتم نوعًا نوعًا، بلا كسر — ولا يُبدأ به قبل استقرار محرك المواقع.

---

## رابعًا: كيف يدخل الناتج إلى أسطح المنصة (جواب سؤال 8)

| السطح | المدخل | النمط المعتمد الموجود |
|---|---|---|
| **الخريطة الحية** | شارة نوع على علامة البلاغ (🏫🚔🏥) + بطاقة موقع عند الضغط | عبر `map-adapter.js` فقط — divIcon/popup، لا تعديل مكتبة الخرائط |
| **التنبيهات** | قاعدة جديدة بنمط `alert-rules.js`: «N بلاغات Confirmed لنفس `placeId` خلال نافذة» → تنبيه «نشاط بلاغي مرتفع في منشأة» | Likely **لا يُنشئ تنبيهًا** — Confirmed فقط |
| **البلاغات** | عمود/وسم نوع الموقع في عرض البلاغ | قراءة من ناتج المحرك |
| **الأرشيف** | قسم «الجهات والمواقع» في لقطة المناوبة: إحصائية تفصيلية قابلة للتدقيق بالتدرج: **نوع الجهة → الجهة → عدد البلاغات المؤكدة → عدد المحتملة → البلاغات → تفاصيل البلاغ → الأدلة التي أدت للتصنيف** (Likely لا يدخل في الأرقام الرسمية مع Confirmed) | نفس نمط `_getHospital()` — حقن مزوّد قراءة في shift-archive-engine |
| **التحليلات** | «أكثر المواقع استقبالًا للبلاغات»، توزيع الأنواع، زمن وصول حسب المنشأة | قراءة مشتقة من ناتج المحرك — لا تخزين مشتق جديد خارج مخزن المحرك |

**مبدأ حاكم:** كل الأسطح تقرأ **ناتج المحرك فقط** — لا أحد يعيد الاستنتاج بنفسه.

---

## خامسًا: كيف نبدأ بأقل تغيير؟ (خطة مراحل — للاعتماد، لا للتنفيذ)

| المرحلة | المحتوى | لا تتضمن |
|---|---|---|
| **PI-0** (هذه الوثيقة) | اعتماد التصميم | — |
| **PI-1** | سجل `places` + بذرة 12 مستشفى + المراكز + خدمة `place-intelligence-service` بالقواعد R1-R5 + حفظ `placeResolution` مع البلاغ — مصمم منذ البداية ليسمح بتدرج تدقيق الأرشيف (نوع ← جهة ← مؤكد/محتمل ← بلاغات ← تفاصيل ← أدلة) دون بناء الواجهة الآن | لا واجهة، لا خريطة، لا تنبيهات |
| **PI-2** | اختبار الدقة على باقة تقييم تاريخية (أدناه) + واجهة مراجعة المواقع المرشحة | لا عرض تشغيلي |
| **PI-3** | شارات الخريطة + بطاقة الموقع (عرض فقط) | لا تنبيهات |
| **PI-4** | قسم الأرشيف + التحليلات | |
| **PI-5** | تنبيه «نشاط بلاغي مرتفع» | |
| **PI-6** (لاحقًا) | تعميم Hospital Monitor تدريجيًا | |

كل مرحلة: اختبارات → تقرير → اعتماد → ثم نشر. لا دمج مراحل.

---

## سادسًا: كيف نختبر دقة التصنيف؟ (جواب سؤال 5)

1. **باقة تقييم ذهبية:** عينة من البلاغات التاريخية في `incident_registry` (مثلًا 200 بلاغ بإحداثيات) تُوسم يدويًا بالنوع الحقيقي (مراجعة بشرية) — تصبح مرجع الحقيقة.
2. تشغيل المحرك على العينة وقياس:
   - **Precision** لكل درجة: من بين ما صنّفه Confirmed، كم كان صحيحًا؟ (الهدف: ≥ 98% لـ Confirmed)
   - **Recall**: كم موقعًا حقيقيًا التقطه؟ (مقبول أن يكون منخفضًا في البداية — Precision First تعني أننا نفضّل Unknown على الخطأ)
   - **معدل Unknown**: مؤشر صحة — ارتفاعه المبكر طبيعي وينخفض مع توسع قاعدة المواقع.
3. **اختبارات وحدة** بنمط الاختبارات القائمة: حالات مصطنعة لكل قاعدة (R1-R5)، حدود نصف القطر، تطبيع النص العربي، غياب الإحداثيات، تضارب الأدلة.
4. **قاعدة عدم الانحدار:** `report_times` والاحتساب والخريطة ودورة المستشفيات — صفر تأثير (المحرك قراءة+استنتاج فقط).

---

## سابعًا: التحفظات والمخاطر

1. **لا يوجد حقل اسم موقع في CAD** — أي ادعاء بـ«قراءة اسم الموقع من CAD» غير صحيح؛ كل التعرف استنتاج بأدلة، ولذلك نظام الثقة ليس رفاهية بل أساس.
2. **دقة الإحداثيات:** بلاغات كثيرة إحداثياتها على مستوى الشارع/الحي لا المبنى — `radiusM` يجب أن يُضبط بالقياس على البيانات الحقيقية، لا بافتراض.
3. **الأسماء المتشابهة:** «مدرسة الملك خالد» قد تتكرر في أحياء مختلفة — لذلك R2 تشترط دليلًا إحداثيًا مصاحبًا للاسم.
4. **Likely لا يقود قرارًا تشغيليًا** — هو سياق فقط؛ التنبيهات والتصنيف الحاسم لـ Confirmed وحده.
5. **المواقع المرشحة تحتاج مراجعة بشرية** — لا يوجد مسار آلي من candidate إلى active.
6. **لا نبني قاعدة ضخمة يدويًا** — البذرة صغيرة والتعلم تدريجي من البيانات الحية، كما طلب المالك.

---

## ملخص الإجابات العشر

| # | السؤال | الجواب المختصر |
|---|---|---|
| 1 | تعميم Hospital Monitor بلا كسر | Monitor يبقى كما هو؛ القالب المعماري (هوية ثابتة + Mutable State + حقن الأرشيف) يُستنسخ، والتعميم الفعلي لاحقًا نوعًا نوعًا |
| 2 | مصادر البيانات الموجودة | incident_registry (إحداثيات/عنوان/حي/وصف) + CAD notes + map-locations.json + operationalCenters |
| 3 | قاعدة إحداثيات موجودة؟ | نعم جزئيًا: 12 مستشفى + المراكز — والأهم آلاف البلاغات المؤرشفة للتعلم |
| 4 | بنية موحدة | سجل `places` واحد بـ `placeType` — لا نظام لكل نوع |
| 5 | عملية التعرف | R1-R5 مرتبة: إحداثي داخل نصف قطر ← اسم+إحداثي ← اسم فقط ← نوع نصي فقط ← Unknown |
| 6 | نظام Confidence | Confirmed/Likely/Unknown؛ الزمن وتشابه النص وحدهما لا ينتجان حسمًا |
| 7 | الفرق بين الدرجات | Confirmed = دليل إحداثي/اسم+موقع · Likely = نصي قوي فقط · Unknown = لا أدلة |
| 8 | دخول الأسطح | الخريطة عبر map-adapter · التنبيهات بنمط alert-rules · الأرشيف بنمط _getHospital · كلها تقرأ ناتج المحرك فقط |
| 9 | لا مصدر حقيقة ثاني للمستشفيات | مخزن 1B يظل مصدر hospitalName الحصري؛ سجل places يحمل مواقع المستشفيات للتطابق الإحداثي فقط |
| 10 | Hospital Monitor كنوع واحد | مستقبلًا وبلا كسر: hospital يصبح placeType ضمن القالب العام بعد استقرار المحرك |

---

**الحالة:** دراسة معمارية جاهزة لاعتماد المالك. لا تعديل على الكود، لا Commit/Push/Deploy.
