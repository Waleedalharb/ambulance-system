# التقرير النهائي — الطبقة التاريخية التحليلية للخريطة (H1–H4)
**التاريخ: 2026-08-24** | النطاق: تنفيذ كامل H1→H4 بتوجيه المالك (16:13) | الحالة: مكتمل محليًا — **بلا Deploy ولا Push ولا Git** بانتظار مراجعة المالك

---

## 1. ماذا أصبح المنتج قادرًا على فعله؟

سطح تحليلي واحد متكامل على `/history-map.html` بأربعة تبويبات يقودها نطاق زمني مشترك (اليوم / 7 أيام / 30 يومًا / مخصص):

| التبويب | القدرة |
|---|---|
| **الخريطة التاريخية (H1)** | كل بلاغات النطاق على الخريطة — المنتهي حدث فعلًا ولا يختفي تحليليًا — بتمييز لوني للحالة (نشط/مغلق/ملغى)، كثافة حرارية، مواقع ساخنة (شارع/حي ×2+)، شارات أكثر الشوارع، وبطاقة بلاغ تعرض الفرق المحتسبة والمستبعدة **بأسبابها** (يدوي/مسحوبة/قبل المباشرة/بعدها) وأزمنة الاستجابة. |
| **الأنماط الزمنية (H2)** | توزيع بالساعة (24) واليوم (7) والفترة (صباح/مساء/ليل) والتسلسل اليومي عبر النطاق، ساعة ويوم الذروة، أكثر 10 أحياء وشوارع، ومقارنة بالفترة السابقة المماثلة (إجمالي، فرق، متوسط يومي، مقارنة بالأنواع). |
| **الطلب × التمركز (H3)** | خريطة تربط كل بلاغ محدد الموقع بنوافذ التمركز الفعلية المسجلة: أقرب تمركز نشط وقت البلاغ + تمركز الفرقة المباشرة نفسها؛ مناطق الطلب (خلايا ~1.1كم ×3 بلاغات+) مصنَّفة مغطاة/ضعيفة بأسباب معلنة. |
| **دعم القرار (H4)** | مواقع تمركز مرشحة مرتبة بالأولوية (طلب × معامل استجابة)، نوافذ يوم×ساعة تستحق إعادة التوزيع، مقارنة بالفترة السابقة، ومحاكاة افتراضية لكل مرشح: كم بلاغًا تاريخيًا ضمن 3كم وما متوسط استجابته الفعلي مقابل النطاق. **تحليل فقط — لا ينفذ أي إجراء.** |

## 2. ما البيانات التي يعتمد عليها كل تحليل؟

| التحليل | مصدره الخام |
|---|---|
| موقع/نوع/حي/شارع/حالة/وقت إنشاء البلاغ | `incident_registry` (أصلية من CAD عبر الـOverlay — التقاط فقط) |
| الفرقة المباشرة وأزمنة الاستجابة | `report_times` (phases الخام + resp_* المحسوبة) بقاعدة الاحتساب الواحدة `isParticipationCounted` — لا withdrawn ولا manual_cancelled ولا بلا تحرك |
| التمركز التاريخي | `positioning_events` (append-only) — لقطة الخطة كاملة عند كل حدث، أزمنتها عبر `PositioningService.parseRiyadhWall` المركزي |
| الفترات/الذروة/الأنماط | اشتقاق من `cad_created_at` عبر محلل CAD المركزي `_cadDateTimeTs` (بديل صادق: `created_at`) |

## 3. ما الذي تم تخزينه وما الذي يُشتق؟

**لم يُضف أي تخزين جديد إطلاقًا — صفر ترحيلات، صفر جداول.** كل ما في الطبقة اشتقاق وقت الاستعلام من الجداول الأصلية، وأي رقم قابل للتتبع إلى صفوفه:

- **مخزَّن (قائم من قبل):** سجل البلاغات بموقعه ونوعه وأزمنة إنشائه، مشاركات الفرق بمراحلها الخام وأزمنة استجابتها وحالات استبعادها، سجل أحداث التمركز.
- **مشتق (لا يُخزن):** الفترات، الذروة، التوزيعات، المناطق الساخنة، خلايا الطلب، تصنيف ضعف التغطية، المرشحون، المحاكاة، المقارنات — كلها تُعاد بناؤها من الخام عند كل استعلام.

## 4. نتائج الاختبارات H1–H4

كلها معزولة كاملًا (VACUUM INTO + DATA_DIR مؤقت + بيانات اصطناعية مستقبلية يناير 2027) — لا لمس لقاعدة التشغيل:

| الطاقم | النتيجة |
|---|---|
| H1 `analytics-historical-test.js` (حماية/ترشيح زمني/صدق/اشتقاق/أزمنة/مؤشرات) | **37/37 ✅** |
| H2 `analytics-patterns-test.js` (ساعة/يوم/فترة/ذروة/ترتيبات/مقارنة) | **23/23 ✅** |
| H3 `analytics-coverage-test.js` (ربط تمركز/مناطق طلب/ضعف تغطية) | **20/20 ✅** |
| H4 `analytics-recommendations-test.js` (مرشحون/محاكاة/نوافذ/قراءة-حصرًا) | **17/17 ✅** |
| انحدار: `cad-overlay-observer-test.js` | 25/25 ✅ |
| انحدار: `cad-manual-cancel-test.js` | 24/24 ✅ |
| انحدار: `cad-report-integration-test.js` | 88/0 ✅ |

**المجموع: 97 اختبار تحليلات جديد + 137 انحدارًا — صفر فشل.**

## 5. القيود والبيانات الناقصة الحقيقية

1. **أساس التوقيت:** محلل CAD يفسّر أوقات CAD بتوقيت الخادم المحلي. على جهاز يعمل بتوقيت الرياض كل شيء صحيح؛ على خادم UTC ستنزاح **تسميات** الساعة/الفترة 3 ساعات (الفروق والأزمنة لا تتأثر لأنها طرح داخل نفس الأساس). التخفيف: تشغيل الخادم بـ `TZ=Asia/Riyadh`.
2. **التمركز المعروف محدود بالمسجَّل:** من لا تمركز له في `positioning_events` وقت البلاغ يظهر «بلا تمركز معروف» بصدق. قيمة H3/H4 ترتفع كلما زاد تسجيل التمركزات في المنصة.
3. **الضغطات اليدوية** (بلا رقم CAD) خارج الطبقة بنيويًا — لا موقع لها — وموثقة في التحفظات.
4. **البلاغات بلا إحداثيات** تُحسب في الإجماليات وتُدرج في قائمة «بلا موقع دقيق» ولا تُرسم — لا تخمين.
5. **ثوابت الاشتقاق موثقة وقابلة للضبط بقرار:** الفترات (07–15/15–23/23–07)، خلية الطلب (~1.1كم ×3 بلاغات)، «بعيد» (3كم)، نصف قطر المحاكاة (3كم).
6. **عند التعادل** في ساعة/يوم الذروة يفوز الأصغر ترقيمًا (حتمي وموثق في الاختبارات).

## 6. مثال واقعي: من بلاغات تاريخية إلى توصية تمركز

السيناريو التالي ليس وصفيًا — بل هو **سيناريو الاختبار المثبت فعليًا** (H3+H4):

> **بلاغات تاريخية:** في نطاق 9–11 يناير: 3 بلاغات في خلية قريبة من نقطة تمركز الفرقة «901» (وصول 10د)، و3 بلاغات في خلية «الحي البعيد» (وصول 25د لكل منها)، وبلاغ بلا إحداثيات (حُسب ولم يُرسم).
>
> **نمط (H2):** ساعتا الذروة 14:00 و20:00 يوم الأحد ببلاغين لكل منهما؛ النطاق أعلى من الفترة السابقة المماثلة (+7 بلاغات، الفارق كامل لأن السابقة فارغة).
>
> **فجوة تغطية (H3):** خلية «الحي البعيد» صُنفت **ضعيفة** بسببين معلنين: أقرب تمركز وقت البلاغات >20كم (الحد 3كم)، ومتوسط الوصول 25د أعلى من متوسط النطاق 19د. الخلية القريبة «مغطاة» (تمركز على <1كم ووصول 10د).
>
> **توصية تمركز (H4):** المرشح الأول = مركز خلية «الحي البعيد» (أولوية 3.95 = 3 بلاغات × 25/19)، مع محاكاة: «لو وُجد تمركز هنا لغطّى 3 بلاغات تاريخية ضمن 3كم (50% من بلاغات النطاق المحددة) — متوسط استجابتها الفعلي 25د مقابل 19د للنطاق». والنافذة المقترحة لإعادة التوزيع: الأحد 14:00.
>
> **ثم يتوقف النظام هنا.** القرار — إنشاء تمركز فعلي أو تعديله — يبقى لصاحبه البشري عبر الأدوات التشغيلية القائمة.

على البيانات الحقيقية ستسير نفس السلسلة، وستقوى التوصيات مع تراكم البلاغات والتمركزات المسجلة.

---

## ملفات التنفيذ (كلها في working tree — غير مودعة في Git)

| الملف | التغيير |
|---|---|
| `services/report-service.js` | `_incidentsInRange` · `_cancelKind` · `_haversineKm` · `getHistoricalSummary` (H1) · `getPatterns` (H2) · `getCoverage` (H3) · `getRecommendations` (H4) + ثوابت موثقة |
| `storage-adapter.js` | `getIncidentHistoryData()` · `getPositioningHistory()` — قراءة صرفة |
| `server.js` | 4 مسارات GET خلف `authenticate` + حارس نطاق مشترك |
| `public/history-map.html` | السطح المتكامل بأربعة تبويبات — **لم يُمس أي ملف قائم آخر** |
| `scripts/analytics-*-test.js` | 4 أطقم اختبار معزولة جديدة |
| `DECISION-HISTORICAL-ANALYTICS.md` | وثيقة القرار (معتمدة ومُنفذة بالكامل) |
