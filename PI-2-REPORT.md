# تقرير إثبات تنفيذ PI-2 — اكتشاف المرشحين + المراجعة البشرية
## 2026-08-29 · تنفيذ محلي فقط · **لا Commit / Push / Deploy تم**

**المرجع:** `pi-2-directive-draft.md` (الأمر + الحواجز الثلاثة المعتمدة) · `PI-1-REPORT.md` (اعتماد PI-1 وشروطه)

---

## 1. Before → After

| | Before (بعد PI-1) | After (PI-2) |
|---|---|---|
| اكتشاف المواقع من البيانات التاريخية | غير موجود | `scripts/place-discovery.js` — اسمي + مكاني خالص |
| المراجعة البشرية | غير موجودة | `scripts/places-review.js` — list/apply بلا واجهة |
| اعتماد Active | `approveCandidate` أساسي | مسار كامل: رفض بلا مبرر + تنقيح بشري + توثيق radiusJustification إلزامي |
| الخطوط الحمراء | قائمة | **لم تُلمس** — الانحدار مطابق للـbaseline (البند 8) |

## 2. الملفات المنشأة والمعدّلة

| الملف | الحالة |
|---|---|
| `scripts/place-discovery.js` | **جديد** — الاستخراج + التجميع + الحواجز الثلاثة |
| `scripts/places-review.js` | **جديد** — قائمة المرشحين + تطبيق القرارات البشرية |
| `scripts/place-discovery-test.js` | **جديد** — 13 اختبارًا (Golden 11 + بندان) |
| `services/place-intelligence-service.js` | **توسيع فقط** (ملف PI-1 الجديد أصلًا): `approveCandidate` تقبل مبرر النطاق، `reviseCandidate`، `setCandidateStatus` — لا تغيير في منطق R1–R5 (اختبارات PI-1 الـ26 ما زالت خضراء) |

**ملفات الأنظمة القائمة المعدّلة: صفر** (git — البند 8).

## 3. المرشحون المكتشفون من البيانات الحقيقية — بصدق كامل

> **البلاغات: 145 · غير المحسومة: 145 · مرشحون مكتشفون: 0**

**الأسباب الموثقة (provenance):**
- العنوان الوحيد الحامل لاسم جهة في السجل كله: بلاغ 1303999 «ملعب الاطفال الثاني بحي العزيزية» — ذِكر مفرد، والعتبة المعتمدة ≥2 للاسم → **لا مرشح** (لا اختراع مرشح من ذِكر واحد).
- التجمع المكاني الخالص: 5 أزواج فقط ضمن 500م (أقربها 131م)، ولا خلية ~150م فيها ≥3 بلاغات → **لا مرشح مكاني**.
- `description` فارغ في 145/145 — المسار النصي الوحيد هو العنوان.

**الخلاصة الصادقة:** حجم العينة المحلية (145 بلاغًا) صغير، والاكتشاف الحقيقي سيبدأ مع تراكم البيانات الحية. المحرك لم يخفّض العتبات ولم يخترع مرشحين لإظهار نتائج.

## 4. Golden Set حالة بحالة (13/13 ✅)

| # | الحالة | النتيجة | الدليل |
|---|---|---|---|
| P1 | استخراج «مدرسة النور الابتدائية» والوقوف عند «بحي» | ✅ | nameGuess=`مدرسه النور الابتدائيه` (3 كلمات، توقف عند كلمة الإيقاف) |
| P2 | «مدرسة النور»+«مدرسه النور» = مرشح واحد | ✅ | التطبيع وحّدهما، incidentCount=2 |
| P3 | «المستشفي» لا يطابق «مستشفى» | ✅ | حدود الكلمة الكاملة منعت المطابقة |
| P4 | نفس الاسم ×3 ضمن 100م ← مرشح واحد | ✅ | (أصلح خلل ازدواج مسارَي الاسم/المكان أثناء الاختبار — بلاغ التجمع الاسمي لا يدخل التجمع الخالص) |
| P5 | نفس الاسم على بعد ~5.7كم ← **مرشحان** | ✅ | حاجز ①: الـ500م نطاق اكتشاف فقط — لا دمج بالقوة |
| P6 | تجمع خالص ×4 بلا اسم ← placeTypeGuess=null | ✅ | حاجز ②: لا يخترع «مدرسة» مهما كان العدد |
| P7 | اعتماد بلا radiusJustification ← **مرفوض** | ✅ | «ممنوع الاعتماد بلا مبرر موثق للنطاق» + السجل لم يدخله شيء |
| P8 | اعتماد مكتمل ← active + placesVersion+1 + المبرر في evidence | ✅ | v: 0←1 |
| P8b | **حاجز ③:** radiusM=62 قرارًا بشريًا وليس المقترح 78 | ✅ | `proposedRadiusM` لم يُنسخ آليًا |
| P9 | المرفوض يُوسم rejected ولا يدخل السجل نهائيًا | ✅ | |
| P10 | الاكتشاف لا يفعّل شيئًا — candidate محفوظ لكن المطابقة R5 | ✅ | |
| P11 | داخل radiusM=62 المعتمد ← R1 Confirmed · خارجه بـ~20م ← لا Confirmed | ✅ | inside=R1/93 · outside=R5 — **النطاق يمثل الجهة فعلًا** |

## 5. دورة approve/reject — إثبات

- **رفض بلا مبرر:** P7 — قرار `approved:true` برadiusM=78 وبلا `radiusJustification` رُفض، والسجل بقي فارغًا.
- **اعتماد موثق:** P8 — `radiusM:62 + "حدود سور المدرسة الفعلية"` ← active، والمبرر حاضر في `evidence` الموقع: «اعتماد بشري: candidate → active · radiusM=62 — المبرر: حدود سور المدرسة الفعلية».
- **الرفض يبقى خارجًا:** P9 — `rejected` ولا يدخل السجل نهائيًا.

## 6. proposedRadiusM مقابل القرار البشري

| المرشح | المقترح الآلي | القرار البشري | النتيجة |
|---|---|---|---|
| P8 (اصطناعي) | 78م | **62م** — «حدود سور المدرسة الفعلية» | اعتُمد 62م ولم يُنسخ 78 |
| العرض الرملي (بند 7) | 96م | **150م** — قرار موثق | اعتُمد 150م |

## 7. إعادة الحسم التحليلية (رملية — السجل الحقيقي لم يُمس)

نسخة رملية من `places.json` في %TEMP% + مرشح عند مركز أقرب زوج حقيقي (1306598 + 1307899، 131م):

```text
قبل الاعتماد: confirmed=0 · likely=1 · unknown=144
بعد الاعتماد: confirmed=2 · likely=1 · unknown=142   (placesVersion 1←2 رمليًا)
السجل الحقيقي: placesVersion=1 كما هو — لم يُمس
```

## 8. عدم الانحدار — بالأدلة

| الحزمة | النتيجة |
|---|---|
| place-intelligence (PI-1) | 26/26 ✅ |
| place-discovery (PI-2) | 13/13 ✅ |
| hospital-monitoring-lost | 48/48 ✅ |
| hospital-cycle | 53/53 ✅ |
| hospital-monitor-smoke | 16/16 ✅ |
| duplicate-detection-v2 | 13/13 ✅ |
| الانحدار الشامل (213) | **201/213 — نفس الـ12 البيئية المعروفة حرفيًا** |

## 9. Git status

```text
?? PI-1-REPORT.md · ?? pi-1-directive-final.md · ?? pi-1-review-checklist.md
?? place-intelligence-architecture.md · ?? scripts/place-discovery-test.js
?? scripts/place-discovery.js · ?? scripts/place-intelligence-historical-run.js
?? scripts/place-intelligence-test.js · ?? scripts/places-review.js
?? scripts/places-seed.js · ?? services/place-intelligence-service.js
```
آخر commit: `ba8bc29` — **صفر ملفات متتبعة معدّلة.**

## 10. تأكيد صريح

**لا Commit / Push / Deploy تم. تنفيذ PI-2 محلي بالكامل. الحواجز الثلاثة مختبَرة صراحة (P5/P6/P7/P8b). لا مرشح تحوّل إلى Active آليًا. لا واجهة ولا PI-3. متوقف بانتظار مراجعة المالك.**

## ملاحظة صادقة للمالك

الاكتشاف على 145 بلاغًا أنتج **صفر مرشحين** — وهذا نتيجة صحيحة لحجم العينة لا خلل في المحرك (Golden Set يثبت أن المحرك يكتشف عندما تتوفر الإشارة). قيمة PI-2 الحقيقية ستظهر على قاعدة الإنتاج الأكبر أو مع تراكم البيانات. **التوصية:** عند نشر مستقبلي، تشغيل `place-discovery.js` على قاعدة الإنتاج (قراءة فقط) هو أول استخدام فعلي له.

---

## ملحق: إثبات عدم تغيّر R1–R5 بعد توسعة الخدمة (طلب مراجعة المالك 2026-08-29)

**① diff التوسعة (إعادة بناء نسخة PI-1 + مقارنة نصية):**
- أسطر أُزيلت من نسخة PI-1: **2 فقط** — توقيع `approveCandidate(placeId, reviewer)` وسطر evidence القديم.
- أسطر أُضيفت: **25** — معامل `justification` الاختياري (مع مبرر النطاق في evidence) + دالتا `reviseCandidate` و`setCandidateStatus` + تعليقاتهما.

**② ما تغيّر / ما لم يتغيّر:**
| تغيّر | لم يتغيّر إطلاقًا |
|---|---|
| توقيع approveCandidate (+معامل اختياري) | `resolve()` كاملًا — صفر أسطر |
| نص evidence الاعتماد (يضيف المبرر) | `CONF_BANDS` · `TYPE_KEYWORDS` · `normalizeArabic` · `phraseInText` |
| دالتان جديدتان للمراجعة البشرية | منطق R1–R5 · السقوف · المطابقة · التخزين · `ENGINE_VERSION` |

**③ اختبار تفاضلي سلوكي:** `resolve()` من النسختين على **452 مدخلًا** (300 اصطناعي متنوع + 7 حالات طرفية + 145 بلاغًا حقيقيًا) → **صفر فروقات** في المخرجات (باستثناء resolvedAt الزمني جوهريًا)، والثوابت/التطبيع/المطابقة متطابقة. السكربت: `workspace\pi2-equivalence-proof.js` (خارج المشروع — لا تلويث).

**④ Golden Set PI-1 كما هو (بدون تعديل الاختبارات): 26/26 ✅** — وبنفس قيم confidence السابقة حرفيًا (G1=94، G3=93، G8=97، E4=98).

**⑤ لم يُعدَّل أي كود أو اختبار أثناء هذه المراجعة. لا Commit / Push / Deploy.**

---

## ✅ اعتماد المالك النهائي (2026-08-29 14:27) — PI-2 معتمد رسميًا

اجتازت التوسعة اختبار المراجعة: diff نصي + تكافؤ سلوكي على 452 مدخلًا (صفر فروقات) + Golden Set PI-1 كما هو 26/26 بنفس القيم.

**الحالة الثابتة: لا Commit / Push / Deploy — تبقى كما هي حتى يقرر المالك ماذا يُنشر ومتى.**
