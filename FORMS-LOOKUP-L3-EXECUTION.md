# تقرير تنفيذ L-3 — ربط نموذجَي حادث + تصعيد بـIncidentLookup

**التاريخ:** 2026-09-02 · **النطاق:** تنفيذ محلي فقط — **لا Commit / Push / Deploy**
**المرجع:** `FORMS-LOOKUP-L3-SPEC.md` + القرارات الثلاثة المعتمدة (حجب بلا فرقة محتسبة · `cadDescription` · تصعيد partial مسموح)

---

## 1) النتيجة

| بند | النتيجة |
|---|---|
| اختبار `scripts/incident-esc-lookup-test.js` | **16/16 ✅** (الفحوص الـ15 + 1ب/8ب) |
| انحدار `e-form-lookup-test.js` (L-2) | ✅ 16/16 |
| انحدار `incident-lookup-api-test.js` (L-1) | ✅ 14/14 |
| انحدار `forms-theme-test.js` | ✅ 47/47 |
| أخطاء Console طوال الجلسة | **صفر** |
| اللقطات السبع | `test-output/incident-esc-lookup-1788302763980/` |

## 2) الملفات التي تغيّرت (L-3 فقط)

| الملف | التغيير |
|---|---|
| `public/forms/form-incident.html` | حذف `incReportNumber` · تركيب `#incIncidentLookup` · `incDateTime`/`incLocation` read-only بوسم «من CAD» · `incUnit` قائمة ديناميكية (أُزيلت الثابتة سريع 1–4/جنوب 1–19) · `incType`/`incCenter`/بيانات المريض كما هي يدوية |
| `public/forms/form-escalation.html` | حذف `escReportNumber` · تركيب `#escIncidentLookup` · `escDateTime`/`escLocation` read-only بوسم «من CAD» · `escEventType` والإحصائيات والجهات كما هي يدوية |
| `public/js/app.js` | **6 hunks جديدة في نطاق دوال الحادث (5761–5936) والتصعيد (6902–7023) فقط** — hunks نموذج E (6603–6807) هي L-2 المعتمدة سابقًا |
| `scripts/incident-esc-lookup-test.js` | **جديد** |
| `index.html` | **صفر تغيير** — المكوّن مُحمّل أصلًا منذ L-2 |

**لم يُمسّ:** الخادم · DB · `form-e.html` ودوال E · `incident-lookup.js/.css` · PI-10 · ملفات الثيم. إعادة استخدام `cadRawToLocalInput` و`eCountedUnits` القائمتين — لا نسخ جديدة.

## 3) الإثباتات من `shift_forms` في temp DB (ليس مجرد نجاح POST)

### حادث 1310627 — الفرقة المختارة فعليًا
اختيار **جنوب 14** (وليس الأسرع): `incidentNumber:'1310627'` · `incidentUnit:'جنوب 14'` · `type:'تصادم مروري'` (يدوي) · `reportNumber:'1310627'` (توافق عرض).

### فحص 8ب — مبدأ «CAD يعبّئ البداية والأصل لا يضيع» (عينة 90000003)
| الحقل | القيمة المثبتة من DB |
|---|---|
| `cadDescription` | `وصف CAD الأصلي للاختبار — حادث تصادم مركبتين` (**أصل CAD حرفيًا**) |
| `description` | `وصف معدّل من الموظف: إصابتان بسيطتان` (**نص الموظف بعد التعديل**) |

### تصعيد 90000003 — بلا فرقة (لا حقل فرقة أصلًا)
`incidentNumber:'90000003'` · `eventType:'حادث تصادم مروري'` (يدوي — بقي فارغًا بعد التوثّق حتى اختاره الموظف) · `cadDescription` = أصل CAD · `details` = نص الموظف المعدّل · `injuries:2`.

## 4) حالات الحجب والنقص المثبتة

| الحالة | السلوك |
|---|---|
| حادث بلا فرقة محتسبة (99900127) | قائمة معطّلة «-- لا توجد فرقة محتسبة --» + الحفظ محظور حتى مع نوع حادث مختار (القرار 1) |
| بلاغ غير موجود (0000000) — النموذجان | «✗ لا يوجد بلاغ» + حارس الحمولة البالية + عدد الصفوف قبل = بعد |
| الملغاة يدويًا (1310420) | ملغاتان موسومتان معطّلتان + «جنوب 5» المحتسبة الوحيدة تُختار تلقائيًا |
| `incType`/`incCenter`/`escEventType` | تبقى فارغة يدوية بعد التوثّق — **صفر اشتقاق من `code`** |
| سجلّان قديمان (`LEGACY-INC-1`/`LEGACY-ESC-1`) | يُعرضان في القائمتين بلا كسر — لا migration |

## 5) اللقطات السبع

`test-output/incident-esc-lookup-1788302763980/`
1. `shot-inc-1-before.png` — حادث قبل البحث
2. `shot-inc-2-resolve.png` — حادث بعد الربط (وقت/موقع CAD بوسم «من CAD»)
3. `shot-inc-3-unit.png` — «جنوب 14» مختارة + نوع الحادث اليدوي + المركز اليدوي الفارغ
4. `shot-4-missing.png` — شارة النقص
5. `shot-5-notfound.png` — «لا يوجد بلاغ بهذا الرقم»
6. `shot-esc-1-before.png` — تصعيد قبل البحث
7. `shot-esc-2-resolve.png` — تصعيد بعد الربط (وصف CAD ظاهر في البطاقة + escDetails مُعبأة + نوع الحدث فارغ)

## 6) حالة شجرة العمل (للفصل المستقبلي)

تغييرات L-3 محصورة في §2. الشجرة تحمل أيضًا L-2 المعتمد (form-e.html + hunks E في app.js + سطرا index.html) وجولة الثيم وPI-10 المعلق — كلها خارج L-3 وتُفصل بالـpatch عند أي Commit مستقبلي بنفس الأسلوب المعتمد.

**لا Commit / Push / Deploy — بانتظار مراجعة المالك وأمره الصريح.**
