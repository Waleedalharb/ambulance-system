# تعليمات المطور - إصلاح التحديثات الفورية

## المشكلة
النظام يقوم بإعادة تحميل كاملة للصفحة (Full Page Refresh) بشكل متكرر، مما يسبب:
- مقاطعة المستخدم أثناء العمل
- فقدان البيانات المدخلة (تسجيل الدخول، التقارير، الرسائل)
- ظهور إشعار "تم الاتصال بالتحديثات الفورية" بشكل مزعج

## المطلوب
لا إلغاء للتحديثات الفورية، بل تنفيذها بالطريقة الاحترافية:
- منع أي `location.reload()` أو `Reload`
- تحديث جزئي (Partial Update) للبيانات فقط
- استخدام WebSocket/SSE لتحديث العناصر بدون إعادة رسم الصفحة

## الملفات المطلوبة للتعديل
1. `public/js/websocket-sync.js` - إزالة `location.reload()` من `onclose`
2. `public/js/chat.js` - إضافة reconnect بدون reload
3. `public/js/app.js` - إضافة `syncUpdate()` لكل صفحة

## الحل التقني
1. استبدال `location.reload()` في `ws.onclose` بـ `reconnect()` تلقائي
2. إضافة `syncUpdate()` function في كل صفحة لتحديث البيانات بدون reload
3. استخدام `BroadcastChannel` للتواصل بين التبويبات

## صفحة تسجيل الدخول
- مستثناة من أي تحديث تلقائي
- لا يتم إعادة تحميل الصفحة أثناء كتابة البيانات
- بعد تسجيل الدخول فقط يبدأ الاشتراك في التحديثات

## إشعار الاتصال
- يظهر مرة واحدة فقط عند أول اتصال
- إعادة الاتصال تتم بصمت بدون إشعار
- عند انقطاع الاتصال يظهر مؤشر صغير فقط

## الحفاظ على حالة الصفحة
- النص المكتوب
- موضع التمرير (Scroll Position)
- النوافذ المفتوحة
- عناصر الإدخال (Input Focus)
- أي بيانات لم يتم حفظها

## المعيار المطلوب
WhatsApp Web, Microsoft Teams, Slack, Discord

## اختبار ما قبل النشر
- [ ] عدم وجود أي Reload تلقائي
- [ ] استمرار استقبال التحديثات الفورية
- [ ] عدم فقدان البيانات أثناء الكتابة
- [ ] عدم انقطاع جلسة المستخدم
- [ ] عدم ظهور إشعارات الاتصال بشكل متكرر
- [ ] الحفاظ على الأداء وسلاسة التجربة

## الأولوية: عالية 🔴
