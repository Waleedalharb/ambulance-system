# خطة تطوير نظام حفظ وأرشفة المناوبات - منصة الجنوب

## نظرة عامة

إعادة تصميم كاملة لآلية حفظ المناوبات وصفحة الأرشيف، لضمان موثوقية البيانات التشغيلية وعدم فقدان أي معلومة.

## المشاكل الحالية

1. المصدر الأساسي للبيانات هو JSON files (shift-data.json, ambulance-data.json) وليس SQLite
2. `savedReports` في JSON تُحفظ بشكل متكرر في الذاكرة فقط
3. لا يوجد validation أن الأرشفة تمت بنجاح قبل بدء مناوبة جديدة
4. صفحة الأرشيف تعرض بيانات بسيطة فقط من JSON
5. لا يوجد حفظ مستمر (Continuous Save) أثناء المناوبة
6. لا يوجد مؤشرات بصرية لحالة الحفظ
7. لا يوجد سجل زمني (Timeline) للأحداث

## المرحلة 1: تطوير آلية الحفظ الموثوق (Shift Save System)

### 1.1 تعديل `db.js`

- إضافة `db.saveShiftData(shiftId, data)` - حفظ كامل للمناوبة
- إضافة `db.getShiftData(shiftId)` - قراءة المناوبة
- إضافة `db.getActiveShift()` - المناوبة النشطة
- إضافة `db.archiveShift(shiftId)` - أرشفة المناوبة
- إضافة `db.addTimelineEvent(shiftId, event)` - إضافة حدث زمني
- إضافة `db.getTimelineEvents(shiftId)` - قراءة الأحداث الزمنية
- إضافة `db.updateShiftMetrics(shiftId, metrics)` - تحديث المؤشرات
- إضافة `db.getShiftMetrics(shiftId)` - قراءة المؤشرات
- إضافة `db.saveShiftSnapshot(shiftId, data)` - حفظ لقطة سريعة
- إضافة `db.getLatestShiftSnapshot(shiftId)` - آخر لقطة محفوظة
- إضافة `db.verifyShiftIntegrity(shiftId)` - التحقق من سلامة البيانات

### 1.2 تعديل `server.js`

- إضافة `POST /api/shift-save` - حفظ كامل للمناوبة في SQLite
  - يستقبل كافة بيانات المناوبة
  - يحفظ في SQLite أولاً (الأساسي)
  - يحفظ في JSON ثانياً (الاحتياطي)
  - يرسل `lastSaved` timestamp
  - يضيف حدث "حفظ" في Timeline
  - يُعيد `status: success` مع `shiftId`

- إضافة `POST /api/shift-archive` - أرشفة صريحة للمناوبة
  - يُحدّث حالة المناوبة إلى `archived`
  - يضيف حدث "أرشفة" في Timeline
  - يُعيد `status: success` مع تأكيد

- تعديل `POST /api/start-new-shift` - تحقق من الأرشفة
  - قبل إنشاء مناوبة جديدة: التحقق من نجاح أرشفة السابقة
  - إذا فشلت الأرشفة: إيقاف العملية وإعادة error
  - إذا نجحت: متابعة إنشاء المناوبة الجديدة

- إضافة `GET /api/shift-status` - حالة المناوبة النشطة
  - يُعيد: `isActive`, `lastSaved`, `dataIntegrity`, `metrics`

- إضافة `GET /api/shift-timeline/:shiftId` - الأحداث الزمنية
  - يُعيد: قائمة الأحداث مرتبة زمنياً

- إضافة `GET /api/shift-snapshot/:shiftId` - آخر لقطة محفوظة
  - يُعيد: آخر نسخة محفوظة من قاعدة البيانات

- إضافة `GET /api/shift-integrity/:shiftId` - التحقق من السلامة
  - يُعيد: `integrity: true/false` مع تفاصيل

### 1.3 تعديل `public/js/app.js`

- تعديل `saveShiftData(silent)`
  - تُرسل إلى `POST /api/shift-save` بدلاً من `/api/update-shift-data`
  - تُضيف `shiftId` في البيانات
  - تُعيد `lastSaved` timestamp
  - تُحدّث مؤشر الحفظ في UI

- تعديل `autoSaveShift()`
  - حفظ كل 30 ثانية (كما هو حالياً)
  - إضافة حفظ عند كل تعديل مهم (توزيع فرق، إضافة بلاغ، إلخ)
  - إضافة `lastSaved` timestamp في UI

- إضافة `checkShiftIntegrity()`
  - التحقق من سلامة البيانات كل دقيقة
  - إظهار تحذير إذا وجد خلل

- إضافة `trackTimelineEvent(event)`
  - إضافة حدث زمني عند كل عملية مهمة
  - إرسال الحدث إلى `POST /api/shift-timeline`

## المرحلة 2: تطوير صفحة الأرشيف المتكاملة

### 2.1 تعديل `public/shift-archive.html`

إعادة تصميم الصفحة بالكامل لتشمل:

**قسم المناوبة النشطة:**
- بطاقة حالة المناوبة (Active/Inactive)
- بطاقة آخر عملية حفظ
- بطاقة وقت آخر حفظ
- بطاقة حالة سلامة البيانات
- بطاقة عدد العمليات المحفوظة
- زر "استعراض آخر نسخة محفوظة"

**مؤشرات الحفظ:**
- 🟢 جميع التعديلات محفوظة
- 🟡 يوجد تعديلات جارٍ حفظها
- 🔴 فشل آخر عملية حفظ

**لوحة الملخص الاحترافية:**
- عدد البلاغات
- عدد الفرق العاملة
- عدد البلاغات المكتملة
- عدد النماذج
- مدة المناوبة
- وقت آخر حفظ
- حالة سلامة البيانات

**تنظيم البيانات في أقسام:**
1. نظرة عامة (معلومات المناوبة الأساسية)
2. توزيع الفرق (الجدول الكامل)
3. البلاغات (قائمة + تفاصيل)
4. النماذج (قائمة + تفاصيل)
5. الملاحظات (سجل الملاحظات)
6. السجل الزمني (Timeline)

**السجل الزمني (Timeline):**
- بدء المناوبة
- تعديل توزيع الفرق
- إضافة/إغلاق بلاغ
- إنشاء نموذج
- تغيير حالة الفرق
- وقت الحفظ
- إنهاء المناوبة

**البحث والتصفية:**
- التاريخ (من/إلى)
- نوع المناوبة
- اسم المشرف
- الفرقة
- رقم البلاغ
- حالة البلاغ

### 2.2 إنشاء `public/js/shift-archive.js`

- `loadActiveShift()` - تحميل المناوبة النشطة
- `loadShiftArchive()` - تحميل قائمة الأرشيف
- `loadShiftDetail(shiftId)` - تحميل تفاصيل المناوبة
- `renderSummaryCards()` - عرض بطاقات الملخص
- `renderTimeline()` - عرض السجل الزمني
- `renderSearchFilter()` - عرض البحث والتصفية
- `handleSaveStatus(status)` - تحديث مؤشر الحفظ
- `viewShiftSnapshot(shiftId)` - عرض آخر نسخة محفوظة

## المرحلة 3: الاختبار المحلي الشامل

### 3.1 سيناريوهات الاختبار

1. **حفظ مستمر:**
   - بدء مناوبة → إضافة بلاغ → التحقق من الحفظ التلقائي
   - تعديل توزيع الفرق → التحقق من الحفظ
   - إضافة نموذج → التحقق من الحفظ

2. **الأرشفة:**
   - إنهاء المناوبة → التحقق من نجاح الأرشفة
   - التحقق من عدم السماح بمناوبة جديدة قبل الأرشفة
   - التحقق من ظهور البيانات في الأرشيف

3. **المناوبة النشطة:**
   - التحقق من ظهور المناوبة النشطة في الأرشيف
   - التحقق من عرض آخر نسخة محفوظة
   - التحقق من مؤشرات الحفظ

4. **السجل الزمني:**
   - التحقق من تسجيل جميع الأحداث
   - التحقق من ترتيب الأحداث زمنياً

5. **البحث والتصفية:**
   - البحث بالتاريخ
   - البحث باسم المشرف
   - البحث برقم البلاغ

6. **الأخطاء:**
   - محاكاة فشل الحفظ → التحقق من المؤشر الأحمر
   - التحقق من عدم الانتقال للمناوبة الجديدة

### 3.2 التحقق من Console و Server Logs

- عدم وجود أي Errors أو Warnings
- التحقق من جميع الـ API calls تنجح
- التحقق من عدم تأثر أي جزء آخر من المنصة

## الملفات المعدلة

### ملفات التعديل:
1. `server.js` - إضافة APIs جديدة وتعديل المنطق
2. `db.js` - إضافة دوال مساعدة للحفظ الموثوق
3. `public/js/app.js` - تعديل آلية الحفظ
4. `public/shift-archive.html` - إعادة تصميم الصفحة
5. `public/js/shift-archive.js` - منطق جديد للأرشيف

### ملفات جديدة:
1. `public/css/shift-archive.css` - تنسيقات صفحة الأرشيف
2. `public/js/shift-archive-detail.js` - تفاصيل المناوبة المنفردة

## ملاحظات التطوير

- عدم حذف أو تغيير أي وظيفة مستقرة
- عدم إنشاء صفحات إضافية (تعديل الصفحات الموجودة فقط)
- التركيز على الاعتماد على SQLite كمصدر أساسي
- عدم النشر قبل اجتياز جميع الاختبارات
