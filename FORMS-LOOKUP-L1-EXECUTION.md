# تقرير تنفيذ L-1 — Incident Lookup (تنفيذ محلي فقط)

**التاريخ:** 2026-09-01 · **الحالة:** نُفّذ محليًا وفق `FORMS-LOOKUP-L1-SPEC.md` حرفيًا — **لا Commit / Push / Deploy**
**قائمة No-Go التُزمت:** لا نموذج عُدّل · لا `index.html` · لا مساس بـ`incident_registry`/`report_times` · لا إعادة حساب أزمنة · لا صلاحية جديدة · لا PI-10 · لا جولة الثيم · لا إصلاحات جانبية.

---

## 1) نتيجة API كاملة — `scripts/incident-lookup-api-test.js` (خادم معزول 3101)

**14/14 ✅** — سجل: `test-output/l1-api-run.log`

| الفحص | النتيجة |
|---|---|
| تسجيل دخول admin | ✅ |
| 1) `200` + `found` + الحقول الأساسية (1310627: فرقتان، state=complete) | ✅ |
| 2) قيم الأزمنة لكل فرقة **تطابق `report_times` حرفيًا** + `bestArrivalMin=6.8` | ✅ |
| 3) رقم مكرر (1300370): الافتراضي الأحدث + `otherShifts` غير فارغة | ✅ |
| 4) `404 {found:false}` لرقم غير موجود | ✅ |
| 5) `400` للصيغ `abc` / `12` / `13 0627` / أرقام عربية + غياب المعامل | ✅ |
| 6) `401` بلا جلسة + `403` لحساب بدور `user` بلا `ops.forms` (عينة في users.json المؤقت فقط) | ✅ |
| 7) 99900127 (بلا `cad_created_at`): `state=missing` + `missing:["cad_created_at"]` + `cadCreatedAtRaw:null` | ✅ |
| 8أ) عينة اصطناعية (temp فقط): الملغاة يدويًا `counted:false` + `manualCancelled:true` + السبب + زمنها 3.0 ظاهر في سجلها | ✅ |
| 8ب) **الإثبات بالقيمة: `bestArrivalMin=12.0` وليس 3.0** — زمن الملغاة الأدنى مستبعد فعليًا | ✅ |
| 8ج) بلاغ حقيقي 1310420: ملغاتها تظهر موسومة و`bestArrivalMin=20` من المحتسبة فقط | ✅ |
| 9) `caller_number` غائب من كل الردود (فحص نصي شامل) | ✅ |
| 10) انحدار: `map-history-traceability-test` ✅ + `nav-perms-test` ✅ | ✅ |

> ملاحظة تنفيذ موثقة: التحقق من صيغة الرقم يستخدم `CAD_NUMBER_RE = /^\d{4,12}$/` **القائم** في server.js (سطر 4734) بدل نمط جديد — اتساقًا مع بقية المنصة. وحارس «قاعدة الاحتساب» هو `isParticipationCounted` القائمة بلا إعادة تنفيذ.

## 2) نتيجة UI كاملة — `scripts/incident-lookup-ui-test.js` (Chrome headless CDP، خادم معزول 3102)

**13/13 ✅** — سجل: `test-output/l1-ui-run.log` · تركيب معزول (حقن برمجي — `index.html` لم يُمسّ)

| الفحص | النتيجة |
|---|---|
| 1) تركيب معزول + صفر أخطاء console/استثناءات طوال الجلسة | ✅ |
| 2أ) «جارٍ البحث…» + قفل الإدخال والزر أثناءها (fetch مؤجل) | ✅ |
| 2ب) «✓ تم العثور على البلاغ» (1310627) | ✅ |
| 2ج) «⚠️ موجود لكن بياناته الزمنية غير مكتملة» (99900127) | ✅ |
| 2د) «✗ لا يوجد بلاغ بهذا الرقم» (0000000) | ✅ |
| 2هـ) «✗ تعذر البحث — تحقق من الاتصال» (محاكاة انقطاع) | ✅ |
| 3) البطاقة تطابق رد الـAPI: الرقم + خام CAD + الفرقتان + «أسرع استجابة 6.8 دقيقة» | ✅ |
| 4) **صفر حقل قابل للكتابة داخل بطاقة بيانات CAD** (فحص DOM: لا input/textarea/select/contenteditable) | ✅ |
| 5) «غير متاح — بيانات CAD ناقصة» تظهر · «0 دقيقة» لا تظهر إطلاقًا | ✅ |
| 6) شارة تعدد المناوبات لـ1300370 («ظهر أيضًا في مناوبة…») | ✅ |
| 7) ثيم داكن: صفر أسطح فاتحة + صفر نص داكن داخل المكوّن | ✅ |

## 3) لقطات الحالات — `test-output/incident-lookup-ui-1788296767442/`

| اللقطة | الحالة |
|---|---|
| `state-1-busy.png` | جارٍ البحث (قفل الإدخال ظاهر) |
| `state-2-found.png` | بطاقة كاملة: شارة خضراء + خام CAD + فرقتان بأزمنتهما + «أسرع استجابة 6.8» |
| `state-3-partial.png` | شارة ⚠️ كهرمانية + «غير متوفر — بيانات CAD ناقصة» بصدق |
| `state-4-duplicate.png` | شارة تعدد المناوبات |
| `state-5-notfound.png` | غير موجود |
| `state-6-error.png` | تعذر البحث |

معاينة بصرية مؤكدة للقطتين المحوريتين (found/partial): الطبقات داكنة متوافقة مع هوية المنصة، والمراحل تظهر رقائق زمنية لكل فرقة.

## 4) قائمة الملفات

**جديدة (4):** `public/js/incident-lookup.js` · `public/css/incident-lookup.css` · `scripts/incident-lookup-api-test.js` · `scripts/incident-lookup-ui-test.js`
**معدّلة (3):** `storage-adapter.js` (+10: `getIncidentRowsByNumber`) · `services/report-service.js` (+57: `getIncidentLookup`) · `server.js` (+18: المسار فقط)

## 5) Git diff / حجم التغييرات

```
 server.js                  | +18 سطرًا خاصًا بـL-1 (المسار وتعليقه)
 services/report-service.js | +57 (دالة واحدة)
 storage-adapter.js         | +10 (دالة واحدة)
 + 4 ملفات جديدة (مكوّن + ورقة ثيم + اختباران)
```
`git diff --stat` الإجمالي للملفات الثلاثة: 120 إضافة / 18 حذفًا — **الحذوف الـ18 كلها تابعة لـPI-10 المعلّق محليًا من جولة سابقة، وليست من L-1** (إعادة كتابة جسم place-suggestion).

## 6) إثبات العزل (git status)

تعديلات L-1 = hunk واحد في `server.js` (يبدأ بـ`// L-1 — Incident Lookup`) + دالة واحدة في كل من `report-service.js` و`storage-adapter.js` + الملفات الأربعة الجديدة. بقية التعديلات في الشجرة **سابقة الوجود ولم يمسسها L-1**:
- `executive-theme.css` / `smart-toolbar.css` / `index.html` ← جولة الثيم (بانتظار اعتمادك للنشر).
- `place-*` + بقية hunks `server.js` ← PI-10 (محلي غير منشور).
أي Commit مستقبلي لـL-1 سيُفصل بنفس أسلوب `rc6-commit.patch` المعتمد.

## 7) النشر

**لم يحدث أي Commit / Push / Deploy.** الخوادم المعزولة أُطفئت، وقواعد temp حُذفت، وبيانات الإنتاج لم تُمسّ.

---

**بانتظار مراجعتك أنت والمالك — ثم قرار الاعتماد أو الانتقال إلى L-2 (نموذج E).**
