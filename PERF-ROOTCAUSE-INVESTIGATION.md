# 🔴 Performance Root Cause Investigation — تقرير المرحلة الأولى (قياس فقط)

**التاريخ:** 2026-09-02 ~22:00 · **النطاق:** Investigation فقط — **صفر تعديل على أي كود قائم** · لا Commit/Push/Deploy
**الأدوات المضافة (جديدة، قراءة/قياس فقط):** `scripts/perf-rootcause-probe.js` · `scripts/perf-prod-snapshot.js`

---

## 1) المشكلة قيد التحقيق

المنصة سلسة بعد Restart لـRender، ثم يتدهور التنقل بين الصفحات تدريجيًا (~ساعة)، والـRestart يعيد السلاسة. المطلوب: سبب جذري مدعوم بأرقام، لا تخمين.

## 2) ما قِيس فعلًا

### أ) قياس محلي مضغوط (خادم معزول + نسخة temp من قاعدة الإنتاج + Chrome headless — 4.2 دقيقة استخدام حقيقي مكثف)

| المورد | T0 (بعد الإقلاع) | T90 (تنقل مكثف) | T180 (خريطة مكثفة) | T240 (خمول/Polling) | الاتجاه |
|---|---|---|---|---|---|
| Server RSS | 127.3 MB | 106.7 MB | 108.3 MB | 108.9 MB | **ثابت** (هبط بعد إقلاع) |
| Browser JS Heap | 14.4 MB | 12.7 MB | 13.4 MB | 13.6 MB | **ثابت** |
| DOM Nodes | 9,176 | 11,942 | 8,979 | 9,147 | متذبذب، يعود |
| **JSEventListeners** | **563** | **643** | **933** | **934** | **⚠️ يكبر أثناء الخريطة ولا يعود** |
| /api/data | 5.8ms | 5.5ms | 4.4ms | 4.8ms | ثابت |
| /api/peak-data | 5.5ms | 4.4ms | 4.4ms | 4.7ms | ثابت |
| /api/current-shift | 3.6ms | 3.1ms | 2.8ms | 2.3ms | ثابت |
| /api/last-update | 2.4ms | 2.3ms | 1.9ms | 2.5ms | ثابت |
| /health | 12.4ms | 8.7ms | 7.5ms | 8.5ms | ثابت |

التشغيل الثاني للمسبار أعاد إنتاج نفس الإشارة: listeners ‏563→643 (تنقل) →**933** (خريطة) →934 (خمول) — **النمو يحدث في سيناريو الخريطة تحديدًا ولا يُسترد**. (التشغيل الأول: 596→1306 في نفس السيناريو.)

> البيانات الكاملة: `test-output/perf-rootcause-1788374783713/perf-timeline.json`

### ب) لقطة الإنتاج الآن (emsoperations.online — 3 جولات × 6 نقاط)

| النقطة | الجولة 1 | الجولة 2 | الجولة 3 | الحجم |
|---|---|---|---|---|
| /health | 386ms | 349ms | 284ms | 1KB |
| /api/last-update | 314ms | 284ms | 268ms | 0KB |
| /api/peak-data | 401ms | **945ms** | 283ms | 4KB |
| /api/current-shift | 400ms | 508ms | 332ms | 0KB |
| /api/data | 333ms | 444ms | 362ms | 2KB |
| /api/employees | 367ms | 338ms | 316ms | 33KB |

**ملاحظة:** لا نعرف uptime الإنتاج لحظة القياس، لذلك هذه اللقطة «حالة راهنة» وليست نقطة على خط الزمن المطلوب. الأساس الشبكي (RTT جهازي ← Render) ~270–280ms — أي أن معالجة الخادم فوق الشبكة حاليًا صغيرة، باستثناء ارتفادة peak-data واحدة (945ms).

### ج) حجم البيانات (نسخة الإنتاج بتاريخ اليوم)

- `ambulance.db` = **17MB** فقط. أكبر الجداول: system_logs ‏14,949 · shift_roster ‏9,979 · notifications ‏8,039 · audit_log ‏2,320 · reports ‏1,082 · report_times ‏551.
- مخازن JSON كلها صغيرة: audit-log.json ‏171KB · hospital-monitor.json ‏3KB · البقية أصغر.

### د) التدقيق الثابت (قراءة كود فقط)

**الخادم:** المؤقتات محدودة ومحكومة: heartbeat للـWS (30s، ينظف) · مُقيّم تنبيهات المستشفيات (60s، `unref` + skip-if-unchanged) · تنظيف blacklist (يومي، `unref`). عملاء SSE في مصفوفة تُنقَّى عند الإغلاق/الخطأ (server.js:983–1057). **لا مؤقت يُنشأ لكل طلب ولا تراكم ظاهر في الكود.**

**الواجهة (لكل عميل):** Polling بمعدل ثابت لا يتراكم: `/api/last-update` كل **3 ثوانٍ** · `/api/peak-data` كل **10 ثوانٍ** · `loadCurrentShift` كل **60 ثانية** · وعند أي تغيير ← `loadAllData()` ← `/api/data`. كلها عبر AuthGate (تُنشأ مرة واحدة عند الإقلاع وتتوقف عند الخروج).

**مزامنة علامات الخريطة:** `syncMarkers` سليم — يزيل العلامة القديمة من المجموعة عند الاستبدال/الإزالة (smart-map.js:480–496). إذن نمو الـlisteners ليس من استبدال العلامات نفسه؛ المصدر الدقيق (دورة `focusOn`/البطاقة/التنبيهات) يحتاج اختبار عزل إضافيًا.

## 3) التصنيف بالأدلة

| الاحتمال | التصنيف | الدليل |
|---|---|---|
| SQLite / نمو البيانات | 🟦 **مستبعد** | القاعدة 17MB وأكبر جدول 15k صف؛ أزمنة الاستعلام 2–6ms ثابتة تحت الحمل |
| Server memory leak (Heap/RSS) | 🟦 **مستبعد محليًا** | RSS ثابت/هابط عبر التشغيل المضغوط (127→109MB)؛ يبقى التأكيد النهائي على خط زمن الإنتاج الحقيقي |
| API/query degradation | 🟦 **مستبعد محليًا** | كل النقاط ثابتة 2–12ms من البداية للنهاية |
| Polling/timer accumulation (خادم أو واجهة) | 🟦 **مستبعد** | كلها singletons بمعدل ثابت؛ لا إنشاء مؤقتات لكل تنقل |
| SSE/connection accumulation | 🟨 **غير محسوم** | المصفوفة تُنقَّى في الكود، لكن لم يُقَس عدد الاتصالات الفعلي على الإنتاج مع الوقت |
| **Map-side browser accumulation (listeners)** | 🟨 **Likely — إشارة مقاسة** | listeners ‏+290 إلى +710 خلال 90 ثانية تفاعل خريطة، **لا تُسترد**، في تشغيلين مستقلين. لكن heap ثابت — التأثير الوظيفي يحتاج إثباتًا إضافيًا |
| Render resource limitation (CPU/شبكة الخطة المجانية) | 🟨 **غير محسوم** | ارتفادة peak-data ‏945ms على الإنتاج مقابل 4.4ms محليًا تلمّح لخنق خارجي متقطع؛ يحتاج خطًا زمنيًا |
| Browser degradation عام (غير الخريطة) | 🟨 غير محسوم | التنقل المكثف رفع listeners ‏+80 فقط وheap ثابت — ضعيف كسبب وحيد |

## 4) التناقض المركزي الذي يجب حسمه

**الدليل المحلي يشير للمتصفح (الخريطة)، بينما ملاحظة «Restart يصلح» تشير للخادم.** الاحتمالان لتفسير ذلك:
1. البطء خادمي فعلًا (مورد يتراكم على Render لا يظهر في تشغيل 4 دقائق محلي — يحتاج خط الزمن الحقيقي).
2. البطء متصفحي، والـRestart يرافقه عادةً إعادة فتح/تحديث المتصفح فيخفي السبب الحقيقي.
الاختبار الحاسم بينهما: **عند ظهور البطء القادم، حدّث المتصفح فقط (F5) دون Restart** — إن عادت السلاسة فالمشكلة متصفحية، وإن بقي البطء فهي خادمية.

## 5) الاختبارات التالية المطلوبة (بأولوية الحسم)

1. **خط زمن الإنتاج الحقيقي:** بعد أول Restart قادم، أخذ لقطة `perf-prod-snapshot.js` عند t=0/15/30/60 دقيقة + تسجيل لحظة ظهور البطء. (يمكنني أتمتتها كل 15 دقيقة كمهمة قياس قراءة فقط إن اعتُمدت.)
2. **اختبار F5 دون Restart** عند ظهور البطء (يحسم §4 فورًا) + فتح المنصة من جهاز ثانٍ بنفس اللحظة.
3. **عزل مصدر listeners الخريطة:** تشغيل محلي يفصل renderIncidents وحده عن دورة focusOn/البطاقة (30 دقيقة) لتحديد المكوّن المسرّب بالضبط.
4. قياس عدد اتصالات SSE المفتوحة على الإنتاج مع الوقت (يحتاج عدّادًا — **يتطلب اعتمادًا لأنه تعديل كود، ولم أُنفذه**).

## 6) إثبات عدم التغيير

`git status` بعد التحقيق مطابق حرفيًا لما قبله: كل ملفات M هي مراحل سابقة معتمدة (L-1→L-6 + جولة الثيم) أو PI-10 المعلق. **التحقيق أضاف ملفين جديدين فقط** (`scripts/perf-rootcause-probe.js` · `scripts/perf-prod-snapshot.js`) وهذا التقرير. صفر تعديل على server.js / DB / API / الخريطة / Render.

## 7) الحكم الحالي

**السبب الجذري غير محسوم بعد — بصدق.** المستبعد بالأرقام: قاعدة البيانات، تدهور الاستعلامات، تسرب ذاكرة الخادم (محليًا)، تراكم الـPolling. أقوى إشارة مقاسة: تراكم listeners في مسار الخريطة بالمتصفح (🟨 Likely) — لكنها لا تفسر وحدها لماذا يصلح Restart ما لم يكن التحديث المتصفحي مرافقًا له. القرار النهائي معلّق على اختبار §5-2 (F5 دون Restart) وخط زمن الإنتاج §5-1.
