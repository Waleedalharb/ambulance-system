# تقرير تشخيص UI-Map / لوحة العمليات — المرحلة A (تشخيص فقط، بلا أي تعديل)

**التاريخ:** 2026-08-31
**النطاق:** ① `/api/audit-log` = 500 · ② تداخل طبقات الخريطة (z-index) · ③ الخريطة الفاتحة مع الواجهة الداكنة
**القيود الملتزَم بها:** لا تعديل أي ملف · لا Commit/Push/Deploy · لا خلط مع Place Intelligence / PI-10 / البلاغات / الصلاحيات · لا تغيير مزود الخرائط.

---

## ① `/api/audit-log` = HTTP 500 — السبب الجذري مثبت

### السلسلة المثبتة

```
GET /api/audit-log          server.js:10346  (محروس بـ authenticate فقط)
        ↓
readAuditLog()              server.js:2018-2026
        ↓
fs.readFile(data/audit-log.json) ثم JSON.parse(data)
        ↓
إن كان الملف تالفًا (JSON مقطوع) ← JSON.parse يرمي SyntaxError
        ↓
الـ catch الداخلي يعالج ENOENT فقط (ملف غير موجود ← [] طبيعي)
وأي خطأ آخر يُرمى للأعلى
        ↓
الـ catch الخارجي server.js:10378-10380 يرجع:
        500 { "error": "فشل في جلب سجل التدقيق" }
        — دون تسجيل الخطأ نفسه في console (لا console.error هنا)
```

### لماذا يتلف الملف؟

`writeAuditLog()` (`server.js:2028-2030`) تكتب بـ `fs.writeFile` **مباشرة على الملف نفسه** — بلا ملف مؤقت ولا `rename`. أي إعادة تشغيل/إيقاف للخدمة (Render restart/deploy) أثناء الكتابة يترك `audit-log.json` **مقطوعًا نصفيًا**. بعدها كل قراءة تفشل بـ JSON.parse إلى الأبد — لأن الكتابة التالية نفسها تبدأ بـ `readAuditLog()` داخل `addAuditLogEntry` (`server.js:2034`) فتفشل أيضًا، فلا يُصلح الملف نفسه أبدًا. وهذا يفسر لماذا الخطأ **دائم** وليس متقطعًا.

### الدليل من الإنتاج (قراءة فقط)

```
GET https://emsoperations.online/api/audit-log   (توكن operator — قراءة فقط)
→ HTTP 500  { "error": "فشل في جلب سجل التدقيق" }
```

### ملاحظة مهمة عن مصدر الحقيقة

الـ endpoint يدمج أيضًا سجل SQLite (`db.AuditLog.getAll()`، `server.js:10351-10376`) — لكن دمج SQLite محمي بـ try/catch داخلي، **بينما قراءة JSON تقع في المسار الرئيسي غير المحمي**. أي أن بيانات SQLite سليمة غالبًا لكن لا يصل إليها أحد لأن قراءة JSON تفشل قبل الدمج.

### المستهلكون المتأثرون بالواجهة (3)

| المستهلك | الملف |
|---|---|
| لوحة الإدارة (سجل التدقيق) | `public/admin-dashboard.html:290` |
| لوحة العمليات «المؤشرات المباشرة» | `public/operations-dashboard.html:1051-1057` |
| الواجهة الرئيسية | `public/js/app.js:1195` |

### أقل إصلاح مقترح (للمرحلة B — لم يُنفَّذ)

1. **`readAuditLog`:** عند فشل JSON.parse (وليس ENOENT): إعادة تسمية الملف التالف إلى `audit-log.json.corrupt-<timestamp>` (حفاظًا على الدليل) وإرجاع `[]` — فيتعافى النظام ذاتيًا من أول كتابة لاحقة.
2. **`writeAuditLog`:** كتابة ذرّية: `writeFile` إلى `audit-log.json.tmp` ثم `rename` — يمنع تكرار السبب الجذري عند أي restart مستقبلًا.
3. **الـ catch الخارجي (`server.js:10378`):** إضافة `console.error('[AuditLog] GET error:', error)` حتى لا يبقى 500 صامتًا في السجلات.

**الملفات المتأثرة:** `server.js` فقط (3 نقاط، ~15 سطرًا). لا تغيير واجهة، لا تغيير مخطط بيانات، لا تغيير صلاحيات.

---

## ② تداخل طبقات الخريطة (z-index) — خريطة كاملة + تصادمات مرشحة

> **تنبيه شفافية:** الصور المرفقة في أمر الجولة لم تصلني (لا أستطيع رؤية المرفقات)، لذلك لم أحدد زوج العناصر المتداخلة **بالعين**. ما يلي خريطة z-index كاملة مثبتة من الكود + التصادمات المؤكدة بنيويًا. أحتاج من المالك تأكيد الزوج المرئي (أو وصفه) قبل تثبيت الإصلاح في المرحلة B.

### أولًا: المنصة فيها سطحا خريطة مختلفان تمامًا

| السطح | الملف | المزود الفعلي |
|---|---|---|
| الخريطة الذكية الرئيسية (opsMap / histMap) | `public/index.html` | **Mapbox GL** في الإنتاج (عبر map-adapter) / Leaflet محليًا |
| خريطة لوحة العمليات (operationsMap) | `public/operations-command.html:2789-2794` | **Leaflet + CARTO dark_all** دائمًا (داكنة أصلًا) |

### ثانيًا: خريطة z-index المجمعة (index.html — الخريطة الذكية)

| العنصر | z-index | المصدر |
|---|---|---|
| `#histMap` (وضع الذاكرة) | 500 | executive-theme.css:2495 |
| `.smap-hist-dock` (لوحة الذاكرة) | 600 | executive-theme.css:2497 |
| `.ops-map-tile-note` (ملاحظة البلاطات) | 800 | executive-theme.css:963 |
| `.smap-card` (بطاقة القرار — أعلى اليسار) | **1100** | executive-theme.css:1083 |
| بطاقة أسفل اليمين | **1100** | executive-theme.css:1155 |
| `.smk-alertbadge` (شارة الزاوية) | **1199** | executive-theme.css:1270 |
| `.smk-alertbar` (شريط التنبيه) | **1200** | executive-theme.css:1249 |
| وضع ملء الشاشة للخريطة | 4000 | executive-theme.css:1223 |
| `.mguide-backdrop` (دليل الاستخدام) | 9000 | executive-theme.css:2581 |
| `.minfo-pop` (نافذة معلومات) | 9500 | executive-theme.css:2573 |
| `.smart-sidebar` (القائمة الجانبية) | **1100** | smart-toolbar.css:1458 |
| `.modal` (النوافذ العامة) | 1000 | app.css:1125 |
| `.peak-alert-overlay` | 999 | app.css:4455 |
| `.skeleton-container` | 9998 | index.html:241 |
| `#loginScreen` / `#chatPreviewPanel` | 9999 | index.html:330 / 721 |
| `.page-loading-overlay` | 99999 | index.html:230 |

### ثالثًا: تحليل سياقات التكديس (ما هو سليم وما هو خطر)

**سليم — عزل صحيح:**
- `.ops-map-section` عليها `backdrop-filter: blur(14px)` (executive-theme.css:900) — وهذا ينشئ **Stacking Context** يحبس كل z-index داخل قسم الخريطة (500/600/1100/1200) فلا تتسرب فوق القائمة الجانبية أو الشريط العلوي.
- `.ops-map-wrap` عليه `overflow: hidden` (executive-theme.css:950) — يقص أي عنصر داخلي عند حدود إطار الخريطة.
- `.map-container` في operations-command.html:960-965 بنفس الوصفة (relative + overflow:hidden) — عناصر Leaflet الداخلية مقصوصة داخل الإطار.

**نقاط التصادم المؤكدة بنيويًا (داخل إطار الخريطة):**

1. **وضع Leaflet (محلي/احتياطي):** مكتبة Leaflet تفرض `.leaflet-top/.leaflet-bottom { z-index: 1000 }` على أدوات التحكم (زوم/إسناد) و`.leaflet-popup-pane { z-index: 700 }`. كلاهما **أعلى من `.smap-hist-dock` (600)** — أي لوحة الذاكرة تُغطَّى بأزرار الزوم والنوافذ المنبثقة عند فتحها.
2. **وضع Mapbox (الإنتاج):** عناصر `smap-light-ops` (بطاقة القرار 1100، شريط التنبيه 1200، لوحة الذاكرة 600) مبنية على افتراض Leaflet ولم تُعاير على طبقات Mapbox GL الداخلية؛ ونافذتا `.mapboxgl-popup` و`.mapboxgl-ctrl-group` لا z-index صريحًا لهما في ثيمنا — تعتمدان على ترتيب DOM داخل حاوية الخريطة.
3. **تعادل 1100 ↔ 1100:** `.smart-sidebar` (1100) مع `.smap-card` (1100) — التعادل يُحسم بترتيب DOM. حاليًا القائمة متأخرة في DOM فتكسب، لكن أي إعادة ترتيب مستقبلية تقلب النتيجة. قيم مكررة بهذا الشكل هشّة.
4. **الشريط العلوي للوحة العمليات:** `.topbar { z-index: 1000 }` مقابل `.sidebar` سطح مكتب `900` وجوال `1100` (operations-command.html:188/314/1130) — نطاق الجوال يقفز فوق الشريط العلوي، وهذا مقصود ظاهريًا لكنه يجعل أي بطاقة زائلة داخل الخريطة (بلا حبس stacking context) عرضة للظهور فوق الشريط.

### الحكم المرحلي للبند ②

البنية العامة سليمة على مستوى «الخريطة لا تغطي المنصة»، والخلل المتوقع **داخل إطار الخريطة نفسه**: لوحة الذاكرة (600) تحت أدوات/نوافذ الخريطة (700–1000) في وضع Leaflet، وتعاير غير مثبت في وضع Mapbox.

**مطلوب من المالك قبل المرحلة B:** تأكيد زوج العناصر المتداخلة المرئي لديك (مثال: «لوحة الذاكرة تُغطى بأزرار الزوم» أو «بطاقة القرار تظهر فوق شريط التنبيه») — أو لقطة توضح الصفحة والحالة (وضع عادي أم ذاكرة). عندها يثبت الإصلاح على الزوج المؤكد بدل لمس سلم z-index كامل.

---

## ③ الخريطة الفاتحة مع الواجهة الداكنة — السبب مثبت بالكامل

### السلسلة المثبتة

```
متغير البيئة MAPBOX_PUBLIC_TOKEN في Render (مضبوط — مؤكد)
        ↓
GET /js/map-config.runtime.js        server.js:793-804
  يحقن: window.SMAP_MAP_CONFIG = { provider: "mapbox",
        accessToken: "pk.…", style: "mapbox://styles/mapbox/light-v11" }
        ↓
map-adapter.js — MB.map() (سطر 164-176) يرسم بالنمط light-v11
        ↓
applyLightOpsTheme() (map-adapter.js:147-157)
  يضيف كلاس smap-light-ops على حاويات الخريطة
        ↓
executive-theme.css:2658-2804 — ثيم فاتح كامل للعناصر
  (بطاقة القرار، شريط التنبيه، لوحة الذاكرة، النوافذ المنبثقة)
```

### الدليل من الإنتاج (قراءة فقط)

```
GET https://emsoperations.online/js/map-config.runtime.js
→ window.SMAP_MAP_CONFIG = { provider: "mapbox", …,
   style: "mapbox://styles/mapbox/light-v11" }
```

### ملاحظة حوكمة مهمة

النمط الفاتح **ليس خطأً عارضًا** — النمط `light-v11` وثيم `smap-light-ops` معتمدان بقرار موثق («اعتماد المالك 2026-08-28 — المسار ب») في تعليقات `server.js:788` و`map-adapter.js:142-146`. لذلك تحويلها إلى داكن هو **تغيير قرار تصميم معتمد سابقًا** ويحتاج اعتمادًا صريحًا جديدًا، مع أن طلب هذه الجولة («مظهر فاتح مع واجهة داكنة») يشير إلى أن المالك يريد الداكن الآن.

### أقل إصلاح مقترح (للمرحلة B — يحتاج اعتماد)

- **الخيار 1 (الأدنى):** تبديل النمط في `server.js:800` من `light-v11` إلى `dark-v11` + إيقاف إضافة كلاس `smap-light-ops` في `map-adapter.js` (أو جعله مشروطًا بالنمط الفاتح) — فتعود قواعد الثيم الداكن الأصلية (الموجودة أصلًا في executive-theme.css قبل 2658) للعمل حرفيًا. تغيير عرض فقط، **نفس المزود، نفس التوكن، لا تغيير مصدر خرائط**.
- **الخيار 2:** إبقاء الفاتح وإضافة وضع داكن قابل للتبديل — أكبر نطاقًا، **لا أوصي به في هذه الجولة**.

**الملفات المتأثرة (الخيار 1):** `server.js` (سطر واحد) + `public/js/map-adapter.js` (سطور applyLightOpsTheme). ولا يُمس `executive-theme.css`.

---

## ملخص الملفات المتوقع تعديلها في المرحلة B (لم تُعدَّل بعد)

| البند | الملف | حجم التغيير |
|---|---|---|
| ① audit-log 500 | `server.js` | ~15 سطرًا (قراءة متسامحة + كتابة ذرّية + تسجيل خطأ) |
| ② تداخل الطبقات | `public/css/executive-theme.css` (وربما `smart-toolbar.css`) | رفع/خفض z-index للزوج المؤكد فقط — بعد تأكيد المالك |
| ③ الخريطة الفاتحة | `server.js` + `public/js/map-adapter.js` | سطر النمط + شرط الكلاس — بعد اعتماد تغيير القرار التصميمي |

## ما لم يُمَسّ (التزامًا بحدود الجولة)

- Place Intelligence / R1–R5 / Registry / PI-10 (معلّقة بانتظار اعتماد النشر — ملفاتها المحلية لم تُدمج مع أي عمل هنا).
- مصدر الخرائط والمزود والتوكن — لم يتغير شيء.
- لا Commit / Push / Deploy. لا تعديل أي ملف إنتاجي أو اختباري.

## الجاهزية للمرحلة B

| البند | الجاهزية |
|---|---|
| ① audit-log 500 | ✅ سبب جذري مثبت + إصلاح أدنى محدد — جاهز للتنفيذ فور الاعتماد |
| ② تداخل الطبقات | ⏸️ بانتظار تأكيد زوج العناصر المتداخلة من المالك |
| ③ الخريطة الفاتحة | ✅ سبب مثبت + خيار أدنى محدد — يحتاج اعتماد تغيير قرار 2026-08-28 التصميمي |
