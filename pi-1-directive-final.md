# أمر PI-1 — Place Intelligence Engine (نسخة نهائية معتمدة 2026-08-29)

```text
معتمد PI-1 — Place Intelligence Engine

ابدأ التنفيذ المحلي فقط.
ممنوع Commit / Push / Deploy حتى اعتماد الجولة.
ولا تخلط PI-1 مع أي نشر آخر (المستشفيات/الجداول) — هذا مسار مستقل.

الهدف:
بناء نواة Place Intelligence Engine فقط، بحيث تستطيع المنصة استنتاج نوع
الموقع/الجهة المرتبطة بالبلاغ وفق قواعد Precision First.

PI-1 يشمل فقط:
1. إنشاء سجل موحد places:
   placeId, placeType, name, nameVariants[], lat, lng, radiusM,
   district, source(seed|manual|learned), status(active|candidate|retired),
   evidence[], createdAt, updatedAt, createdBy
2. إدخال بذرة المستشفيات الـ12 من map-locations.json والمراكز التشغيلية.
   تنبيه معماري: بذرة المستشفيات لغرض التطابق الإحداثي فقط؛ مصدر
   hospitalName ورحلات النقل يبقى مخزن 1B حصريًا — ممنوع إنشاء مصدر
   حقيقة ثانٍ للمستشفيات. المراكز التشغيلية تُوسم كمواقع داخلية ولا
   تُصنَّف كجهات خارجية.
3. إنشاء services/place-intelligence-service.js (خدمة معزولة، fs/path،
   تخزين JSON معزول — نفس نمط hospital-monitor).
4. تنفيذ قواعد R1–R5:
   R1: إحداثيات داخل radiusM لموقع active → Confirmed
   R2: اسم معروف مطابق (nameVariants بحدود كلمة كاملة) + توافق مكاني
       داخل radiusM للموقع المعتمد → Confirmed
   R3: اسم معروف مطابق لكن خارج radiusM أو بدون إحداثيات → Likely فقط
       (ممنوع Confirmed مهما كانت قوة التطابق النصي)
   R4: كلمة/دليل نوع المنشأة فقط → Likely فقط
   R5: لا أدلة كافية → Unknown
5. تطبيع النص العربي قبل المطابقة (همزات، تاء مربوطة، أرقام عربية،
   حدود كلمة كاملة — ممنوع substring ساذج يجعل «مدرسة خالد» تطابق
   «مدرسة الملك خالد»).
6. استخدام haversineMeters المصدَّرة من duplicate-detection — ممنوع
   كتابة نسخة ثانية.
7. الإخراج لكل بلاغ (يُخزن مفتوحًا على eventId):
   placeId, placeType, placeName, decision(confirmed|likely|unknown),
   confidence(0-100), rule(R1-R5), evidence[], resolvedAt,
   engineVersion, placesVersion
8. نظام الثقة الرقمي بسقوف ثابتة لكل قاعدة:
   R1: 90–99 (دالة المسافة الفعلية داخل radiusM، والمسافة تُذكر في evidence)
   R2: 90–97
   R3: 70–85 (سقف ثابت)
   R4: 60–75 (سقف ثابت)
   R5: 0 (Unknown بلا رقم تجميلي)
   النطاق 86–89 محظور — ممنوع Likely فوق 85 وConfirmed تحت 90.
   الرقم مشتق من الدليل الحاسم وموثق في evidence، وليس معادلة أوزان مجردة.
9. ممنوع إنشاء أي موقع active تلقائيًا من البلاغات التاريخية.
   اكتشاف candidate فقط، ولا يتحول إلى active إلا عبر مراجعة بشرية.
   خط أحمر: النظام لا يتعلم من خطئه — لا يوجد مسار آلي
   (بلاغ → استنتاج → موقع موثوق → بلاغات ترث الاستنتاج).
10. إعادة حسم البلاغات السابقة مسموحة فقط عند اعتماد بشري لموقع جديد،
    وتُسجل placesVersion مع كل نتيجة ليبقى كل تصنيف قابلًا للتتبع
    لنسخة القاعدة التي أنتجته.
11. تصميم التخزين يجب أن يسمح لاحقًا بإحصائية أرشيف قابلة للتدقيق
    بالتدرج التالي — بدون بناء واجهة الآن:
    نوع الجهة → الجهة → عدد البلاغات المؤكدة → عدد البلاغات المحتملة
    → البلاغات → تفاصيل البلاغ → الأدلة التي أدت إلى التصنيف.
    Likely لا يدخل في الأرقام الرسمية مع Confirmed.

لا واجهة. لا تعديل للخريطة. لا تنبيهات.
لا تعديل لـ Hospital Monitor أو Duplicate Detection أو report_times
أو الاحتساب أو CAD أو دورة المناوبات أو الأرشيف الحالي.

Golden Test Set إلزامي (حالات اصطناعية):
- مدرسة داخل 150م (R1)
- مدرسة خارج 150م (لا R1)
- مدرستان بنفس الاسم في حيّين مختلفين (R2 يفرّق بالإحداثي داخل radiusM)
- كلمة «مدرسة» فقط في الوصف (R4 → Likely بسقف 75 كحد أقصى)
- اسم مدرسة بدون إحداثيات (R3 → Likely، ممنوع Confirmed)
- اسم مدرسة معروفة لكن البلاغ خارج radiusM (R3 → Likely، ممنوع Confirmed)
- بلاغ بدون أي دليل (R5 → Unknown)
- مستشفى من map-locations.json (R1)
- مركز إسعاف: لا يُصنَّف جهة خارجية
- تضارب الاسم والإحداثيات (يفوز الإحداثي داخل radiusM أو Likely — لا تخمين)
- غياب lat/lng (المسار النصي فقط، بسقوفه)
كل نتيجة يجب أن تحمل evidence تشرح سبب القرار والرقم.

اختبار عدم الانحدار — أثبت أن PI-1 لا يغيّر:
Hospital Monitor · Duplicate Detection · report_times · احتساب
المشاركات · الجداول · CAD · الأرشيف الحالي

التقرير النهائي المطلوب (لا تقل فقط "الاختبارات نجحت"):
- Before → After
- الملفات المعدلة والمنشأة، عدد الاختبارات ونتائجها
- نتيجة كل قاعدة R1–R5 على Golden Set مع evidence فعلية
- أمثلة حقيقية من البلاغات المؤرشفة (بدون بيانات حساسة)
- أي حالة لم يستطع المحرك حسمها ولماذا
- إثبات عدم الانحدار

ولا تبدأ PI-2 أو أي واجهة قبل اعتماد نتيجة PI-1.
```
