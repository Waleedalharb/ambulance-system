# Single Source of Truth – Personnel

**الحالة:** معتمدة بشروط المالك الخمسة — شريحة SR-2 (2026-07-23)
**القاعدة الحاكمة:** لا Bridge، لا Fallback، لا قراءة تشغيلية من JSON إطلاقًا.

---

## 1. مسار البيانات (الاتجاه الوحيد المسموح)

```
الجدولة (ملف إكسل/استيراد)
        │  POST /api/schedule/employees  (أو scripts/import-schedule-to-roster.js لمرة واحدة)
        ▼
RosterSyncService          ← المزامنة الوحيدة — ذرية بالكامل (معاملة واحدة)
        │
        ├──▶ employees       (الكادر: هوية بـ employee_code فقط)
        └──▶ shift_roster    (من هو مجدول، لأي فريق، بأي رمز دوام، في أي تاريخ)
        ▼
جميع الأنظمة التشغيلية تقرأ من قاعدة البيانات فقط
```

`data/schedule-employees.json` هو **ملف استيراد (Import) فقط** — يُكتب عند حفظ الجدولة،
ولا يُقرأ أثناء التشغيل من أي مستهلك تشغيلي.

## 2. من يكتب ماذا

| البيانات | الكاتب الوحيد | متى |
|---|---|---|
| `employees` | `RosterSyncService.syncFromSchedule` | عند حفظ/استيراد الجدولة |
| `shift_roster` | `RosterSyncService.syncFromSchedule` | يُعاد بناؤه ذريًا للفترات المشمولة بالاستيراد |
| `operational_events` (غياب/تأخر/تكليف/جاهزية) | `StaffingEventsService` / `CompletionService` | أحداث Timeline — Append-Only، لا تعديل ولا حذف |
| `users` (حسابات الدخول) | إدارة منفصلة تمامًا | **لا** تُنشأ حسابات من الجدولة إطلاقًا |

## 3. من يقرأ ماذا (أثناء التشغيل)

| المستهلك | مصدره الوحيد |
|---|---|
| شاشة التكميل (الكادر + الجاهزية) | `GET /api/staffing/state` ← `shift_roster` + `operational_events` |
| الصفحة الرئيسية (القوى البشرية) | `GET /api/staffing/state` نفسها — بلا أي حساب محلي |
| الاشتقاق والجاهزية | `StaffingEventsService.deriveTeamReadiness` ← `shift_roster` فقط |
| دعم المركبات في الجاهزية | أحداث `operational_events` (domain=center) |
| التحديث اللحظي للواجهات | SSE (`staffing_changed`، `roster_synced`، …) |

## 4. قواعد الهوية والمزامنة

- `employee_code` هو مفتاح الهوية الوحيد (Unique). الاسم قابل للتغيير، الرقم لا يتغير.
- موجود بالرمز ⇒ **UPDATE** (اسم/هاتف/مسمى + إعادة تفعيل).
- غير موجود ⇒ **INSERT** (`is_active = 1`).
- لم يعد في الجدولة ⇒ **لا حذف إطلاقًا** — `is_active = 0` فقط.
- ربط الفرق بالاسم (`rapid_N` ⇒ «سريع N»)، وغير التشغيلية ⇒ `team_id = NULL`.
- الصفوف المشوهة (تاريخ/رمز دوام غير صالح) تُتخطّى وتُحصى — لا تُسقِط المزامنة.
- مدخل فارغ ⇒ مسح `shift_roster` بالكامل + تعطيل الجميع (بلا حذف).

## 5. فصل المسؤوليات (لا خلط)

| الجدول | مسؤوليته | ليس مسؤولًا عن |
|---|---|---|
| `users` | تسجيل الدخول والصلاحيات | الكادر، الجدولة |
| `employees` | هوية الكادر | حسابات الدخول |
| `shift_roster` | من يعمل متى وأين | الأحداث التشغيلية |
| `operational_events` | كل ما يحدث أثناء التشغيل | الجدولة الأساسية |

## 6. حدود ممنوعة (مراجعة دائمة)

- ❌ أي قراءة تشغيلية من `schedule-employees.json` أو أي JSON آخر للقوى البشرية.
- ❌ أي Bridge أو Fallback أو "مصدر احتياطي".
- ❌ أي حساب محلي للقوى البشرية في الواجهة — الخادم يشتق، الواجهة تعرض فقط.
- ❌ حذف موظفين أو تعديل أحداث تاريخية.
- ✅ عزل الاختبارات عبر `DB_PATH` + `SCHEDULE_FILE` — ملفات التشغيل المشتركة لا تُمس.

*هذه الوثيقة هي المرجع النهائي. أي تغيير مستقبلي في مصادر القوى البشرية يجب أن يُحدّث هذه الوثيقة أولًا.*
