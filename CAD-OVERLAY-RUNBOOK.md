# دليل تشغيل CAD Overlay — «＋ تسجيل للجنوب»

**الحالة:** مكوّن معتمد من المشروع (ليس PoC) — اعتماد المالك 2026-08-20.
**الغرض:** تسجيل بلاغات CAD في منصة الجنوب بضغطة واحدة، مع رقم البلاغ ووقت إنشائه
الفعلي والموقع (الحي/الشارع/العنوان/الإحداثيات الأصلية) والفرق وأزمنة المراحل —
لتغذية توزيع البلاغات والخريطة التشغيلية الذكية ومؤشر زمن الاستجابة.

**البنية الأمنية (قرار المالك 2026-08-20 — شرط الاعتماد الرسمي):**
صفحة CAD لا يدخلها أي سر إطلاقًا. المراقبة والالتقاط في سياق الصفحة (MAIN world)
بلا مفتاح، والإرسال يمر: `صفحة CAD (postMessage) ← bridge.js (معزول، بلا سر)
← background.js (سياق الإضافة — المكان الوحيد للمفتاح) ← المنصة`.

---

## 1. المكوّنات

| الملف | الدور | يحمل سرًا؟ |
|---|---|---|
| `scripts/cad-overlay.js` | **المصدر الوحيد للحقيقة** — مراقبة + التقاط + واجهة (معتمد 74/74) | لا |
| `extension/manifest.json` | تعريف الإضافة MV3 — بلا صلاحيات، host مقصور على localhost:3002 | لا |
| `extension/cad-overlay-content.js` | نسخة مولّدة حرفية من المصدر — تعمل في MAIN world عند فتح CAD | لا |
| `extension/bridge.js` | جسر معزول يرحّل postMessage ↔ خلفية الإضافة بتحقق صارم | لا |
| `extension/background.js` | خلفية الإضافة — **المكان الوحيد للمفتاح**، خارج سياق الصفحة تمامًا | **نعم (مستثنى من Git)** |
| `scripts/build-extension.js` | يولّد الملفين أعلاه من المصدر المعتمد + المفتاح النشط | — |
| `scripts/inject-cad-overlay.js` | الحاقن اليدوي (بديل تطويري عبر WebBridge) — يحقن بلا مفتاح | لا |
| `data/integration-keys.json` | مفتاح التكامل المقصور (خارج أي Commit) | نعم |
| `POST /api/cad-reports` | مسار الاستقبال في الخادم (خادم المنصة نفسه — لا خادم موازٍ) | — |

## 2. المتطلبات

1. خادم المنصة المحلي يعمل: `node server.js` (المنفذ 3002).
2. مناوبة نشطة في المنصة (بدونها يُرفض التسجيل — نفس قاعدة الإدخال اليدوي).
3. تبويب CAD (`cad.alsahab.sa`) مفتوح في متصفح المالك.
4. جسر المتصفح WebBridge يعمل (127.0.0.1:10086).

## 3. التشغيل

### 3.أ الطريقة الأساسية — التثبيت من منصة الجنوب نفسها (أجهزة العمل)

التوزيع من المنصة المنشورة، لا من مجلد المشروع (قرار المالك 2026-08-21):

1. الموظف يسجّل دخوله للمنصة (المنشورة أو المحلية).
2. من القائمة الجانبية ← «الإعدادات والأدوات» ← **「تثبيت أداة CAD」**
   (أو يفتح `/cad-overlay` مباشرة).
3. الصفحة تفحص تلقائيًا هل الأداة مثبتة على هذا الجهاز وتعرض ✅/⚠️ فورًا
   (عبر علامة `platform-marker.js` التي تزرعها الإضافة على صفحات المنصة فقط).
4. يضغط **「🚑 تثبيت CAD Overlay — تنزيل الحزمة」** ← يتنزّل
   `south-cad-overlay-extension.zip` مولّدًا لحظتها من الخادم.
5. يفكّ الضغط في مجلد ثابت (لا يُحذف لاحقًا)، ثم `chrome://extensions`
   (أو `edge://extensions`) ← تفعيل **وضع المطوّر** ← **Load unpacked**
   ← يختار المجلد المفكوك.
6. يفتح CAD — تظهر لوحة 「🚑 منصة الجنوب」 تلقائيًا. **مرة واحدة فقط**؛
   بعدها تعمل الأداة تلقائيًا كل ما فُتح CAD، وتعطيلها من صفحة الإضافات
   يزيلها كليًا.

خصائص الحزمة الموزَّعة:

- تُولَّد من `scripts/cad-overlay.js` حرفيًا (Source of Truth) عبر
  `scripts/extension-builder.js` — نفس البناء المختبَر حيًا بلا أي فرق.
- **عنوان المنصة داخل الحزمة = عنوان المنصة التي نُزّلت منها تلقائيًا**
  (بعد النشر: `https://emsoperations.online`؛ محليًا: عنوان الجهاز ومنفذه) —
  ويُثبَّت صراحة بمتغير البيئة `PLATFORM_PUBLIC_BASE` على الخادم عند الحاجة.
- التنزيل محمي بجلسة المنصة (`GET /api/cad-overlay/package` يتطلب JWT) لأن
  الحزمة تحمل مفتاح التكامل — في `background.js` فقط (الفصل الأمني §5).
- **لا تثبيت صامت:** Chrome لا يسمح لصفحة ويب بتثبيت إضافة تلقائيًا؛ هذه
  أقرب تجربة ممكنة ضمن قيود المتصفح: تنزيل ← فك ضغط ← Load unpacked مرة واحدة.
- **شرط أول نشر:** يلزم مفتاح تكامل نشط في `data/integration-keys.json`
  على قرص Render (`/data`) وإلا رُفض توليد الحزمة (§7).

### 3.ب توليد يدوي للمطوّر (بديل تطويري)

```bash
# 1) ولّد سكربت المحتوى من الـOverlay المعتمد (مرة واحدة، أو بعد أي تحديث معتمد):
node scripts/build-extension.js
# 2) chrome://extensions ← وضع المطوّر ← Load unpacked ← مجلد extension/ من المشروع

# حزمة مستقلة قابلة للنسخ (نفس ما تنزّله صفحة المنصة):
node scripts/build-extension.js --dist
# ← dist/south-cad-overlay-extension/ مع «اقرأني-التثبيت.txt»
#   منصة على جهاز آخر؟ set PLATFORM_BASE=http://<الخادم>:3002 قبل التوليد.
```

> الإضافة بلا أي صلاحيات (`permissions: []`) — سكربت محتوى مقصور على نطاق
> CAD فقط (`world: MAIN` + `document_idle`) + جسر معزول + علامة منصة،
> و`host_permissions` مقصورة على عنوان المنصة. الاتصال بالمنصة يتم من خلفية
> الإضافة (خارج سياق الصفحة) فلا يخضع لـCORS الصفحة أصلًا.
> ملاحظة: `extension/background.js` ملف مولّد يحوي المفتاح — مستثنى من Git،
> ولا يُعدَّل يدويًا؛ أي تحديث يكون على `scripts/cad-overlay.js` ثم
> `node scripts/build-extension.js` ثم «إعادة تحميل» للإضافة من صفحة الإضافات.

**للتوزيع المُدار لاحقًا (اختياري):** عند التعميم على أجهزة القطاع تُثبَّت مركزيًا
بسياسة `ExtensionInstallForcelist` بعد رفعها لسجل داخلي/Chrome Web Store غير مدرَج —
لا حاجة لها الآن.

### 3.ج البديل — الحقن عبر WebBridge (يبقى معتمدًا كما هو)

```bash
# 1) من محادثة Kimi: استعار تبويب CAD عبر WebBridge باسم الجلسة cad-observe
#    (find_tab على cad.alsahab.sa مع active:true)
# 2) ثم نفّذ:
node scripts/inject-cad-overlay.js
```

نجاح الحقن يطبع: `✅ حُقن CAD Overlay في تبويب CAD`.
إعادة الحقن **آمنة دائمًا** — الـOverlay يزيل نسخته السابقة ويثبّت الجديدة،
وتحدّث المعالجات دون كسر المراقبة القائمة.

## 4. الاستخدام اليومي (الموظف)

1. يفتح البلاغ في CAD كالمعتاد — لا شيء يتغير في عمله.
2. تظهر لوحة 「منصة الجنوب」 العائمة — **قابلة للسحب** من مقبضها إلى أي مكان
   مناسب، وقابلة للطيّ بزر ✕ (تبقى شارة 「🚑 الجنوب」 لإعادة فتحها)، ويُحفظ
   آخر موضع وحالة طيّ عبر تحديث الصفحة، ولا تخرج خارج حدود الشاشة أبدًا.
3. **ضغطة واحدة** على 「＋ تسجيل للجنوب」← يُسجَّل البلاغ في المنصة فورًا بما هو
   متاح لحظتها: الرقم + النوع + الفرقة + الأزمنة + الموقع إن وُجد.
4. **ثم تبدأ المراقبة التلقائية فورًا (قرار المالك 2026-08-21 — لا ضغطة ثانية
   أبدًا):** أي بيانات تصل لاحقًا من CAD — الإحداثيات، العنوان، المراحل الجديدة
   («البحث»/«العلاج»…)، تبديل/سحب الفرقة، وقت الإنشاء المتأخر — تُرسَل تحديثًا
   لنفس رقم البلاغ تلقائيًا خلال ثوانٍ، والخادم يُثري السجل القائم ويبث للخريطة
   لحظيًا. عند اكتمال الموقع تظهر للموظف رسالة 「🌐 اكتمل موقع البلاغ تلقائيًا」.
5. الضغط مرة ثانية على نفس البلاغ **لا يكرره** (المنع بنيوي: رقم البلاغ هو
   المفتاح عبر كل المناوبات — بلاغ يمتد عبر التسليم يُلحق بسجله ولا يتكرر).
6. **إنهاء البلاغ — مصدر الحقيقة هو قائمة بلاغات CAD (قرار المالك 2026-08-22
   بعد التجربة الحية، ومصدره موثق بالرصد):** CAD نفسه يستطلع كل ~10 ثوانٍ
   `dispatch-manager/api/cfs/v2/{operation-controller|response-coordinator}/incident-list`
   ويبني من `data.items[].eventId` قائمتي «البلاغات المرحلة» و«تنسيق الاستجابة»
   (نفس المصدر والبنية لكلتيهما — تحققنا بالرصد الحي). الـOverlay يقرأ هذه
   الاستجابات سلبيًا كما يحمّلها CAD: **الحضور في أي قائمة ينشّط البلاغ ويصفّر
   عدّاد غيابه**، والغياب يُحسب فقط من لقطة مكتملة الصفحات (`last=true`؛
   قائمة بانتظار الترحيل `awaiting-list` تُثبت الوجود فقط ولا تُثبت الغياب).
   **لا نقرأ كلمات الحالة من تفاصيل البلاغ إطلاقًا** (أثبتت التجربة أنها غير
   موثوقة في CAD لدينا وأُزيلت تلك الطبقة). عند **الغياب المؤكد** — ٣ لقطات
   مكتملة متتالية + ٦٠ ثانية بلا ظهور في أي قائمة (٦ دورات استطلاع كاملة)، أو
   15 دقيقة لبلاغ لم يظهر في أي قائمة منذ تسجيله — يرسل الـOverlay تحديثًا
   أخيرًا بحالة `cancelled` لنفس رقم البلاغ ثم **يوقف مراقبته** ويُعلم الموظف
   برسالة 「⛔ اختفى من قائمة بلاغات CAD — أُخرج من التشغيل النشط」. البلاغ
   المنتهي يخرج فورًا من الخريطة والتنبيهات والمؤشرات ويبقى محفوظًا تاريخيًا
   بكل بياناته (رقم/موقع/فرق/أزمنة) لتحليلات الضغط والذروة والأداء لاحقًا.
   **الخريطة = ما يحدث الآن، قاعدة البيانات = ما حدث فعليًا.**
7. تستمر مراقبة البلاغ النشط 6 ساعات من آخر نشاط ثم تتوقف تلقائيًا
   (احتياط صامت فقط إن لم تُحمَّل القائمة أبدًا — ليست بديلًا عن حكم القائمة)،
   وتُحفظ قائمة المراقبة وعدّادات الغياب في تخزين التبويب فقط.

**معيار القبول المعتمد:** فتح بلاغ CAD ← ضغطة تسجيل واحدة ← انتظار تحميل CAD
لبيانات الموقع ← ظهور الموقع على خريطة المنصة **تلقائيًا دون أي ضغطة إضافية**.

## 5. قواعد صارمة (لا تُكسر)

- **قراءة فقط من CAD:** لا ضغط أي إجراء فيه ولا تعديل عليه ولا كتابة فيه.
- **المراقبة سلبية:** يلتقط فقط الاستجابات التي يحمّلها تطبيق CAD بنفسه.
  **استثناء معتمد نصيًا (المالك 2026-08-20):** عند غياب إحداثيات بلاغ مسجَّل
  ومعرفة `location_id` الخاص به، يجوز قراءة مورد الموقع
  `location-manager/api/locations/{id}` بنفس جلسة الصفحة — قراءة `latitude/longitude`
  فقط، بحد أقصى 6 محاولات بفاصل 45 ثانية لكل بلاغ، ثم يُسجَّل بلا إحداثيات بصدق.
  لا Geocoding خارجي ولا تخمين إحداثيات إطلاقًا.
- **ممنوع قطعًا:** قراءة/نقل/طباعة أي Session Token من أي تبويب.
- **المصادقة:** مفتاح تكامل مقصور على `POST /api/cad-reports` فقط — لا دخول ولا
  صلاحية أخرى، ويُلغى عند انتهاء الحاجة (انظر §7). **لا يدخل المفتاح سياق صفحة
  CAD من أي مسار** — لا في `cad-overlay.js` ولا في `cad-overlay-content.js`
  ولا في `bridge.js`؛ مكانه الوحيد `background.js` (سياق الإضافة المعزول).
- **لا إحداثيات مختلقة:** فشل الالتقاط ← `null` بصدق ويظهر البلاغ في
  «بلاغات بلا موقع دقيق» على الخريطة. لا Geocoding خارجي.
- **لا GPS للمسعفين:** تُقرأ إحداثيات موقع البلاغ فقط.
- **قاعدة عدالة الفرق:** فرقة بلا «تحرك» لا تُحتسب في عدّادها؛ الفرقة التي
  تحركت ووصلت تُحتسب — حتى مع التبديل.
- **الحالة النهائية تُخرج البلاغ من التشغيل (قرار المالك 2026-08-22):**
  `active` فقط يدخل المؤشرات التشغيلية والتنبيهات والخريطة النشطة؛ أي حالة
  نهائية (`closed`/`cancelled`) تُخرجه فورًا وتحتفظ به تاريخيًا كاملًا.
  **مصدر الحكم الوحيد هو قائمة بلاغات CAD الموثقة** (`dispatch-manager …
  /incident-list` التي يستطلعها CAD نفسه — غياب مؤكد = `cancelled`) —
  لا كلمات حالة من تفاصيل البلاغ، ولا مؤقت انتهاء كبديل؛ حد الـ6 ساعات
  احتياط صامت فقط. والحالة النهائية لاصقة سيرفريًا: لا ترتد إلى `active`
  إلا بإشارة `active` صريحة.
- **الفصل بين العدّائين:** إجمالي البلاغات = أرقام البلاغات الفريدة؛ توزيع
  الفرق = المشاركات الفعلية. تعدد الفرق على بلاغ واحد لا يكرره في الإجمالي.

## 6. استكشاف الأخطاء

| العرض | السبب المرجح | الحل |
|---|---|---|
| الزر لا يظهر في CAD | الإضافة غير مثبتة/معطلة، أو (للحقن اليدوي) التبويب أُغلق بعد الحقن | تأكد أن الإضافة مفعّلة في `chrome://extensions` — أو أعد `node scripts/inject-cad-overlay.js` |
| «تعذر الوصول لمنصة الجنوب» | الخادم متوقف | `node server.js` |
| رفض 401 عند التسجيل | المفتاح أُلغي أو تغيّر | أنشئ/فعّل مفتاحًا في `integration-keys.json` ثم أعد الحقن |
| «لا توجد مناوبة نشطة» | لا مناوبة مفتوحة | ابدأ المناوبة من المنصة |
| بلاغ بلا نقطة على الخريطة | إحداثياته لم تلتقط بعد | المراقبة التلقائية تستكملها خلال ثوانٍ-دقائق عند وصولها من CAD (بلا ضغطة ثانية)؛ إن تعذرت يبقى مدرجًا بصدق في «بلا موقع دقيق» |
| اللون/المرحلة لا تتحدث | تحديث الصفحة في المتصفح | لا يجب أن يحدث (SSE)؛ إن تكرر راجع Console |
| «سجّل دخولك للمنصة أولًا» عند تنزيل الحزمة | `/api/cad-overlay/package` محمي بجلسة المنصة | سجّل الدخول للمنصة ثم أعد فتح `/cad-overlay` ونزّل |
| «تعذّر توليد الحزمة» / رفض من الخادم | لا مفتاح تكامل نشط على هذا الخادم | أنشئ مفتاحًا نشطًا في `data/integration-keys.json` (على قرص Render `/data` بعد النشر) |

## 7. إلغاء مفتاح التكامل (عند انتهاء التجربة أو تسريبه)

عدّل `data/integration-keys.json` واجعل `"active": false` للمفتاح — يتوقف قبوله
فورًا دون إعادة تشغيل. لا تحذف السجل (يبقى أثرًا للمراجعة).

## 8. التحقق

```bash
node scripts/cad-report-integration-test.js   # 88/88 — قواعد المحرك + هوية الإحداثيات + ازدواج التسليم ⑰ + المسحوبة ⑱
node scripts/alert-rules-test.js              # 32/32 — لا Timer/Alert لفرقة لم تتحرك أو سُحبت
node scripts/cad-overlay-package-test.js      # 21/21 — حزمة التوزيع: هوية المصدر + الفصل الأمني + عنوان المنصة + وظيفة المفتاح
node scripts/incident-lifecycle-test.js       # 29/29 — دورة الحياة: فريد/مشاركات + النهائي يخرج فورًا + الإثراء بلا ازدواج
node scripts/run-regression-isolated.js       # 193/202 = خط الأساس
```

**دورة قبول تثبيت الإضافة (§15 — جهاز العمل):**
`تثبيت Load unpacked ← إغلاق المتصفح كليًا ← فتحه ← فتح CAD ← الزر ظاهر تلقائيًا
← تسجيل بلاغ ← وصوله للمنصة`. الإضافة unpacked تبقى مثبتة عبر إعادة تشغيل
المتصفح بنيويًا، وتعطيلها من `chrome://extensions` يزيل كل أثر لها فورًا.

**ممنوعات دائمة على هذا المكوّن:** لا Commit / Push / Deploy إلا بقرار صريح من
المالك، ولا تعديل على CAD نفسه، ولا منصة بديلة.
