# تشخيص نظام النماذج + Incident Lookup Integration

**التاريخ:** 2026-09-01 · **النطاق:** تشخيص وتحليل فقط بأمر المالك — **صفر تعديل كود / DB / API / تصميم · لا Commit / Push / Deploy**
**المنهج:** كل حقل قورن بمصادر البيانات الفعلية في قاعدة `data/ambulance.db` (1082 بلاغ توزيع · 551 مشاركة فرقة · 145 سجل بلاغ CAD) والكود الحالي. ما لم يُحسم من الكود/البيانات كُتب صراحة «غير محسوم».

---

## 0) حقائق البنية المثبتة (أساس كل ما يلي)

| المكوّن | الواقع الحالي (مثبت) |
|---|---|
| **تخزين النماذج الستة** | كلها تحفظ عبر `FormsService.submit()` في جدول واحد `shift_forms` (`form_name` = النوع: `incident / e_case / escalation / daily_report / air_ambulance / senior_shift`، و`form_data` = JSON كامل). الجداول القديمة (`e_reports`, `incident_reports`, `escalation_reports`, `air_ambulance_records`) **فارغة (0 صف)** — ميراث ما قبل التوحيد. |
| **مصدر بيانات البلاغ (CAD)** | جدول `incident_registry` (145 سجل): `number, code (ترميز MDS مثل 32B03), type, cad_created_at, address/region/district/street/city, lat/lng, status, caller_number, description`. |
| **مشاركات الفرق والأزمنة** | جدول `report_times` (551 مشاركة): `incident_number + unit + phases (JSON) + resp_arrival_min + resp_mubashara_min`. |
| **الصلاحية** | كل نقاط حفظ النماذج محمية بـ`authorizePerm('ops.forms')` — صلاحية واحدة قائمة تصلح للـLookup أيضًا. |
| **ربط النموذج بالبلاغ اليوم** | **غير موجود بنيويًا.** `form_data` يحمل `reportNumber` كنص حر يدخله الموظف بلا تحقق؛ العينتان المحفوظتان (`لابيس`, `1234`) لا تطابقان أي بلاغ CAD حقيقي. |

---

## 1) مصفوفة النماذج الستة

| النموذج | حقول CAD | حقول مشتقة | حقول يدوية | نسبة الأتمتة المقترحة | يرتبط ببلاغ؟ |
|---|---:|---:|---:|---:|---|
| بلاغ حادث (13 حقلًا) | 4 (رقم/وقت/موقع/فرقة) + وصف كتعبئة مسبقة | 0 | 8 | ~38% | ✅ نعم |
| حالات E (10 حقول) | 3 (رقم/وقت/موقع) + الفرقة من المشاركات | **1 (زمن الاستجابة — موجود مركزيًا!)** | 5 | ~50% | ✅ نعم — أولوية |
| التصعيد (8 حقول) | 3 (رقم/وقت/موقع) + تفاصيل كتعبئة مسبقة | 0 | 4 | ~44% | ✅ نعم |
| الإسعاف الجوي (10 حقول) | 0–1 (رقم البلاغ **غير محسوم**) | 0 | 9 | ~5% | ⚠️ غير محسوم — يحتاج تحقق |
| التقرير اليومي (8 حقول) | 0 | 2–3 (عدد البلاغات الجوية، وربما الفرق المستجيبة) | 5 | ~31% | ❌ تقرير مجمّع |
| كبار المسعفين (16 حقلًا) | 0 | 3–5 (مناطق التمركز الفعلية من `positioning_events`، أسماء التسليم من `employees.job_title`، التواريخ) | 11 | ~25% | ❌ تسليم مناوبة |

---

## 2) مصفوفة الحقول (تفصيل كل نموذج)

### 2.1 نموذج بلاغ حادث (`form-incident.html`)

| الحقل | المصدر | تلقائي؟ | Read-only؟ | مشتق؟ | إلزامي؟ | ملاحظات |
|---|---|---|---|---|---|---|
| `incReportNumber` رقم البلاغ | CAD `incident_registry.number` | بحث | نعم بعد الربط | لا | نعم | مفتاح Lookup |
| `incDateTime` التاريخ والوقت | CAD `cad_created_at` | نعم | نعم | لا | نعم | صيغة CAD: `20/08/2026 12:09:20 PM` |
| `incType` نوع الحادث | يدوي (قائمة أدق من `type` الخام) | لا | لا | لا | نعم | ترجمة `code` (MDS) للنوع **غير محسومة — يحتاج تحقق** |
| `incLocation` الموقع | CAD `address/district/street` | نعم | نعم | لا | نعم | |
| `incUnit` الفرقة | CAD `report_times.unit` (مشاركات البلاغ) | نعم (قائمة فعلية) | نعم | لا | نعم | بدل قائمة «سريع 1–4 / جنوب 1–19» الثابتة المكررة في 3 نماذج |
| `incCenter` المركز | يدوي | لا | لا | لا | حسب النموذج | لا يوجد في بيانات CAD الواصلة؛ جدول `centers` قائم ويمكن تغذية القائمة منه |
| `incPatientName` اسم المريض | يدوي | لا | لا | لا | لا | لا يصل من CAD حاليًا |
| `incAge` / `incGender` | يدوي | لا | لا | لا | لا | |
| `incDescription` وصف الحالة | CAD `description` | تعبئة مسبقة | لا (قابل للتحرير) | لا | لا | CAD مصدر البداية، الموظف يستكمل |
| `incActions` الإجراءات | يدوي | لا | لا | لا | لا | |

### 2.2 نموذج حالات E (`form-e.html`) — أولوية

| الحقل | المصدر | تلقائي؟ | Read-only؟ | مشتق؟ | إلزامي؟ | ملاحظات |
|---|---|---|---|---|---|---|
| `eReportNumber` | CAD | بحث | نعم بعد الربط | لا | نعم | مفتاح Lookup |
| `eDateTime` | CAD `cad_created_at` | نعم | نعم | لا | نعم | |
| `eLocation` | CAD `address/district` | نعم | نعم | لا | نعم | |
| `eUnit` الفرقة المستجيبة | CAD مشاركات البلاغ | نعم | نعم | لا | نعم | عدة فرق محتملة ← تُعرض كلها أو تُختار من الفعلية |
| **`eResponseTime` زمن الاستجابة** | **مشتق مركزي: `resp_arrival_min`** | **نعم** | **نعم** | **نعم** | نعم | **موجود أصلًا ومحسوب لكل مشاركة — يُمنع بقاؤه حقلًا يدويًا** |
| `eAge` / `eGender` | يدوي | لا | لا | لا | لا | |
| `eHospital` المستشفى المستلم | يدوي | لا | لا | لا | لا | جدول `hospitals` قائم ويمكن تغذية القائمة منه |
| `eOutcome` الحالة النهائية | يدوي (3 خيارات) | لا | لا | لا | نعم | |
| `eNotes` | يدوي | لا | لا | لا | لا | |

### 2.3 نموذج التصعيد (`form-escalation.html`)

| الحقل | المصدر | تلقائي؟ | Read-only؟ | ملاحظات |
|---|---|---|---|---|
| `escReportNumber` / `escDateTime` / `escLocation` | CAD | بحث + تعبئة | نعم | مثل السابق |
| `escEventType` نوع الحدث | يدوي (14 خيارًا) | لا | لا | لا نظير له في CAD الواصل |
| `escInjuries` / `escDeaths` | يدوي | لا | لا | |
| `esc-agency` الجهات المشاركة | يدوي (8 جهات) | لا | لا | |
| `escDetails` التفاصيل | CAD `description` تعبئة مسبقة | جزئي | لا | |

### 2.4 نموذج الإسعاف الجوي (`form-air.html`)

| الحقل | المصدر | ملاحظات |
|---|---|---|
| `airReportNumber` | **غير محسوم — يحتاج تحقق** | الـplaceholder يقترح صيغة `2025-AIR-001` لا تشبه أرقام CAD (`1300918`). طلب الإخلاء الجوي قد لا يكون بلاغ CAD أصلًا — **يحتاج قرارًا تشغيليًا: هل طلبات الجوي ترد كبلاغات CAD؟** |
| `airDateTime` ووقت الطلب + بقية الحقول | يدوي | طبيعة النموذج (طلب نقل) لا نظير لها في بيانات CAD الواصلة |
| `airUnit` الفرقة | قائمة ثابتة مكررة | يمكن تغذيتها من `/api/cad-reports/south-teams` (قائمة فرق الجنوب الفعلية — موجودة) |

### 2.5 نموذج التقرير اليومي (`form-daily.html`)

| الحقل | المصدر | ملاحظات |
|---|---|---|
| `dailyReportNumber` | يدوي | يمكن توليده تلقائيًا (`DAILY-YYYY-MM-DD`) |
| `dailyDate` | تلقائي (اليوم) | |
| `dailyAir` عدد بلاغات الجوي | مشتق = عدد `air_ambulance` لليوم | قابل للأتمتة مباشرة |
| `dailyResponseTeams` الفرق المستجيبة | مشتق من بيانات المناوبة | **غير محسوم** بدقة التعريف التشغيلي (من التكميل؟ من الفرق النشطة؟) — يحتاج تحقق |
| البلاغات الحدودية / المسارات / الملخص | يدوي | |

### 2.6 نموذج كبار المسعفين (`form-senior.html`)

| الحقل | المصدر | ملاحظات |
|---|---|---|
| المركبات (عاملة/متعطلة/احتياط) وفرق الاوفرلاب | يدوي | جداول `assets/vehicles` قائمة لكن **غير محسوم** هل تعكس الجاهزية اليومية — يحتاج تحقق |
| مناطق تمركز الاوفرلاب | يدوي (5 مناطق ثابتة) | `positioning_events` تسجل التمركزات الفعلية — يمكن اقتراح المناطق المستخدمة فعليًا في المناوبة |
| أسماء التسليم الثلاثة | يدوي | قابلة للتعبئة من `employees.job_title` بالمطابقة التامة (قرار التصنيف الإداري المعتمد سابقًا: «كبير مسعفين»/«مساعد كبير المسعفين») — «قائد المنطقة» غير محسوم |
| التواريخ | تلقائي (اليوم) | |

---

## 3) Incident Lookup Architecture (مقترح — بانتظار الاعتماد)

### 3.1 ما يوجد وما ينقص

| المكوّن | الحالة |
|---|---|
| `GET /api/analytics/incident-detail?shiftId&number` | ✅ موجود — لكنه **ثقيل ومخصص للتحليلات**: يتطلب `shiftId` ونطاقًا زمنيًا ويبني نوافذ تمركز. لا يصلح بحثًا سريعًا أثناء تعبئة نموذج. |
| `getIncidentByNumber(shiftId, number)` (طبقة التخزين) | ✅ موجود — لكنه مقيّد بالمناوبة |
| نقطة بحث خفيفة بالرقم فقط | ❌ **غير موجودة** — هذه هي الفجوة الوحيدة |
| مكوّن واجهة موحد | ❌ غير موجود — كل نموذج حقل نصي حر مستقل |

### 3.2 المقترح (بلا توسيع نطاق)

```
GET /api/incidents/lookup?number=1314119        ← نقطة واحدة خفيفة جديدة
    authenticate + authorizePerm('ops.forms')    ← نفس صلاحية النماذج القائمة
    البحث: incident_registry بالرقم (الأحدث أولًا عبر المناوبات)
         + مشاركات report_times (الفرق + phases + الأزمنة المحسوبة)
    يرد:
    {
      found: true,
      incident: { number, code, type, cadCreatedAt, address, district, street, lat, lng, status, description },
      units: [{ unit, phases, respArrivalMin, respMubasharaMin, ... }],
      timeCompleteness: { hasCadCreated: true, hasArrival: true, ... },
      ambiguity: null | { shifts: [..] }        ← إن ظهر الرقم في أكثر من مناوبة
    }
    404: { found: false }
```

**مكوّن الواجهة `IncidentLookup`** (واحد يُحقن في النماذج الثلاثة المرتبطة): حقل بحث + زر + حالات (`بحث… / ✓ موجود / ✗ غير موجود / ⚠️ موجود لكن بياناته الزمنية ناقصة`) + بطاقة عرض read-only + تمرير البيانات للنموذج. يمنع الإرسال ببلاغ غير مُتحقق منه، ويمنع تعديل حقول CAD، ويعرض آخر بيانات متوفرة عند النقص.

**مبدأ ثابت:** لا منطق حساب جديد — زمن الاستجابة يُقرأ من `resp_arrival_min` المحسوب مركزيًا (§4).

### 3.3 تعدد النتائج

رقم البلاغ يتكرر عبر المناوبات (135 رقمًا فريدًا من 145 سجلًا — مثل `1300370` في مناوبتين). القاعدة المقترحة: **الأحدث افتراضيًا + مؤشر «هذا الرقم ظهر أيضًا في مناوبة تاريخ X» مع إمكانية التبديل** — لا إخفاء ولا تخمين صامت.

---

## 4) Time Engine — تدقيق الحقول الزمنية

### 4.1 المصادر والصيغ (مثبتة من البيانات)

| الوقت | المصدر | الصيغة | الاكتمال الفعلي |
|---|---|---|---|
| إنشاء البلاغ في CAD | `incident_registry.cad_created_at` | تاريخ كامل + 12 ساعة: `20/08/2026 12:09:20 PM` | **111/145 (77%)** |
| قبول الفرقة | `phases.قبول` | وقت فقط بلا تاريخ: `10:56:12 AM` | 180 مشاركة |
| التحرك | `phases.التحرك` | نفس الصيغة | 180 |
| **الوصول للموقع** | `phases.البحث` | نفس الصيغة | 154 |
| **المباشرة** | `phases.العلاج` | نفس الصيغة | 106 |
| النقل | `phases.النقل` | نفس الصيغة | 47 |
| الجاهزية / بدء / انتهاء التسليم | `phases.الجاهزية` إلخ | نفس الصيغة | 31 / 30 / 22 |
| استقبال المنصة | `incident_registry.created_at` | ISO 8601 UTC | 145/145 |

### 4.2 الحساب المركزي القائم (لا يُنشأ بديل عنه)

`services/report-service.js` (سطور 165–224) — تعريف المالك المعتمد 2026-08-20:
- **زمن الاستجابة = إنشاء البلاغ في CAD ← الوصول (`البحث`)** — `قبول` و`التحرك` أزمنة وسيطة تُحفظ ولا تكون نقطة بداية.
- **زمن المباشرة = `البحث` ← `العلاج`** (توحيد 2026-08-25).
- عبور منتصف الليل يُعالج بـ`+1440` دقيقة.
- الناتج يُحفظ مع كل مشاركة: `resp_arrival_min` / `resp_mubashara_min` — **محسوب حاليًا لـ137/551 مشاركة فقط** لأن الباقي ينقصه `cad_created_at` أو `البحث`.
- **لا يوجد حساب موازٍ آخر في المنصة.** المكان الوحيد الذي يطلب زمن استجابة يدويًا هو نموذج E (`eResponseTime`) — وهذا بالضبط ما يجب أن يتحول لقراءة من الحساب المركزي.

### 4.3 المخاطر المثبتة

1. **صيغ phases بلا تاريخ** — عبور منتصف الليل يُعالج استدلاليًا؛ بلاغ قبل منتصف الليل ووصول بعده يُحسب صحيحًا بالقاعدة، لكن **بلاغًا يتجاوز 24 ساعة أو يتكرر رقمه يومين متتاليين غير مميَّز زمنيًا داخل phases**.
2. **نقص `cad_created_at` لـ34 بلاغًا (23%)** ← زمن الاستجابة يبقى `null` بصدق (لا صفر وهمي) — سلوك صحيح قائم يجب أن يرثه النموذج.
3. صيغتان للوقت في النظام (CAD الخام 12 ساعة + ISO UTC) — أي Lookup يجب أن يعرض بصيغة الرياض الجدارية الموحدة (`time-riyadh.js` قائم).

---

## 5) نموذج E — التفصيل الخاص (أولوية)

| السؤال | الجواب المثبت |
|---|---|
| مصدر كل وقت | إنشاء البلاغ: `cad_created_at` · الوصول: `phases.البحث` · المباشرة: `phases.العلاج` — لكل فرقة مشاركة |
| المعادلة الحالية | `_responseFor` (report-service.js:205) — مركزية ومعتمدة |
| هل يوجد حساب آخر؟ | **لا.** النموذج يطلبه يدويًا فقط |
| إعادة استخدام؟ | نعم — قراءة `resp_arrival_min` مباشرة، **ممنوع حساب موازٍ** |
| وقت الوصول غير موجود؟ | `resp_arrival_min = null` ← النموذج يعرض «⚠️ زمن الاستجابة غير متاح — بيانات CAD ناقصة» ولا صفر ولا قيمة وهمية |
| `cad_created_at` غير موجود؟ | نفس المعاملة + توثيق النقص |
| عدة فرق؟ | تُعرض أزمنة كل فرقة؛ الأسرع (`MIN`) هو زمن البلاغ التحليلي — لكن النموذج الطبي يرتبط بفرقة المريض الفعلية، فيُختار زمنها (قرار UX يُعتمد) |

**الحد الأدنى لصلاحية بلاغ لنموذج E:** رقم موجود ✓ + `cad_created_at` ✓ + `البحث` ✓ — وإلا `⚠️ البلاغ موجود، لكن بياناته الزمنية غير مكتملة`.

---

## 6) البيانات الناقصة والمشاكل الحالية

| # | المشكلة | الأثر |
|---|---|---|
| 1 | `form_data` لا يحمل مفتاح بلاغ مُتحققًا منه — `reportNumber` نص حر | لا يمكن بناء «النماذج المرتبطة بالبلاغ X» بموثوقية اليوم |
| 2 | `eResponseTime` يدوي رغم وجود الحساب المركزي | ازدواجية حقيقة + قابلية خطأ بشري |
| 3 | 23% من البلاغات بلا `cad_created_at` | زمن استجابة غير قابل للحساب لها — يُعرض كنقص صادق |
| 4 | قوائم الفرق ثابتة مكررة في 3 نماذج (`سريع 1–4 / جنوب 1–19`) | تفترق عن الواقع؛ يوجد بديل حي `/api/cad-reports/south-teams` |
| 5 | رقم بلاغ الإسعاف الجوي غير محسوم الصلة بـCAD | يحتاج قرارًا تشغيليًا قبل أي ربط |
| 6 | تكرار رقم البلاغ عبر المناوبات | يتطلب قاعدة تعدد نتائج (§3.3) |
| 7 | phases بلا مكوّن تاريخ | قيد معروف، معالَج استدلاليًا حتى 24 ساعة |
| 8 | إحداثيات البلاغ متوفرة لـ50/145 فقط | لا يؤثر على Lookup النصي؛ يؤثر على أي ميزة خرائطية مستقبلية |

---

## 7) UX Recommendations (تقييم فقط — بلا تنفيذ)

**عام:**
- إدخال رقم البلاغ اليوم حر بلا تحقق ولا تأكيد بصري — أعلى أولوية UX هي بطاقة «✓ تم العثور على البلاغ» الموحدة.
- توحيد تسمية «رقم البلاغ» (اليوم: «رقم البلاغ» / «رقم التقرير» / placeholders بصيغ مختلفة `2025-AIR-001` / `E-2025-XXXX` لا تشبه أرقام CAD الحقيقية — **تضليلي ويستحق التوحيد**).
- الفرق والمستشفيات والمراكز: قوائم حية من مصادرها بدل قوائم ثابتة.

**لكل نموذج:**
- **حادث:** ترتيب منطقي؛ وصف الحالة يُعبأ مسبقًا من CAD ويستكمله الموظف. «المركز» يُشتق من الفرقة المختارة إن أمكن.
- **E:** حذف حقل زمن الاستجابة اليدوي ← بطاقة أزمنة محسوبة بارزة (هو جوهر النموذج). إبقاء النتيجة النهائية والمستشفى يدويين.
- **تصعيد:** النموذج الأخف؛ التفاصيل تُعبأ من CAD. لا حاجة لتقسيم.
- **جوي:** قبل أي تحسين، حسم علاقته بـCAD. قائمة الفرقة من المصدر الحي.
- **يومي:** توليد الرقم والتاريخ تلقائيًا؛ اشتقاق عدد بلاغات الجوي. حقل «تعبئة النموذج» مسمّى مربك ← إعادة تسمية «ملاحظات التعبئة».
- **كبار المسعفين:** تعبئة أسماء التسليم من الموظفين حسب المسمى؛ اقتراح مناطق التمركز من التمركزات الفعلية للمناوبة. النموذج طويل — قسم التوقيعات مرشح للطي افتراضيًا.

---

## 8) خطة التنفيذ المرحلية (مقترحة — بانتظار الاعتماد)

| المرحلة | المحتوى | السبب |
|---|---|---|
| **L-1** | نقطة `GET /api/incidents/lookup` + مكوّن `IncidentLookup` + اختباراتهما — **بلا ربط بأي نموذج بعد** | يبنى مرة واحدة ويُختبر معزولًا |
| **L-2** | ربط **نموذج E** فقط: Lookup + بطاقة الأزمنة المحسوبة + إزالة الإدخال اليدوي لزمن الاستجابة + حفظ `incidentNumber` مُتحققًا في `form_data` | الأولوية المعلنة + أعلى قيمة (زمن استجابة مركزي) |
| **L-3** | ربط حادث + تصعيد بنفس المكوّن | تعميم المثبت |
| **L-4** | يومي + كبار المسعفين (اشتقاقات داخلية، لا Lookup) | لا يرتبطان ببلاغ مفرد |
| **L-5** | حسم الإسعاف الجوي بعد الجواب التشغيلي | محجوب بسؤال غير محسوم |
| **L-6** | «النماذج المرتبطة بالبلاغ» في بطاقة البلاغ | يعتمد على `incidentNumber` المُتحقق المتراكم من L-2+ |

**أسئلة محجوبة تحتاج قرار المالك قبل مراحلها:** ① هل طلبات الإسعاف الجوي بلاغات CAD؟ ② تعريف «الفرق المستجيبة» في اليومي ③ مصدر جاهزية المركبات اليومية ④ «قائد المنطقة» في التوقيعات — من أين يُشتق؟

---

**نهاية التشخيص — بانتظار اعتماد المالك قبل أي تنفيذ.**
