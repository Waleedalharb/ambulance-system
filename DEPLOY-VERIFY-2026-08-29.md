# DEPLOY-VERIFY-2026-08-29 — تقرير نشر التحقق الميداني (وليس اعتماد Release)

**التاريخ:** 2026-08-29 · **الهدف:** تحقق ميداني فقط بأمر المالك — الجولة **غير معتمدة** بعد
**Commit:** `b22a0ec` (فوق `ba8bc29`) → `origin/main` → Render (ambulance-management-system, frankfurt)

---

## ١. ما نُشر (تطابق النطاق — تحقق قبل Commit)

| العنصر | المحتوى |
|---|---|
| `server.js` (+31) | PI-3 hook في cad-reports (additive، طابور تسلسلي) + PI-4 كشف `places` من اللقطة المختومة |
| `shift-archive-engine.js` (+29) | PI-4: قسم الجهات في اللقطة |
| `public/operations-dashboard.html` (+168) | تبويب «الجهات والمواقع» في الأرشيف |
| `public/smart-schedule.html` (+2/−2) | إصلاح toast→showToast (السطران 3160 و5444) |
| خدمات + سكربتات PI + الوثائق | PI-1..PI-4 + Seed Safety + تقارير التشخيص والإصلاح |
| **مستبعد عمدًا** | `scripts/pi4-preview-gen.js` (قرار Release Audit) |

فحص الأسرار: لا `pk.` ولا tokens ولا passwords في أي سطر مضاف — Push Protection مرّ بلا اعتراض.

## ٢. خط الأساس قبل النشر ↔ بعده

| المؤشر | قبل | بعد |
|---|---|---|
| `/health` | ok (uptime 35219s) | ok (إقلاع جديد، storage/jwt/websocket ✅) |
| `toast('⏳…` المعطوبة في smart-schedule.html | **2** (العلة في الإنتاج) | **0** |
| `showToast('⏳…` المُصلحة | 0 | **2** |
| تبويب «الجهات والمواقع» (PI-4) | — | موجود (7 مطابقات) |

## ٣. الفحص الميداني الإنتاجي (بلا جلسة — النمط المعتمد، وممنوع الإصلاح أثناءه)

```
✅ /health → status:ok · storage:/data persistent:true · jwt:true · websocket:true
✅ /smart-schedule.html → 200 + مؤشر الإصلاح ×2 وصفر بقايا معطوبة
✅ /operations-dashboard.html → 200 + تبويب PI-4 منشور
✅ /js/smart-schedule-cell-editor.js → 200 (مودال التعارض)
✅ /js/smart-map.js → 200 (الخريطة لم تُمس في هذه الجولة)
✅ / و /index.html → 200
⚪ /login.html → 404 — ليس اسم صفحة فعليًا في المنصة (الدخول عبر /index.html) — ليس انحدارًا
```

لا أخطاء ظاهرة في الفحص. لم يُنفَّذ أي إصلاح أو تعديل بعد النشر.

## ٤. places-seed على الإنتاج (البند 4 من الأمر)

**لم يُشغَّل ولن يُشغَّل تلقائيًا:** البذر سكربت يدوي وليس جزءًا من الإقلاع؛ مخازن
الأماكن (`places.json`…) غير موجودة على قرص الإنتاج فيبدأ المحرك فارغًا
(Candidates-only عند تشغيل الاكتشاف لاحقًا). أي تشغيل مستقبلي يكون بأمر منفصل
وبنسخة reconcile المُصلحة فقط.

## ٥. ما لم يُختَبر إنتاجيًا — يتطلب جهاز الإداري (بحكم الأمر: الجولة غير معتمدة)

المحرك سيرفري والمسارات الحساسة خلف الجلسة؛ الدليل المحلي على نفس الـcommit:
قبول 409 ‏14/14 · قنوات الكتابة 15/15 · ميزات الجداول 24/24.
**الخطوة الحاسمة المتبقية — من جهازك:**

```text
استيراد جدول شهر 9 الحقيقي
   → 409 (متوقع إن وُجدت تعيينات يدوية نشطة)
   → نافذة التعارض بأسماء الموظفين والفرقتين
   → «كتابة فوق» + تأكيد
   → Network: official-import → 409 ثم official-import → 200
   → لا ReferenceError في Console
ثم: الجدول كاملًا · عدد الموظفين · الفرق/التعيينات · الإجازات · الأوفرلاب
ثم: تبويب «الجهات والمواقع» على مناوبة مختومة فعلية
ثم: عدم تأثر المستشفيات والتكميل وباقي النظام
```

## ٦. git status النهائي

```
HEAD = origin/main = b22a0ec
?? scripts/pi4-preview-gen.js   ← مستبعد عمدًا فقط — لا شيء آخر معلق
```

**الخلاصة:** النشر اكتمل والتحقق الساكن/الصحي أخضر بالكامل. الاعتماد النهائي معلّق
على نتيجة استيراد شهر 9 الحقيقي من جهاز الإداري + فحص PI-4 على مناوبة مختومة.
