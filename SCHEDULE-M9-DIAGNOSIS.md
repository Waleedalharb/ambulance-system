# SCHEDULE-M9-DIAGNOSIS — لماذا «نجح الحفظ» ولم ينعكس جدول شهر 9

**التاريخ:** 2026-08-30 · **النطاق:** تشخيص فقط — لا إصلاح، لا تعديل كود، لا Commit/Push/Deploy
**الأدلة:** قراءة كود + إعادة إنتاج معزولة **10/10** (`repro-month9-refresh-loss.js`، قاعدة SQLite طارئة، صفر لمس لبيانات المشروع)

---

## ١. السلسلة المطلوبة — مع الحكم المُثبت لكل حلقة

```
Excel                    ✅ قُرئ وحُلّل (تقرير الاستيراد عُرض: 146 موظفًا)
  ↓
Browser payload (طلب 1)  ✅ حمل سبتمبر فعلًا — أعاد 409 (الحماية الصحيحة)
  ↓
official-import          ✅ استقبل ورفض بلا كتابة (سلوك مقصود)
  ↓
⚠️ بين الطلبين: oiApply يعيد القراءة من القاعدة ويستبدل المصفوفة العامة
   (smart-schedule.html:5515-5519 ← 3069-3077) — والقاعدة بلا سبتمبر
   (الـ409 رفض الكتابة) ← بيانات سبتمبر تُمحى من الذاكرة ومن الكاشين
  ↓
Browser payload (طلب 2 — «كتابة فوق»)  ❌ ← *** نقطة الانقطاع هنا ***
   oiSaveToServer(true) يبني payload = { employees: employees }
   من المصفوفة المستبدَلة (5442-5445 ← 5428) — سبتمبر غائب عن الطلب
  ↓
official-import          ✅ «نجح» — لكنه زامن بيانات بلا سبتمبر
  ↓
writeScheduleEmployees   ✅ نفذت — كتبت JSON بلا سبتمبر (ملف عام بلا مفتاح شهر: 2160)
  ↓
syncFromSchedule         ✅ نفذت — لكن periods مستمدة من مداخل الجدولة
   (roster-sync-service.js:303-318)؛ بلا مداخل سبتمبر لا تُبنى فترة 2026-09
  ↓
مصدر قراءة نظام الجداول  ✅ يعمل بأمانة — القاعدة SSOT (3261-3268) وتقرأ
   shift_roster؛ قائمة الشهور من /api/shift-roster/months (server.js:11659)
  ↓
شهر 9 ظاهر في النظام     ❌ — لأنه لم يصل إلى أي مخزن أصلًا
```

## ٢. السبب الجذري (جملة واحدة)

> **إصلاح `toast` جعل «كتابة فوق» تشتغل — لكنها تشتغل على مصفوفة فارغة من سبتمبر:**
> `oiApply` يعيد القراءة من القاعدة **بعد** إرسال الطلب الأول و**قبل** أن يقرر المستخدم
> من نافذة التعارض، فيستبدل المصفوفة العامة بالبيانات القديمة؛ وعندما يضغط المستخدم
> «كتابة فوق» يبني `oiSaveToServer(true)` حمولته من المصفوفة المستبدَلة — فيصل الطلب
> الثاني بلا أي مدخل لشهر 9، و«النجاح» المعاد يخص إعادة بناء الأشهر القديمة فقط.

## ٣. الدليل التجريبي (إعادة إنتاج معزولة 10/10)

| البند | سيناريو أ — التدفق الحقيقي | سيناريو ب — ضبط مرجعي (الطلب 2 يحمل سبتمبر) |
|---|---|---|
| طلب 1 | 409 ✅ | 409 ✅ |
| المصفوفة بعد إعادة القراءة | سبتمبر غائب ✅ (دليل المحو) | — (بلا إعادة قراءة) |
| طلب 2 | 200 · `rosterRows=5 · periods=["2026-8"]` | 200 · `rosterRows=3 · periods=["2026-9"]` |
| `/api/shift-roster/months` بعده | `["2026-08"]` — **بلا سبتمبر** ✅ | `["2026-08","2026-09"]` ✅ |
| صفوف سبتمبر في shift_roster | **0** رغم «نجاح الحفظ» ✅ | 3 ✅ |

نفس الكود المنشور (`b22a0ec`) — السيرفر لم يُغيَّر بين السيناريوهين؛ المتغير الوحيد
هو **حمولة الطلب الثاني**. هذا يثبت أن الانقطاع في الواجهة، لا في السيرفر.

## ٤. إجابات الأسئلة الثمانية المطلوبة

1. **ماذا أعاد official-import فعليًا؟** 200 `{success:true, rosterSync:{…}}` — صادق، لكنه يصف مزامنة الحمولة المستلَمة (بلا سبتمبر)، لا «حفظ جدول شهر 9».
2. **أين كُتبت بيانات شهر 9؟** **لا مكان.** المخازن الثلاثة (shift_roster، schedule-employees.json، IndexedDB/localStorage) كلها كُتبت ببيانات بلا سبتمبر.
3. **هل writeScheduleEmployees نفذت؟** نعم — لكنها كتبت الحمولة المستلَمة (بلا مداخل سبتمبر). والملف **عام بلا مفتاح شهر** (server.js:576, 2160-2162).
4. **هل syncFromSchedule نفذت؟** نعم — حذفت وأعادت بناء الفترات الموجودة في الحمولة فقط (الأشهر القديمة). لم تلمس 2026-09 لأنه لم يرد.
5. **ما مصدر قراءة نظام الجداول عند فتح شهر 9؟** القاعدة هي SSOT: `/api/employees` + `/api/shift-roster` (fetchEmployeesFromDBSilent:2987)، وقائمة الشهور من `shift_roster` مباشرة. JSON/IndexedDB/localStorage سقوط مُنحط فقط (3261-3262).
6. **هل المصدر بعد الاستيراد يحتوي سبتمبر؟** لا — مُثبت: months بلا 2026-09 وsepRows=0.
7. **هل Cache/IndexedDB/JSON يعرض قديمًا؟** ليس هو الجذر — الكاشات كُتبت من القاعدة بعد 409 (adoptServerEmployees يكتب الكاشين write-through:3071-3072). الكاش «وفيّ» للقاعدة الفارغة من سبتمبر. (إعادة فتح الصفحة لا تحل المشكلة: القاعدة نفسها بلا سبتمبر.)
8. **مقارنة قبل/بعد:** أرسل المتصفح سبتمبر (طلب 1) ← رُفض بلا كتابة (409 صحيح) ← أُعيد إرسال بلا سبتمبر (طلب 2) ← حُفظ «بنجاح» ← القراءة تعرض ما حُفظ فعلًا: لا سبتمبر.

## ٥. Source of Truth لكل مرحلة (كما هو قائم فعلًا)

| المرحلة | المصدر الفعلي | ملاحظة |
|---|---|---|
| تحليل الإكسل | `oiState.parsed` (ذاكرة) | **المكان الوحيد الذي بقيت فيه سبتمبر سليمة — ثم أُهمل (`oiState=null`)** |
| حمولة الطلبين | المصفوفة العامة `employees` | تتغير بين الطلبين بفعل إعادة القراءة — **وهذا هو الخلل** |
| الكتابة السيرفرية | `handleScheduleEmployeesSave` → JSON + `syncFromSchedule` | أمينة للحمولة المستلَمة |
| القراءة/العرض | قاعدة البيانات (`shift_roster` + `employees`) | SSOT معلن وعامل |
| قائمة الشهور | `SELECT DISTINCT` من shift_roster | لا سبتمبر بلا صفوف سبتمبر |
| الكاشات | IndexedDB/localStorage/JSON | سقوط مُنحط بلا سلطة عرض |

## ٦. ملاحظتان جانبيتان موثقتان (لا إصلاح الآن)

- **أثر جانبي للطلب الثاني:** لأن الحمولة المستبدَلة تحمل مداخل الأشهر القديمة،
  `syncFromSchedule` حذفت تلك الفترات وأعادت بناءها — أي **أعادت كتابة الأشهر
  التاريخية بلا داعٍ** (`periods=["2026-8"]` في سيناريو أ) وغيّرت `id`s صفوفها.
  البيانات نفسها سليمة، لكن أي مرجعية على `rosterId` قديم تتقادم.
- **متى لا يظهر الخلل:** استيراد بلا تعارض (لا 409) يكمل بنفس الحمولة فينجح —
  ولهذا نجحت كل اختبارات القبول السابقة التي لم تكن تعرف أن إعادة القراءة تسبق
  قرار «كتابة فوق». الخلل يظهر **حصريًا في مسار 409 → كتابة فوق**.

## ٧. اتجاهات الإصلاح المقترحة — بلا تنفيذ (كلٌّ منها يحتاج أمرًا منفصلًا)

1. **تثبيت الحمولة:** عند ظهور 409، تحفظ الواجهة حمولة الطلب الأول (أو `oiState.parsed`)
   وتعيد إرسال **هي نفسها** مع `confirmOverwriteManual: true` — بدل بناء حمولة جديدة
   من مصفوفة قابلة للاستبدال.
2. **أو تأجيل إعادة القراءة:** لا تُستدعى في oiApply إلا بعد `saveResult === 'ok'` نهائي
   (بما فيه نجاح الكتابة فوق)، لا قبل قرار المستخدم.
3. **حارس سيرفري (دفاع عميق):** رفض/تحذير عند `confirmOverwriteManual=true` مع حمولة
   لا تحمل أي مدخل جديد نسبةً للطلب المرفوض سابقًا — أو أدنى: تسجيل `rosterPeriods`
   في سجل التدقيق حتى لا يكون «النجاح» أصمّ عما بُني فعلًا.

**التوصية:** الاتجاه 1 هو الأصغر والأدق سببًا. لكن القرار لك بعد المراجعة — لم يُمس أي سطر.
