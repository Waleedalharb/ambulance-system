# SCHEDULE-409-DIAGNOSIS — تشخيص رفض استيراد جدول شهر 9 (409 Conflict)

**التاريخ:** 2026-08-29 · **النطاق:** تشخيص فقط — لا إصلاح، لا Commit، لا Push، لا Deploy
**الأمر:** تحديد سبب `POST /api/schedule/official-import → 409 Conflict` رغم نجاح `[DB] Saved 146 employees` في المتصفح.

---

## ١. الخلاصة التنفيذية

> **الـ409 ليس خطأً في الكود — هو حماية مقصودة تعمل كما صُممت.**
>
> الخادم رفض الاستيراد لأنه اكتشف أن جدول شهر 9 يضع موظفًا واحدًا على الأقل في فرقة
> **تختلف** عن الفرقة التي نُقل إليها **يدويًا** من واجهة «نقل موظف بين الفرق» (بنطاق
> «من تاريخ فصاعدًا»). الحماية تمنع الاستيراد من استبدال القرار اليدوي **بصمت**،
> وتتطلب تأكيدًا صريحًا («كتابة فوق») قبل المتابعة.
>
> جسم الاستجابة 409 يحمل قائمة `conflicts` بأسماء الموظفين المتعارضين — وهي المفتاح
> لمعرفة من سبّب الرفض تحديدًا في الإنتاج.

---

## ٢. المسار الكامل (واجهة ← خادم ← تخزين)

```
smart-schedule.html
   │  ① تحليل ملف الإكسل محليًا + حفظ نسخة في IndexedDB
   │     ← هنا يظهر «[DB] Saved 146 employees»
   │     ← نجاح محلي في المتصفح فقط، لا علاقة له بقبول الخادم
   │  ② حارس تطابق الشهر (شهر الملف ↔ الشهر المختار) — لو اختلف يتوقف قبل الإرسال
   ▼  ③ POST /api/schedule/official-import
server.js:9540  →  handleScheduleEmployeesSave  (نفس المعالج لـ /api/schedule/employees)
   │  ④ server.js:9491-9495 — كشف التعارض قبل أي كتابة
   │     rosterSyncService.detectManualConflicts(employees)
   │     إذا وُجد تعارض ولم يرد confirmOverwriteManual=true
   │        → 409 { error: 'يوجد تعارض مع تعديلات يدوية', conflicts }
   │        → توقف كامل: لا JSON ولا قاعدة (لا حالة نصف-مستوردة)
   ▼  ⑤ عند عدم وجود تعارض فقط:
writeScheduleEmployees (JSON) + syncFromSchedule (معاملة ذرية في القاعدة)
```

## ٣. الشرط البرمجي الذي أنتج 409 (بالسطور)

**البوابة — server.js:9491-9495:**
```js
if (rosterSyncService && !confirmOverwriteManual) {
    const conflicts = await rosterSyncService.detectManualConflicts(employees);
    if (conflicts.length > 0) {
        return res.status(409).json({ error: 'يوجد تعارض مع تعديلات يدوية', conflicts });
    }
}
```

**تعريف التعارض — services/roster-sync-service.js:120-147:**
```js
// التعيينات اليدوية النشطة فقط:
WHERE ta.end_date IS NULL AND ta.source = 'manual'
// ثم لكل موظف في ملف الاستيراد له تعيين يدوي نشط:
if (importTeamId == null || a.team_id !== importTeamId) {  // ← السطر 136
    conflicts.push({...});
}
```

**السطر 136 هو قلب القضية.** التعارض يتحقق في حالتين:

| الحالة | الشرط | المعنى |
|---|---|---|
| فرقة مختلفة فعلًا | `a.team_id !== importTeamId` | الجدول الجديد يضع الموظف في فرقة غير التي نُقل إليها يدويًا |
| **فرقة الاستيراد لا تُحلّ** | `importTeamId == null` | خلية الفرقة فارغة / «إجازة» / اسم لا يطابق جدول teams — يُحسب تعارضًا **حتى لو لم تكن هناك نية لتغيير الفرقة** |

## ٤. البيانات التي حقّقت الشرط

**من أين تأتي التعيينات اليدوية:** المسار الوحيد الذي يكتب `source='manual'` في
`team_assignments` هو `POST /api/employees/:employeeCode/transfer` بنطاق
`scope='from-date'` (server.js:12211، 12254-12269): يُغلق التعيين النشط ويفتح
تعيينًا يدويًا جديدًا «محميًا من الاستيراد الصامت».

**السيناريو الأرجح في الإنتاج:** بعد اعتماد جدول شهر 8، نُقل موظف واحد أو أكثر يدويًا
بنطاق «من تاريخ فصاعدًا» (مثلًا: نقل مسعف من جنوب 3 إلى سريع 2 اعتبارًا من منتصف
الشهر). عند استيراد شهر 9، وضع الملفُ الموظفَ في فرقة مختلفة عن فرقة النقل اليدوي
(أو خليته غير قابلة للحل) → `detectManualConflicts` أعاد قائمة غير فارغة → 409.

**إثبات أن البيانات هي المتغير الحاسم (قاعدة محلية، `data/ambulance.db`):**
```
manual active: 0          ← صفر تعيينات يدوية نشطة محليًا
by source:        import:187
active by source: import:140
```
لهذا **لا يتكرر الـ409 محليًا** — لا توجد تعيينات يدوية نشطة أصلًا. الـ409 حدث في
الإنتاج لأن بياناته تحمل تعيينات `manual` نشطة.

**قيد صريح:** لا أستطيع رؤية قاعدة الإنتاج، لذلك لا أستطيع تسمية الموظف/الموظفين
المتعارضين تحديدًا. جسم استجابة الـ409 (`conflicts`) — الذي تحفظه الواجهة في
المودال — يحمل الأسماء والفرقتين وتاريخ التعيين لكل متعارض، وهو الدليل المباشر.

## ٥. لماذا ظهر الخطأ بعد «Saved 146 employees»

«Saved 146 employees» رسالة من **IndexedDB في المتصفح** — نسخة محلية احتياطية تُحفظ
قبل إرسال الطلب. نجاحها يعني فقط أن الملف قُرئ وحُفظ محليًا؛ ثم أُرسل الـPOST
فرفضه الخادم. الحدثان مستقلان تمامًا، وتتابعهما في الـConsole طبيعي ومتوقع.

## ٦. التصنيف

**Backend — منطق حماية مقصود يعمل كما صُمم. ليس خطأ كود.**
المشكلة في **تقاطع البيانات مع التدفق**: قرارات نقل يدوية قائمة في الإنتاج + ملف
شهر جديد لا يعرف عنها → الحماية توقف الاستيراد كما يجب أن تفعل.

## ٧. إعادة الإنتاج المعزولة (7/7 ✅)

`sandbox`: قاعدة SQLite مؤقتة بنفس المخطط + الخدمة الحقيقية `roster-sync-service.js`
كما هي (محوّل run/get/all فقط). صفر لمس لبيانات المشروع.

| # | الحالة | النتيجة | الدلالة |
|---|---|---|---|
| A | بلا تعيينات يدوية، الاستيراد لأي فرقة | صفر تعارض ✅ | لماذا نجح استيراد شهر 8 |
| B | يدوي نشط (جنوب 1) + الاستيراد يضعه جنوب 2 | **تعارض** ✅ | سبب 409 — أعاد `{employeeCode, manualTeam:'جنوب 1', importTeam:'جنوب 2', assignedDate}` |
| C | يدوي (جنوب 1) + الاستيراد نفس الفرقة | صفر تعارض ✅ | الملف المتوافق مع النقل اليدوي يمر |
| D | فرقة الاستيراد لا تُحلّ («إجازة»/«تحكم عملياتي» بلا ال) + يدوي نشط | **تعارض** ✅ | الخطر الصامت في السطر 136: تعارض بلا نية تغيير فرقة |
| E | التعيين اليدوي منتهٍ (`end_date` مضبوط) | صفر تعارض ✅ | النطاق «يوم واحد» والتعيينات المغلقة لا تحجب الاستيراد |
| F | تعيين `source='import'` + فرقة مختلفة | صفر تعارض ✅ | الحماية لليدوي فقط — الاستيراد يحدّث الاستيراد بحرية |
| G | موظف جديد غير موجود بالقاعدة | صفر تعارض ✅ | الموظفون الجدد لا يمكن أن يتعارضوا |

## ٨. الحلول المقترحة — بلا تنفيذ (كل حل يحتاج أمرًا منفصلًا)

**(أ) القرار التشغيلي المتاح فورًا — بلا أي تعديل كود:**
الواجهة تعرض أصلًا مودال 409 بقائمة المتعارضين وزرَّي «إلغاء» / «كتابة فوق».
الإجراء الصحيح الآن:
1. إعادة استيراد شهر 9 → قراءة قائمة `conflicts` في المودال (الأسماء + الفرقتان).
2. مراجعة كل اسم: هل النقل اليدوي ما زال ساريًا في شهر 9؟
   - **نعم** ← صحّح فرقة الموظف في ملف الإكسل لتطابق النقل اليدوي، ثم أعد الاستيراد (يمر بلا 409 — حالة C).
   - **انتهى/لم يعد ساريًا** ← اضغط «كتابة فوق» بوعي: يُستبدل التعيين اليدوي ويُسجَّل في سجل التدقيق (`import_overwrite_manual`).

**(ب) تحسين مقترح لاحقًا (يحتاج أمر تنفيذ):** تمييز حالة `importTeamId == null` في
السطر 136 بين «بلا فريق مقصود» (إجازة/خارج الكادر) و«اسم لا يُحلّ» — الحالة الأولى
لا يجب أن تُنتج تعارضًا ضد تعيين يدوي إن كانت سياسة النقل اليدوي تتجاوز الإجازات.

**(ج) تحسين مقترح لاحقًا:** قبل الاستيراد، عرض استباقي للتعيينات اليدوية النشطة
(قراءة فقط) حتى يراها الإداري قبل رفع ملف الشهر الجديد، بدل اكتشافها عبر 409.

## ٩. حدود التشخيص وما يلزم لإغلاقه نهائيًا

- لم أرَ ملف شهر 9 الفعلي ولا جسم استجابة 409 من الإنتاج — سيناريو (B/D) مثبت
  كوديًا ومعزولًا، لكن **أي الموظفين تحديدًا** تسبب في 409 الإنتاج يحتاج أحد:
  جسم الاستجابة من المتصفح (تبويب Network/المودال)، أو ملف شهر 9 + قائمة التعيينات
  اليدوية النشطة من الإنتاج.
- لم يُلمس: الكود، البيانات، report_times، الاحتساب، المناوبات، CAD، Place Intelligence.
- حالة git لم تتغير (آخر commit `ba8bc29`).

**التوصية:** الخطوة التالية ليست تعديل كود — هي تنفيذ الإجراء (أ) وقراءة قائمة
المتعارضين، ثم يُتخذ قرار: تصحيح الملف أو «كتابة فوق» واعية.
