/* ============================================================
   إعداد مزود الخريطة — ملف مُدرج في Git (آمن: بلا أي مفاتيح)
   الافتراضي leaflet = السلوك القائم حرفيًا، والمنصة تعمل بدون أي إعداد.

   لتفعيل Mapbox محليًا: أنشئ public/js/map-config.local.js (خارج Git)
   بالمحتوى الموضح في map-config.example.js — يُحمَّل بعد هذا الملف
   فيعيد تعريف window.SMAP_MAP_CONFIG. المفتاح العام (pk.) يبقى في
   الملف المحلي فقط، مقيّدًا بنطاق المنصة من لوحة Mapbox، ومنفصلًا
   تمامًا عن مفتاح تكامل CAD.
   ============================================================ */
window.SMAP_MAP_CONFIG = window.SMAP_MAP_CONFIG || {
    provider: 'leaflet'
};
