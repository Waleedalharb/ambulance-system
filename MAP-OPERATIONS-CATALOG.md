# كتالوج تشغيل الخرائط والتقاط CAD

**النسخة:** 2026-08-25 · **الحالة:** معتمد كمرجع تشغيلي رسمي قبل النشر
**الغرض:** أن يستطيع أي شخص — إداري أو فني — معرفة ما يراه على الخريطة، ومن أين جاء، وكيف وصل الرقم إليه.

> 📖 **للمستخدم الميداني/الإداري:** يوجد داخل المنصة نفسها **«دليل استخدام الخريطة التشغيلية»**
> (زر 📖 في شريط الخريطة + أزرار ⓘ بجانب المؤشرات) بلغة تشغيلية مبسطة — هذا الملف
> هو المرجع الفني/التشغيلي الرسمي الموازي له، ومصدر الدليلان واحد (هذه الوثيقة).

---

## 0 — فصل المفاهيم الثلاثة (قاعدة حاكمة)

| الرمز | الطبقة | سؤالها | مصدرها | عمر البيانات |
|---|---|---|---|---|
| 🟢 | **الخريطة الحية** | ماذا يحدث **الآن**؟ | `GET /api/cad-reports` (المناوبة النشطة) + `/api/staffing/state` | لحظي، عبر SSE |
| 🔵 | **ذاكرة الخريطة** | ماذا حدث **تاريخيًا** وما النمط؟ | `GET /api/analytics/{incidents,patterns,coverage,recommendations}` | نطاق زمني يختاره المستخدم |
| 🟣 | **الـOverlay** | ماذا يعرض **CAD** الآن؟ | واجهة CAD نفسها (التقاط سلبي فقط) | لحظي |

- الحية لا تعرض تحليلًا تاريخيًا، والذاكرة لا تعرض ضغطًا لحظيًا، والـOverlay لا يحلل ولا يقرر — **يلتقط فقط**.
- لا رقم في أي طبقة بلا مصدر قابل للتتبع، ولا قيمة غائبة تُخمَّن — تُعرض «غير متوفر».

---

## A — عناصر الخريطة الحية (NOW)

**مصدر البيانات:** ملخص محرك البلاغات للمناوبة النشطة (`reportService.getIncidentSummary`).
**قناة الوصول (كيف تصل الحالة إلى العلامة):**

1. حدث لحظي: أي إنشاء/إثراء/تغيّر حالة بلاغ ← بث SSE `new_report` ← إعادة جلب الملخص ← إعادة رسم.
2. عند اتصال أو **إعادة اتصال** SSE: إعادة جلب كاملة من الخادم — أي إغلاق فُوِّت أثناء انقطاع يُصحَّح فورًا من مصدر الحقيقة.
3. بلا SSE (وضع احتياطي): جلب دوري كل 3 ثوانٍ يشمل ملخص البلاغات.
4. عند فتح الصفحة: جلب أولي مرة واحدة.

**دورة حياة العلامة (من مصدر الحالة، لا إخفاء بصري ولا مؤقتات):**
كل إعادة رسم تمسح طبقة البلاغات **كليًا** وتعيد بناءها من الملخص مفلترًا ببوابة `isFinal` —
فالعلامة لا تُخفى ولا تُنتَظر: البلاغ الذي لم يعد نشطًا **لا يُرسم أصلًا** في الدورة التالية.

### A1 📍 علامة البلاغ
| | |
|---|---|
| **المعنى** | بلاغ **نشط** (`status = active`) له إحداثيات CAD فعلية |
| **المصدر** | `incidents[]` في ملخص المناوبة — الإحداثيات من CAD فقط (لا موقع مختلق) |
| **يظهر** | عند أول ملخص يكون فيه البلاغ نشطًا ومحدد الموقع |
| **يختفي** | فور تحول حالته إلى `closed` أو `cancelled` (الحالة النهائية **لاصقة**: لا ترتد إلا بإشارة `active` صريحة من CAD) |
| **اللون** | خطورة سيرفرية: 🟢 وصلت/باشرت فرقة محتسبة · 🟡 قيد الانتظار/الطريق ضمن الحد · 🔴 تجاوز المنقضي متوسط وصول القطاع |
| **شارة إضافية** | ⚠️ «احتمال تكرار — يحتاج تحقق» فوق العلامة عند وجود مرشحي تكرار (تنبيه فقط — القرار في CAD) |
| **التفاعل** | نقرة ← بطاقة القرار (النوع/الموقع/الإنشاء/الفرق وأزمنتها/أدلة التكرار) + ربط بصري بالفرقة والمركز |

### A2 🚑 علامة الفرقة
| | |
|---|---|
| **المعنى** | فرقة مناوبة من بيانات التكميل، بحالة جاهزيتها |
| **المصدر** | `/api/staffing/state` — **لا GPS للفرق**: موضعها حلقة عرضية صغيرة حول مركزها (تموضع عرضي بحت) |
| **اللون** | 🟢 جاهزة · 🟡 بانتظار قرار التكميل · 🔴 ناقصة/خارج الخدمة أو مركبة غير جاهزة |
| **التفاعل** | نقرة ← بطاقة الفرقة (الطاقم/المركبة/البلاغ المرتبط/آخر تحديث) |

### A3 🏥 علامة المركز
| | |
|---|---|
| **المعنى** | مركز تشغيلي ثابت |
| **المصدر** | جدول `operationalCenters` الثابت |
| **اللون** | أسوأ حالة فرقة تابعة (أحمر ← أصفر ← أخضر) |
| **التفاعل** | نقرة ← بطاقة المركز (فرقه وجاهزيتها وبلاغاته المرتبطة) |

### A4 🚨 نظام التنبيه التشغيلي + الهالات
- **المصدر:** `alert-rules.js` — محرك الأزمنة الموحد (إعادة بناء 2026-08-25): أربع مراحل مستقلة لكل منها Start/End/Target — وصول: إنشاء CAD → البحث (10د · **Echo 8د**) · مباشرة: البحث → العلاج (2د) · موقع: العلاج → النقل/نهاية البلاغ (10د) · منشأة: بدء التسليم → انتهاء التسليم (10د). التنبيه عند التجاوز الفعلي فقط (المساواة ليست تأخيرًا) و«قريب» (≥80%) مؤشر منفصل.
- **التصنيف:** A/B/C/D/E من حرف `proQACode` (Parser مركزي وحيد في المحرك) — A بارد · B متوسط · C عادي · D خطير · E مهدد للحياة 🚨 — ويظهر في سبب التنبيه مع الهدف والفعلي والتجاوز.
- **بوابة صارمة:** فرقة بلا حدث «التحرك» ← لا مؤقت ولا تنبيه إطلاقًا. بلاغ بلا وقت إنشاء ← بلا مؤقت. بلاغ نهائي ← تنتهي تنبيهاته فورًا. كل مرحلة يتوقف مؤقتها فور تسجيل حدث نهايتها (لا تنبيهات وهمية).
- **العرض:** شريط أعلى الخريطة (قابل للإغلاق — الإغلاق يخفي العرض فقط) + هالة حول علامة الفرقة/البلاغ + مؤقتات المراحل الأربع في بطاقة البلاغ (تُجمَّد عند اكتمال كل مرحلة).

### A5 ⚠️ خطوط احتمال التكرار
خطوط متقطعة كهرمانية بين البلاغ ومرشحيه **الظاهرين** على الخريطة فقط. مرشح منتهٍ/بلا إحداثيات يبقى موثقًا في بطاقة الأدلة بلا خط مختلق.

### A6 مؤشرات الشريط العلوي (KPIs)
| المؤشر | مصدره |
|---|---|
| الضغط الحالي | أسوأ خطورة بين البلاغات **النشطة** فقط |
| بلاغات المناوبة | `activeCount` — النشطة فقط (المنتهية في الإحصائيات التاريخية) |
| فرق جاهزة | جاهزة/إجمالي من التكميل (الفرق الميدانية فقط) |
| متوسط الوصول | متوسط أفضل وصول لكل بلاغ بين فرقه المحتسبة + عدد البلاغات الداخلة |
| حالات حرجة | عدد التنبيهات المتجاوزة للحد |

### A7 لوحة «بلاغات بلا موقع دقيق»
البلاغات **النشطة** بلا إحداثيات — قائمة تشغيلية تتطلب اكتمال موقعها الآن. لا تُخمَّن مواقعها أبدًا.

### A8 ما أُزيل من الخريطة الحية (فصل العرض — 2026-08-25)
الطبقات التحليلية التالية كانت تظهر في الحية وهي **مفاهيم تاريخية/تحليلية** مكانها الذاكرة:
المواقع الساخنة · الكثافة · الذروة · الشوارع الأكثر بلاغًا · KPI «ذروة المناوبة» · لوحتا «أكثر المواقع» و«الذروة».
**المنطق لم يُحذف** — أُزيلت عناصر العرض من الصفحة فقط، والكود نائم ولا يُرسم.

---

## B — عناصر ذاكرة الخريطة (HISTORY)

**مصدر البيانات:** مشتقات سيرفرية صرفة من الجداول الخام (`incident_registry` / `report_times` / `positioning_events`) عبر `/api/analytics/*` — **لا تخزين مشتق ولا جداول موازية**، والواجهة عرض فقط (Zero Business Logic).
**النطاق:** يختاره المستخدم (افتراضي: آخر 7 أيام). المنتهي يبقى في الذاكرة — «المنتهي حدث فعلًا».

### B1 تبويب الكثافة — قابل للضغط والتتبع
- حرارية للمشهد العام + **نقطة صفراء لكل بلاغ محدد الموقع**.
- **نقرة على نقطة ← بطاقة البلاغ الفعلي:** الرقم/الحالة/النوع/وقت الإنشاء CAD/الموقع/الفرق المحتسبة/أفضل زمن وصول/أقرب تمركز وقت البلاغ — بالحقول المتوفرة فقط، والغائب «غير متوفر».
- عداد «محددة الموقع» = عدد النقاط حرفيًا: لا نقطة بلا بلاغ ولا بلاغ محدد الموقع بلا نقطة.

### B2 المناطق الساخنة / فجوات التغطية — قابلة للضغط والتتبع
- **المنطقة** = خلية شبكية ~1.1كم (0.01°) فيها **3 بلاغات فأكثر** محددة الموقع.
- **نقرة على منطقة ← نافذتها:** العدد + الفترة + متوسط الوصول + ذروتها + أقرب تمركز تاريخي + حالة التغطية وسببها + **قائمة بلاغاتها الفعلية** (رقم/وقت/نوع/حي) — وكل بلاغ في القائمة يفتح بطاقته.
- نفس القائمة كاملة تحت كل منطقة في اللوحة الجانبية. **«8 بلاغات» = هذه البلاغات الثمانية بالتحديد** (`incidentNumbers` من الخادم).

### B3 الأنماط الزمنية
التوزيع بالساعة/اليوم/الفترة (صباح 07–15 · مساء 15–23 · ليل 23–07) + الذروة + أكثر الأحياء والشوارع + مقارنة بالفترة السابقة المماثلة.

### B4 التمركزات التاريخية
نقاط التمركز الفعلية من سجل `positioning_events` فقط — فرقة بلا تمركز مسجل لا يُخمَّن مكانها.

### B5 دعم القرار
مرشحو تمركز (درجة + محاكاة أثر على البلاغات التاريخية ضمن 3كم) + نوافذ تستحق إعادة التوزيع. **دعم قرار فقط — لا تنفيذ تلقائي لأي إجراء.**

### B6 كيف وصلنا إلى الرقم؟ (جدول التتبع)
| الرقم | مصدره وقاعدته | ما يُستبعد |
|---|---|---|
| الكثافة | مواقع البلاغات الفعلية المسجلة في النطاق، وزن متساوٍ | بلا إحداثية (يُحسب في الإجمالي ولا يُرسم) |
| عدد بلاغات المنطقة | خلية `toFixed(2)` درجة (~1.1كم) بحد أدنى 3 — العدد = `incidentNumbers` حرفيًا | بلا إحداثية |
| متوسط الوصول | لكل بلاغ: **أفضل** وصول بين فرقه **المحتسبة** (من إنشاء CAD حتى «البحث») ثم متوسط البلاغات | الفرق التي لم تتحرك/المسحوبة/الملغاة يدويًا/وحدات خطة الاستجابة |
| الذروة | أكثر ساعة إنشاء بلاغات من `cad_created_at` الفعلي (محلل CAD المركزي) | بلا وقت قابل للقراءة (يُحسب في `unresolvedTimeCount`) |
| أكثر حي/شارع | تكرار `district`/`street` الخام — بلاغ مرة واحدة مهما تعددت فرقه | — |
| نسبة محددة الموقع | `positioned / incidents` في النطاق | — |
| فجوة التغطية | منطقة طلب + أحد: أقرب تمركز وقت البلاغات > 3كم · بلاغات بلا تمركز معروف وقتها · متوسط وصولها أعلى من متوسط النطاق | — |
| مرشح التمركز | خلية طلب مرتفع، درجة من عدد البلاغات والتغطية، ومحاكاة ضمن 3كم على البلاغات الفعلية | لا ينفَّذ — عرض فقط |
| المشاركة المحتسبة | قاعدة `isParticipationCounted` الوحيدة في المنصة: لا مسحوبة ولا ملغاة يدويًا، ولقطة CAD لا تُحسب إلا بدليل «التحرك» | withdrawn · manual_cancelled · خطة الاستجابة |

---

## C — الـOverlay تشغيليًا (التقاط فقط)

| القاعدة | المعنى |
|---|---|
| **No Journey = No Participation** | لا أوقات تُنسب لفرقة بلا Journey خاصة بها — ولا نسخ أوقات بين الفرق إطلاقًا |
| **Available ≠ Assigned ≠ Responding ≠ Positioning ≠ Participating** | وحدة «خطة الاستجابة» (بلا أوقات ولا حالة وحدة) لا تُنشئ مشاركة — تُوثَّق في `planUnitsIgnored` |
| خطة الاستجابة | مصدر **موقع البلاغ** فقط — لا مصدر مشاركة |
| البصمة | لكل `incident + unit + journey` — لا يُرسل إلا التغيّر الحقيقي |
| إغلاق/اختفاء البلاغ | الغياب المؤكد عن قائمة CAD (3 لقطات متتالية + 60 ثانية، أو مهلة 15 دقيقة لمن لم يُرَ) ← `status = cancelled` ← يخرج من التشغيل ويبقى تاريخيًا |
| الحالة النهائية | لاصقة سيرفريًا — لا ترتد إلى active إلا بإشارة active صريحة من CAD |
| هوية الوحدة | `eventId + cad_run_unit_id` = نفس المشاركة مهما تغيّر المسمى (فرقة من CAD تُربط بسجل التكميل اللاحق بلا تكرار) |
| الإلغاء اليدوي | `manual_cancelled` — يُستبعد فورًا من كل عدّاد ومؤشر ويبقى في التاريخ والتدقيق (لا حذف) |
| الـOverlay لا يحلل | لا احتساب ولا تحليل فيه — كل الاشتقاق سيرفري |

---

## ⚠️ يحتاج قرارًا تشغيليًا (موثق — لا تغيير بلا قرار)

1. **سقف 10 فرق لكل بلاغ** في مسار التسجيل (`slice(0, 10)` في POST `/api/cad-reports`): بلاغ بأكثر من 10 فرق يُقتطع فائضه. بانتظار قرار: رفع السقف أو إبقاؤه.
2. **سجلات تاريخية ملوثة** من قبل قاعدة «Journey لكل فرقة» (لقطات مشتركة الأوقات): **حُسمت 2026-08-26 بقرار المالك** — قاعدة `legacy_snapshot` في `ReportService.isLegacySnapshot` (قبل إغلاق 2026-08-23.c + بلا هوية CAD + phases موقوتة = historical_unverified): مستبعدة من كل العدادات (byCrew/الإنجازات/التوزيع/المؤشرات/الذاكرة التحليلية) ومحفوظة للتدقيق بحالتها — لا حذف ولا تعديل phases ولا تصفير يدوي. 149 مشاركة على 14 فرقة، ويظهر السجل في التفاصيل بـ `legacySnapshot: true`.
3. **ثوابت التحليل قابلة للضبط بقرار:** حجم الخلية 0.01° (~1.1كم) · حد المنطقة 3 بلاغات · بُعد التغطية 3كم · نصف قطر المحاكاة 3كم (`ANALYTICS_RULES`).
4. **خطورة البلاغ 🔴** تعتمد متوسط وصول القطاع المتحرك داخل المناوبة — قاعدة عرض تشغيلي (تنبيه انتباه)، ليست حكمًا طبيًا.
5. **«بلاغات المناوبة» في الشريط العلوي = النشطة فقط.** الإجمالي التاريخي الشامل (مع المنتهية) متاح في تقرير المناوبة وتوزيع البلاغات — مكانان لسؤالين مختلفين، موثق هنا حتى لا يُقرأ أحدهما خطأً.

---

## اختبارات تثبيت هذا الكتالوج

| الطقم | العدد | ماذا يثبت |
|---|---|---|
| `scripts/map-live-lifecycle-test.js` | 24 | ظهور/اختفاء البلاغ من مصدر الحالة · اللصق النهائي · عقد «لا Markers قديمة» · قناة SSE/الاحتياطي · فصل الطبقات |
| `scripts/map-history-traceability-test.js` | 21 | كل منطقة تكشف بلاغاتها (`incidentNumbers = count`) · بلا إحداثية لا يُخمَّن · حقول البطاقة · اتساق الإجماليات |
| `scripts/incident-lifecycle-test.js` | 29 | دورة حياة البلاغ على مستوى المحرك |
| `scripts/alert-rules-test.js` | 32 | كل تنبيه مرتبط بحدث CAD فعلي |
| أطقم التحليلات الأربعة (`analytics-*`) | 97 | H1–H4 كما اعتُمدت |
