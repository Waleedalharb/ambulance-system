# مواصفة L-1 التنفيذية — Incident Lookup (نقطة API + مكوّن واجهة + اختبارات)

**التاريخ:** 2026-09-01 · **الحالة:** للمراجعة والاعتماد فقط — **ممنوع كتابة أي كود قبل توقيع المالك**
**المرجع:** `FORMS-INCIDENT-LOOKUP-DIAGNOSIS.md` المعتمد · **النطاق:** L-1 فقط — **لا ربط بأي نموذج في هذه المرحلة**

---

## 1) الهدف والنطاق

بناء أساس «البحث عن بلاغ» مرة واحدة، معزولًا وقابلًا لإعادة الاستخدام:

- **نقطة API خفيفة واحدة:** بحث عن بلاغ برقمه فقط، تعيد بياناته وأزمنة فرقه كما هي مخزنة ومحسوبة مركزيًا.
- **مكوّن واجهة واحد `IncidentLookup`:** حقل بحث + حالات + بطاقة عرض read-only — يُبنى ويُختبر **مستقلًا**، ولا يُحقن في أي نموذج من النماذج الستة إلا في L-2+ باعتماد منفصل.

### خارج النطاق صراحةً

- لا تعديل على النماذج الستة أو `forms/*.html` أو شاشاتها.
- لا تعديل على `incident_registry` أو `report_times` أو أي مخطط DB — **قراءة فقط**.
- لا منطق حساب زمن جديد — تُقرأ `resp_arrival_min / resp_mubashara_min` كما خزّنها الحساب المركزي القائم (`report-service.js:205`).
- لا صلاحية جديدة — تُستخدم `ops.forms` نفسها (نفس حارس نقاط حفظ النماذج القائم).
- لا مساس بـPI-10 / Place Identity / R1–R5 / `app.css` / `app.js`.

---

## 2) عقد نقطة الـAPI

```
GET /api/incidents/lookup?number=<digits>
authenticate → authorizePerm('ops.forms')
```

### 2.1 التحقق من المدخل

| القاعدة | السلوك |
|---|---|
| `number` غائب/فارغ | `400 { error: 'number مطلوب' }` |
| لا يطابق `^\d{3,12}$` بعد `trim` | `400 { error: 'صيغة رقم بلاغ غير صالحة' }` (أرقام CAD الفعلية 7–8 خانات؛ الحد يحجب النص الحر دون افتراض صيغة جديدة) |
| غير موجود في `incident_registry` | `404 { found: false, number }` |

### 2.2 دلالات البحث

1. كل صفوف `incident_registry` بالرقم، **الأحدث أولًا** (`created_at` DESC).
2. الافتراضي = أحدث ظهور؛ إن وُجد في مناوبات أخرى تُعاد قائمتهم في `otherShifts` (لا إخفاء ولا تخمين صامت — قاعدة التعدد المعتمدة في التشخيص §3.3).
3. مشاركات الفرقة للبلاغ الافتراضي من `report_times ⋈ reports` (نفس join القائم في `getIncidentParticipationRows`).
4. **قاعدة الاحتساب:** يُعاد استخدام `isParticipationCounted` القائمة حرفيًا — المسحوبة/الملغاة يدويًا **تُعرض موسومة ولا تُحذف ولا تُفلتر بصمت**، ولا تدخل في «الأسرع».

### 2.3 شكل الرد `200`

```jsonc
{
  "found": true,
  "number": "1314119",
  "shiftId": 1784126563184,               // مناوبة الظهور الافتراضي (الأحدث)
  "otherShifts": [                         // [] إن لم يتكرر
    { "shiftId": 1784000000000, "createdAt": "...", "status": "cancelled" }
  ],
  "incident": {
    "number": "1314119",
    "code": "32B03",                       // ترميز MDS كما ورد — بلا ترجمة
    "type": "medical",                     // الخام كما هو
    "cadCreatedAtRaw": "20/08/2026 12:09:20 PM",  // خام CAD للتتبع
    "cadCreatedAtTs": 1787...,             // مللي ثانية عبر _cadDateTimeTs القائمة — أو null
    "status": "active",
    "address": "...", "district": "...", "street": "...", "city": "...", "region": "...",
    "lat": 24.6, "lng": 46.7,              // أو null (متوفر لـ50/145 فقط)
    "description": "..."                    // وصف CAD — أساس التعبئة المسبقة في L-3
  },
  "units": [
    {
      "unit": "جنوب 11",
      "phases": { "قبول": "10:56:12 AM", "التحرك": "...", "البحث": "...", "العلاج": "...", "النقل": "..." },
      "respArrivalMin": 8.4,               // كما خُزّن — null إن تعذر الحساب
      "respMubasharaMin": 3.1,             // كما خُزّن — null إن تعذر
      "counted": true,                      // isParticipationCounted القائمة
      "flags": { "withdrawn": false, "manualCancelled": false, "manualCancelReason": null }
    }
  ],
  "bestArrivalMin": 8.4,                    // MIN على المشاركات counted فقط — null إن لا شيء
  "timeCompleteness": {
    "state": "complete" | "partial" | "missing",
    "missing": ["cad_created_at" | "arrival_phase" | "no_counted_participation"]
  }
}
```

### 2.4 قرارات العقد المحسومة

| القرار | الحكم | السبب |
|---|---|---|
| `caller_number` | **مستثنى من الرد** | بيانات شخصية لا تحتاجها أي نموذج — أقل كشف ممكن |
| الأزمنة | تُعاد كما خُزّنت؛ لا إعادة حساب في النقطة | ممنوع منطق موازٍ — القابلية للتتبع شرط المالك |
| `bestArrivalMin` | MIN فوق counted فقط، `null` بصدق إن لا شيء | نفس دلالة التحليلات القائمة |
| اكتمال الأزمنة | `timeCompleteness` صريح — لا صفر ولا قيمة وهمية | قرار المالك: «لا زمن استجابة وهميًا أو صفرًا» |
| الأداء | استعلامان محدودان بالرقم؛ بلا pagination (مشاركات البلاغ الواحد قليلة فعليًا) | لا حمل جديد على الخادم |

### 2.5 موضع التنفيذ (طبقات قائمة — بلا طبقة موازية)

| الملف | الإضافة |
|---|---|
| `storage-adapter.js` | `getIncidentRowsByNumber(number)` (كل المناوبات، الأحدث أولًا) + `getIncidentParticipationRows` **القائمة تُعاد استخدامها كما هي** |
| `services/report-service.js` | `getIncidentLookup(number)` — تجميع + `isParticipationCounted` + `timeCompleteness` (قراءة وتجميع فقط) |
| `server.js` | تسجيل المسار (~15 سطرًا) بنفس نمط المسارات المجاورة |

---

## 3) عقد مكوّن الواجهة `IncidentLookup`

### 3.1 الملفات (جديدة كلها — صفر تعديل على ملفات الواجهة القائمة في L-1)

- `public/js/incident-lookup.js` — المكوّن (vanilla JS، نمط المنصة القائم، بلا تبعية جديدة).
- `public/css/incident-lookup.css` — ثيم داكن من لوحة المنصة (`--pi-ink`, `--pi-ink-dim`, زجاج `rgba(255,255,255,0.05)`…) متوافق مع جولة ثيم النماذج المنجزة.
- في L-1 يُختبر المكوّن عبر **تركيب برمجي معزول في الاختبار** (CDP) — لا يُربط بأي نموذج ولا يُحمَّل في `index.html` إلا مع أول نموذج فعلي (L-2)؛ هذا يمنع شحن كود غير مستخدم للإنتاج.

### 3.2 آلة الحالات

```
فارغ ──بحث──► جارٍ البحث… ──┬─ 404 ──► ✗ «لا يوجد بلاغ بهذا الرقم»
                            ├─ 200 + complete ──► ✓ بطاقة البلاغ
                            ├─ 200 + partial/missing ──► ⚠️ «البلاغ موجود، لكن بياناته الزمنية غير مكتملة»
                            │                          + البطاقة بما توفر (الحقول الناقصة موسومة «غير متوفر»)
                            ├─ 200 + otherShifts ──► نفس البطاقة + شارة «ظهر أيضًا في مناوبة <تاريخ>»
                            └─ خطأ/انقطاع ──► ✗ «تعذر البحث — تحقق من الاتصال» (قابل لإعادة المحاولة)
```

- قفل إدخال أثناء البحث + منع إرسال مكرر (debounce/إلغاء).
- الإدخال يقبل أرقامًا فقط ويرفض الصيغ غير الصالحة قبل الإرسال (نفس قاعدة الـAPI).
- **read-only ببنية:** بيانات CAD تُعرض عناصر نصية/حقول `disabled` موسومة «من CAD» — لا توجد أصلًا كمدخلات قابلة للكتابة داخل المكوّن.
- زمن الاستجابة يُعرض بارزًا لكل فرقة + «الأسرع»، وعند `null` يُعرض «غير متاح — بيانات CAD ناقصة» ولا يُعرض صفر إطلاقًا.
- الأوقات تُعرض بصيغة الرياض الجدارية عبر `time-riyadh.js` القائم؛ `cadCreatedAtRaw` يبقى قابلًا للاطلاع (tooltip/تفصيل) للتتبع.
- API عام واحد: `IncidentLookup.mount(el, { onResolved(payload) })` — قناة التمرير التي ستستهلكها النماذج في L-2+ دون تعديل المكوّن.

---

## 4) الاختبارات (قبول L-1)

### 4.1 `scripts/incident-lookup-api-test.js` — خادم معزول (نمط VACUUM INTO القائم)

| # | الفحص | البيانات الفعلية المستخدمة |
|---|---|---|
| 1 | `200` + كمال المخطط لبلاغ موثق | `1300918` (له `cad_created_at`) |
| 2 | القيم **تطابق صفوف DB حرفيًا** (respArrivalMin = `report_times.resp_arrival_min`) | نفس البلاغ — قابلية التتبع |
| 3 | الافتراضي = الأحدث + `otherShifts` عند التكرار | `1300370` (مناوبتان) |
| 4 | `404` لرقم غير موجود | `0000000` |
| 5 | `400` لصيغ غير صالحة (`abc`, فراغ, خانة واحدة) | — |
| 6 | `401` بلا جلسة + `403` لجلسة بلا `ops.forms` | حسابات الاختبار القائمة |
| 7 | `timeCompleteness=partial` لبلاغ بلا `cad_created_at` | `99900127` |
| 8 | مشاركة ملغاة يدويًا تظهر موسومة وخارج «الأسرع» | بلاغ فيه `manual_cancelled=1` |
| 9 | `caller_number` **غائب** من الرد | أي بلاغ |
| 10 | انحدار: الاختبارات الخضراء القائمة لا تتأثر | traceability 23 · nav-perms 33 |

### 4.2 `scripts/incident-lookup-ui-test.js` — Chrome headless CDP (نمط `forms-theme-test.js`)

| # | الفحص |
|---|---|
| 1 | التركيب المعزول + صفر أخطاء console |
| 2 | آلة الحالات الخمس كلها تظهر بالنصوص المعتمدة |
| 3 | بطاقة بلاغ حقيقي: الاسم/الموقع/الأزمنة/«الأسرع» صحيحة مقارنة بالـAPI |
| 4 | **صفر حقل قابل للكتابة يحمل بيانات CAD** (فحص DOM برمجي) |
| 5 | زمن غير متاح يظهر «غير متاح» ولا يظهر «0» إطلاقًا |
| 6 | شارة تعدد المناوبات تظهر لـ`1300370` |
| 7 | الثيم الداكن: صفر أسطح فاتحة + تباين نصوص (نفس مقاييس جولة الثيم) + لقطة بصرية |

---

## 5) الملفات والمخاطر والتراجع

| بند | التفصيل |
|---|---|
| **ملفات جديدة** | `incident-lookup.js` · `incident-lookup.css` · الاختباران · هذا الملف |
| **ملفات تُعدَّل** | `server.js` (مسار واحد) · `services/report-service.js` (دالة واحدة) · `storage-adapter.js` (دالة قراءة واحدة) — ولا شيء آخر. `index.html` **لا يُمسّ في L-1** |
| **مخاطر** | منخفضة: قراءة فقط + ملفات جديدة معزولة + لا DB migration + لا مساس بمسارات قائمة |
| **التراجع** | حذف المسار والملفات الجديدة — لا بيانات ولا سلوك قائم يتغير |
| **فصل الجولات** | شجرة العمل تحمل حاليًا تعديلات جولة ثيم النماذج (بانتظار اعتماد النشر) وPI-10 (محلي) — أي Commit مستقبلي لـL-1 يُفصل بنفس أسلوب `rc6-commit.patch` المعتمد |

## 6) معايير إغلاق L-1

1. الاختباران أخضران بالكامل (10/10 + 7/7) على الخادم المعزول.
2. تقرير تنفيذ بلقطات البطاقة بحالاتها الخمس.
3. تأكيد صريح: لا ربط بأي نموذج · لا كود غير مستخدم شُحن للإنتاج · صفر تعديل على PI-10/الثيم ضمن حزمة L-1.
4. **لا Commit/Push/Deploy إلا بأمر صريح بعد مراجعة النتيجة المحلية.**

---

**بانتظار توقيع المالك على هذه المواصفة قبل كتابة أي سطر.**
