# تقرير تنفيذ L-2 — ربط نموذج E بمكوّن IncidentLookup

**التاريخ:** 2026-09-02 · **النطاق:** تنفيذ محلي فقط — **لا Commit / Push / Deploy**
**المرجع:** `FORMS-LOOKUP-L2-SPEC.md` (اعتماد المالك 2026-09-02 00:34)

---

## 1) النتيجة

| بند | النتيجة |
|---|---|
| اختبار `scripts/e-form-lookup-test.js` | **16/16 ✅** (الفحوص الـ12 + فحصان فرعيان 3أ/3ب و1ب/6ب) |
| انحدار `incident-lookup-api-test.js` | ✅ أخضر |
| انحدار `forms-theme-test.js` | ✅ أخضر |
| أخطاء Console طوال الجلسة | **صفر** |
| اللقطات الخمس | محفوظة في `test-output/e-form-lookup-1788299488210/` |

## 2) الملفات التي تغيّرت (L-2 فقط)

| الملف | التغيير |
|---|---|
| `public/forms/form-e.html` | حذف `eReportNumber` و`eResponseTime` اليدويين · تركيب `#eIncidentLookup` · `eDateTime`/`eLocation` أصبحا `readonly` بوسم «من CAD» · `eUnit` قائمة ديناميكية معطّلة حتى التوثّق · عرض الزمن `#eResponseTimeView` read-only |
| `public/js/app.js` | **3 hunks فقط في نطاق دوال نموذج E** (6520–6724): `initELookup` · `fillEFromLookup` · `eSelectedUnit` · `updateEResponseView` · `cadRawToLocalInput` · إعادة كتابة `saveE`/`clearEForm`/`sendEWhatsApp` |
| `public/index.html` | سطرا تحميل المكوّن (`incident-lookup.css?v=1` + `incident-lookup.js?v=1`) + رفع `app.js` إلى `?v=51` |
| `scripts/e-form-lookup-test.js` | **جديد** — خادم معزول + Chrome CDP + زرع عينات temp |

**لم يُمسّ:** `server.js` · قاعدة البيانات/migration · النماذج الخمسة الأخرى · `incident-lookup.js/.css` (أُعيد استخدامهما كما اعتُمدا في L-1) · PI-10 · ملفات جولة الثيم.

## 3) إثبات فحص #8 من `shift_forms` في temp DB (ليس مجرد نجاح POST)

حفظان فعليان لنفس البلاغ **1310627** بفرقتين مختلفتين:

| الفرقة المختارة | `incidentNumber` | `incidentUnit` | `responseTime` | `responseTimeSource` |
|---|---|---|---|---|
| سريع 1 | `1310627` | `سريع 1` | **6.8** | `cad-central` |
| جنوب 14 | `1310627` | `جنوب 14` | **26** | `cad-central` |

الإثبات الحاسم: اختيار **جنوب 14** حفظ **26** وليس `bestArrivalMin` التحليلي (6.8) — الزمن يتبع الفرقة الفعلية المختارة فقط.

## 4) إثبات فحص #9 — فرقة محتسبة بلا زمن

عينة temp `90000002` (فرقة محتسبة + `resp_arrival_min = NULL`):
- الواجهة عرضت: «⚠️ زمن الاستجابة غير متاح — بيانات CAD ناقصة».
- الحفظ مرّ بـ `responseTime: null` + `responseTimeSource: 'cad-unavailable'` — **لا صفر وهمي**.

## 5) حالات الحجب المثبتة

| الحالة | السلوك المثبت |
|---|---|
| بلاغ بلا فرقة محتسبة (99900127) | قائمة الفرق معطّلة «-- لا توجد فرقة محتسبة --» + الحفظ محظور بتنبيه |
| بلاغ غير موجود (0000000) | «✗ لا يوجد بلاغ بهذا الرقم» + **حارس الحمولة البالية** يمنع الحفظ (عدد الصفوف قبل = بعد) |
| مشاركة ملغاة يدويًا (1310420) | تظهر معطّلة موسومة «(ملغاة — خارج الاحتساب)» — البلاغ فيه **ملغاتان** (جنوب 1 + سريع 1) وكلتاهما معطّلتان |

## 6) تصحيحان موثّقان عن المواصفة (لا توسيع نطاق)

1. **تحويل وقت CAD:** لم يُستخدم `cadCreatedAtTs` مع كائن `Date` لأن الخادم يبنيه بمنطقته الزمنية (Render=UTC ← انزياح +3س). أُضيف `cadRawToLocalInput()` الذي يفكك الخام الجداري للرياض بريجكس ويبني `YYYY-MM-DDTHH:mm` من المكوّنات مباشرة. الخام يبقى tooltip (`title`).
2. **حارس الحمولة البالية:** المكوّن (L-1، لا يُعدَّل) يصفّر `lastPayload` عند فشل البحث دون إخطار المضيف. أُضيف تحقق `getLastPayload() !== payload` في `saveE`/`sendEWhatsApp` حتى لا يُحفظ نموذج على بيانات بلاغ سابق بعد بحث فاشل.

## 7) التوافق الخلفي

سجل `e_case` قديم مزروع بلا الحقول الجديدة (`LEGACY-77`) ظهر في «الحالات المحفوظة» بلا كسر — لا migration ولا لمس للتاريخ.

## 8) اللقطات

`test-output/e-form-lookup-1788299488210/`
1. `shot-1-before-search.png` — النموذج قبل البحث
2. `shot-2-after-resolve.png` — بطاقة CAD + تعبئة الوقت/الموقع بوسم «من CAD» + خيار الفرقة الإلزامي
3. `shot-3-unit-select.png` — «جنوب 14» مختارة ← «الاستجابة (وصول): 26 دقيقة · المباشرة: 26.6 دقيقة»
4. `shot-4-missing.png` — شارة النقص + «غير متاح»
5. `shot-5-notfound.png` — «لا يوجد بلاغ بهذا الرقم»

## 9) حالة شجرة العمل (للفصل المستقبلي)

تغييرات L-2 محصورة في الملفات الأربعة بالجدول §2. شجرة العمل تحمل أيضًا تغييرات جولة الثيم (`executive-theme.css`, `smart-toolbar.css`, سطرا `?v=` في `index.html`) وPI-10 المعلق (`server.js`, `services/place-*`, `place-suggestion.js`) — **كلها خارج L-2** ويجب فصلها بنفس أسلوب الـpatch المعتمد عند أي Commit مستقبلي.

**لا Commit / Push / Deploy — بانتظار مراجعة المالك وأمره الصريح.**
