# مواصفة L-4 التشخيصية — التقرير اليومي + كبار المسعفين

**الحالة:** تشخيص + مواصفة للمراجعة فقط — **لا كود قبل الاعتماد**
**المرجع:** `FORMS-INCIDENT-LOOKUP-DIAGNOSIS.md` §2.5/§2.6 · خطة التنفيذ المرحلية §8 (L-4 = يومي + كبار · الجوي مؤجل L-5)
**تاريخ الفحص:** 2026-09-02 — كل رقم أدناه مثبت من قاعدة البيانات الفعلية

---

## 0) الحكم التشخيصي الأول — فرق جوهري عن L-1/L-2/L-3

**هذان النموذجان ليسا مرتبطين ببلاغ CAD.** لا يوجد فيهما «رقم بلاغ» أصلًا:

| النموذج | طبيعته | هل ينطبق IncidentLookup؟ |
|---|---|---|
| E / حادث / تصعيد (L-2/L-3) | توثيق حالة مرتبطة ببلاغ CAD محدد | ✅ نعم |
| التقرير اليومي | ملخص مستوى المناوبة/اليوم | ❌ لا — لا مفتاح بلاغ |
| كبار المسعفين | تسليم مناوبة (جاهزية + تسليم واستلام) | ❌ لا — لا مفتاح بلاغ |

لذلك L-4 **ليس «ربطًا بـLookup»** بل **تعبئة من مصادر داخلية موثّقة** بنفس المبدأ: النظام يعبّئ ما يعرفه، والموظف يكمل، ولا حسابات موازية، ولا فقدان للتاريخ.

## 1) حقائق البيانات المثبتة (فحص 2026-09-02)

| # | الحقيقة | الدليل |
|---|---|---|
| F1 | `employees.job_title` يحتوي القيمتين بالمطابقة التامة: «كبير مسعفين» (5 نشطين) + «مساعد كبير مسعفين» (5 نشطين) | استعلام مباشر — يطابق قرار التصنيف الإداري المعتمد |
| F2 | **لا يوجد مسمى «قائد المنطقة»** في `job_title` إطلاقًا (القيم: مسعف، قائد فريق، مشرف، سائق، مسعف أول، فني طوارئ، كبير مسعفين، دعم لوجستي، فني اسعاف، مسعف متدرب، مساعد صحي، تنسيق استجابة، مساعد كبير مسعفين، تحكم عملياتي، أخصائي اسعاف) | `SELECT DISTINCT job_title` |
| F3 | جدول `vehicles` (26 مركبة) يحمل `admin_status` = أساسية 17 / احتياط 8 / خدمة 1 — **تصنيف إداري وليس جاهزية تشغيلية**؛ لا يوجد عمود «عاملة/متعطلة» يومي | `GROUP BY admin_status` |
| F4 | `air_ambulance_records` = **0 سجل** · `shift_forms (air_ambulance)` = **0 سجل** — لا يوجد أي بلاغ جوي محفوظ حتى الآن | عدّ مباشر |
| F5 | `positioning_events` (14 حدثًا) عناوينها حرة («ملاعب الجوكر»، «تمركز») — **لا تطابق المناطق الخمس الثابتة** في النموذج | عينة payload |
| F6 | نقطة قراءة قائمة بلا تعديل خادم: `GET /api/employees` و`GET /api/air-ambulance` موجودتان | server.js:10729 / 7831 |
| F7 | التواريخ الثلاثة في كبار المسعفين و`dailyDate` تُعبأ تلقائيًا بتاريخ اليوم **حاليًا** — تعمل ولا تحتاج تدخلًا | `initForm_senior`/`initForm_daily` |

## 2) مصفوفة الحقول — التقرير اليومي (`form-daily.html`)

| الحقل | المصدر المحتمل | الحكم التشخيصي |
|---|---|---|
| `dailyDate` | تلقائي (اليوم) | ✅ **يعمل حاليًا — لا تغيير** (F7) |
| `dailyReportNumber` | يدوي الآن؛ قابل للتوليد `DAILY-YYYY-MM-DD` | ❓ **س1 — يحتاج قرارك** |
| `dailyAir` عدد بلاغات الجوي | مشتق: عدد نماذج `air_ambulance` المحفوظة في اليوم | ❓ **س2 — التعريف والفائدة يحتاجان قرارك** (F4: سيعيد 0 حاليًا دائمًا) |
| `dailyResponseTeams` عدد الفرق المستجيبة | «غير محسوم» في التشخيص — التعريف التشغيلي غير واضح | ❓ **س3 — أهم سؤال في اليومي** |
| `dailyBorderReports` البلاغات الحدودية | لا مصدر في النظام | يدوي — يبقى كما هو |
| `dailyPath1–8` المسارات الطبية | لا مصدر في النظام | يدوية — تبقى كما هي |
| `dailyFormFill` / `dailySummary` | نص حر | يدوي — يبقى كما هو |

## 3) مصفوفة الحقول — كبار المسعفين (`form-senior.html`)

| الحقل | المصدر المحتمل | الحكم التشخيصي |
|---|---|---|
| `senAsstDate`/`senChiefDate`/`senCmdrDate` | تلقائي (اليوم) | ✅ **تعمل حاليًا — لا تغيير** (F7) |
| `senChiefName` كبير المسعفين | `employees` بالمطابقة التامة «كبير مسعفين» (F1) | ❓ **س4 — قابل للتحويل إلى قائمة اختيار** |
| `senAsstName` مساعد كبير المسعفين | `employees` بالمطابقة التامة «مساعد كبير مسعفين» (F1) | ❓ **س4 — نفس القرار** |
| `senCmdrName` قائد المنطقة | **لا مصدر** (F2) | يدوي — يبقى كما هو |
| التواقيع الثلاثة | نص حر | يدوية — تبقى كما هي |
| `senWorkingCars`/`senBrokenCars`/`senReserveCars` | `vehicles.admin_status` **تصنيف إداري لا جاهزية** (F3) | ❓ **س5 — أهم سؤال في كبار المسعفين** |
| `senOverlapTeams` عدد فرق الاوفرلاب | لا مصدر محسوم | يدوي — يبقى كما هو |
| مناطق التمركز الخمس | `positioning_events` لا تطابقها (F5) | يدوية — تبقى كما هي |
| `senNotes` | نص حر | يدوي — يبقى كما هو |

## 4) النقاط غير المحسومة — تحتاج قرارك قبل أي كود

### س1 — رقم التقرير اليومي
- **الخيار A:** توليد تلقائي `DAILY-YYYY-MM-DD` read-only (إن وُجد تقرير سابق لنفس اليوم يُلحق `-2` إلخ).
- **الخيار B:** يبقى يدويًا.
- **اقتراحي:** A — يزيل إدخالًا حرًا بلا قيمة تشغيلية، والرقم ليس مفتاح CAD.

### س2 — عدد بلاغات الجوي
- **الحقيقة:** المصدر الوحيد الممكن هو نماذج `air_ambulance` المحفوظة في اليوم، وعددها التاريخي **صفر** (F4) — الاشتقاق سيعيد 0 دائمًا حتى تبدأ طلبات الجوي بالحفظ عبر النموذج.
- **الخيار A:** اشتقاق تلقائي read-only من `GET /api/air-ambulance` (عدد سجلات اليوم) — صحيح من اليوم الأول حتى لو صفر.
- **الخيار B:** يبقى يدويًا.
- **اقتراحي:** A بشرط تثبيت التعريف كتابةً بجانب الحقل: «عدد طلبات الجوي المسجلة في المنصة لهذا اليوم» — حتى لا يُقرأ كإحصاء جوي رسمي.

### س3 — عدد الفرق المستجيبة (غير محسوم تشغيليًا)
- تعريفات محتملة بمصادر مختلفة: (أ) الفرق الفريدة في `reports` للمناوبة/اليوم · (ب) الفرق المجدولة في `shift_roster` · (ج) الفرق المكمّلة في `shift_completions`.
- **اقتراحي:** **يبقى يدويًا في L-4** — أي اشتقاق الآن يفرض تعريفًا تشغيليًا لم يُحسم. إن حسمتَ التعريف (أ/ب/ج) نفذه read-only بنفس نمط CAD.

### س4 — أسماء التسليم (كبير/مساعد كبير)
- **الخيار A:** تحويل الحقلين إلى قائمة اختيار من `GET /api/employees` بالمطابقة التامة للمسميين (5+5 نشطين) + خيار «أخرى — اسم حر» للاستثناءات.
- **الخيار B:** تبقى نصًا حرًا.
- **اقتراحي:** A — قائمة اختيار لا تعبئة صامتة (لا يمكن معرفة من هو المناوب من الخمسة)؛ تمنع أخطاء الإملاء وتربط الاسم بسجل موظف حقيقي. «قائد المنطقة» يبقى نصًا حرًا (F2).

### س5 — أعداد المركبات (عاملة/متعطلة/احتياط)
- **الحقيقة:** `admin_status` = تصنيف إداري (أساسية/احتياط/خدمة) ولا يقول شيئًا عن عطل اليوم (F3). الاشتقاق منه سيكون **مضللًا**: «احتياط 8» ≠ «مركبات احتياط جاهزة 8».
- **اقتراحي:** **تبقى يدوية بالكامل في L-4** — تعبئة «احتياط» من admin_status تخالف مبدأ «لا أرقام مضللة». إن أردت لاحقًا جاهزية يومية حقيقية فهي جولة مستقلة (مصدرها أحداث الأعطال/الصيانة وليس هذا النموذج).

## 5) نطاق التنفيذ المقترح بعد الاعتماد (لا كود الآن)

| الملف | التغيير المقترح |
|---|---|
| `public/forms/form-daily.html` | حسب س1/س2/س3 فقط |
| `public/forms/form-senior.html` | حسب س4/س5 فقط (الحقلان → select بقائمة الموظفين) |
| `public/js/app.js` | نطاق `initForm_daily`/`saveDailyReport` + `initForm_senior`/`saveSenior` فقط |
| `scripts/daily-senior-autofill-test.js` | جديد — نفس بنية اختبارات L-2/L-3 |

**لا تُمسّ:** الخادم · DB · النماذج الأربعة المربوطة بـLookup · `incident-lookup.*` · PI-10 · الثيم. الحفظ يمر عبر `POST /api/daily-reports` و`POST /api/senior-shifts` القائمتين.

## 6) اختبارات القبول المقترحة (تُحسم بعد إجاباتك)

1. رقم اليومي المولّد بالصيغة المعتمدة + تسلسل اليوم الثاني.
2. `dailyAir` يطابق عدد سجلات الجوي المزروعة في temp DB لليوم (وليس صفرًا ثابتًا).
3. قائمتا كبار المسعفين تعرضان حملة المسميين فقط بالمطابقة التامة (لا «مشرف» ولا «قائد فريق»).
4. «قائد المنطقة» والمركبات والمناطق تبقى يدوية فارغة — لا تعبئة مضللة.
5. حفظ فعلي للنموذجين يُثبت من `shift_forms` (`daily_report`/`senior_shift`).
6. توافق خلفي للسجلات القديمة.
7. انحدار: L-3 + L-2 + L-1 + الثيم.

**بانتظار أجوبتك على س1–س5. لا كود قبل الاعتماد.**
