# OPS-UI-FORMS-THEME-DIAGNOSIS — تشخيص ثيم النماذج الستة (موسّع)

التاريخ: 2026-09-01 · الجولة: **Operations UI Theme Audit — المرحلة ② (النماذج)** · تشخيص فقط بأمر المالك · صفر تعديل.
يُكمل `OPS-UI-THEME-DIAGNOSIS.md` (المرحلة ①: النوافذ والشاشات) — جذورها ①②③ ما تزال قائمة ومدرجة في نطاق الإصلاح أدناه.

## 1) المنهج والأدلة

- قياس حي على الإنتاج (CDP + getComputedStyle + getMatchedStylesForNode + cssRules) لكل نموذج من الستة، فتحًا فعليًا داخل `formsModal` بحساب قراءة.
- النتائج الخام: `test-output/forms-theme-audit-1788266140149/forms-results.json` + 6 لقطات.
- السكربتات: `scripts/_forms-theme-audit.js` · `_input-cascade-probe.js`.

## 2) جدول النماذج الستة (القياس الفعلي)

| # | النموذج | الملف | حقول (select) | أسطح فاتحة | نصوص ↓تباين | نص الحقول | placeholder | focus |
|---|---|---|---|---|---|---|---|---|
| 1 | كبار المسعفين | form-senior.html | 19 (0) | **2** (شبكة التواقيع + زر حذف) | **12** | داكن ✗ | مختلط ✗ | غير مرئي ✗ |
| 2 | الإسعاف الجوي | form-air.html | 10 (1) | 0 | 0 | داكن ✗ | مختلط ✗ | غير مرئي ✗ |
| 3 | التصعيد | form-escalation.html | 15 (1) | 0 | 0 | داكن ✗ | مختلط ✗ | غير مرئي ✗ |
| 4 | حالات E | form-e.html | 10 (3) | 0 | 0 | داكن ✗ | مختلط ✗ | غير مرئي ✗ |
| 5 | بلاغ حادث | form-incident.html | 11 (4) | 0 | 0 | داكن ✗ | مختلط ✗ | غير مرئي ✗ |
| 6 | تقرير يومي | form-daily.html | 15 (0) | 0 | 0 | داكن ✗ | مختلط ✗ | غير مرئي ✗ |

**خلفيات الحقول** في الستة كلها سليمة داكنة (`rgba(255,255,255,0.05)`) ✓ · **التسميات** موحدة فاتحة هادئة ✓ · **العناوين** فاتحة ✓ عدا senior (تدرجان خاصان: بنفسجي + أزرق) · **الأزرار** 4 لغات (أزرق مصمت / أحمر مصمت / زجاجي شفاف / شريحة coral-50 فاتحة).

## 3) الأسباب الجذرية (كلها مثبتة حيًا، لا ظن)

### R1 — نص الحقول داكن على داكن في 19 نافذة (الخلل الأم)
`smart-toolbar.css:2192-2231` (القسم 5) — قاعدة مجمّعة لمدخلات **19 نافذة**:
```css
#shiftModal … #formsModal .modal-content input, … {
    background: rgba(255,255,255,0.05) !important;   /* داكن ✓ */
    color: #1E293B !important;                        /* ← نص داكن على داكن ✗ */
    border-color: rgba(255,255,255,0.1) !important;
}
```
- السبقية (1,1,1) + !important تتغلب على كل الطبقات اللاحقة (exec / platform-identity / كتلة index.html المضمنة ⑩) — **الإثبات:** getMatchedStylesForNode أظهر هذه القاعدة آخر الفائزين بـ`color: rgb(30,41,59)`.
- على الأغلب بقية نسخ-لصق من ثيم فاتح (كان النص #1E293B منطقيًا على أبيض، ثم غُمّقت الخلفية ونُسي النص).
- **الأثر أوسع من النماذج:** كل حقول distribution/control/analytics/charts/audit/seniorShift/… (19 نافذة) — لهذا أضافت executive-theme لاحقًا رقعة لـ3 نوافذ فقط (peakTime/peakAlert/operationsRoom سطور 2425-2444) بنفس السبقية؛ بقية الـ16 ما تزال مريضة.
- **يقتل التركيز أيضًا:** `border-color … !important` بسبقية (1,1,1) تعمل في كل الحالات فتسحق أي `:focus` من طبقة أدنى ⇒ قياس التركيز: صفر تغيير مرئي (R8).

### R2 — جداول فاتحة كامنة في 8 نوافذ
`smart-toolbar.css:2236-2269` (القسم 6): `th { background:#F1F5F9 !important; color:#2563EB !important }` و`td { border-color:#E2E8F0 !important }` و`table { color:#1E293B !important }`. خاملة حاليًا (النوافذ المقاسة لا ترسم `<table>`) لكنها تفجّر رأسًا أبيض فور ظهور أي جدول.

### R3 — تسميات رمادية ضعيفة كامنة
`smart-toolbar.css:2158-2178` (القسم 4): `.form-row label { color:#64748B !important }` — رمادي متوسط على داكن. لا يظهر في النماذج الست (بنيتها `.form-group`) لكنه يضرب بنى `.form-row` (مثل seniorShift).

### R4 — المحدد الواسع يفرض الأبيض (من المرحلة ①)
`app.css:2047` `#formContent div[style*="grid-template-columns"]` — يلتقط شبكة تواقيع senior (السطح الأبيض 867×351 + كسر تخطيط 3-أعمدة + 12 نصًا فاتحًا على أبيض).

### R5 — التعليق الآكل للقاعدة (من المرحلة ①)
`executive-theme.css:2334` — `peak-*/` يبتلع قاعدة `#analyticsModal .analytics-section` الداكنة (أقسام analytics البيضاء + 10 نصوص).

### R6 — فجوات تغطية (من المرحلة ①)
`.analytics-header` + ثلاثي controlModal (`.control-person`/`.display`/`.control-role-badge`) = 3 + 28 سطحًا فاتحًا.

### R7 — placeholder غير موحد
عائلتان مقاستان داخل النموذج الواحد: `rgb(107,120,153)` (هادئ ✓ — من exec) و`rgb(30,41,59)` (داكن خفي ✗ — حقول date/datetime التي لا يغطيها محدد `::placeholder`).

### R8 — لا مؤشر تركيز مرئي (تبع R1)
### R9 — checkbox/radio بلون المتصفح الافتراضي (accent-color: auto) — غير منسجم.
### R10 — أزرار بأربع لغات (مصمت أزرق/أحمر · زجاجي · شريحة فاتحة) — تحتاج توحيد لغة لا إعادة تصميم.

## 4) العناصر المشتركة القابلة للتوحيد المركزي (من طبقة Theme فقط)

حقول input/select/textarea/number/date · placeholder (بما فيها `::-webkit-datetime-edit`) · :focus · التسميات · العناوين (.form-card-title) · الأزرار (.btn-primary/.btn-coral/ghost) · checkbox/radio (accent-color) · الجداول (th/td) · بطاقات المحفوظات · رسائل الحالة (نجاح/تنبيه/خطأ — لغة الشارات الزجاجية المعتمدة).

## 5) الـPalette المقترحة (من رموز platform-identity المعتمدة أصلًا — لا هوية جديدة)

| العنصر | القيمة |
|---|---|
| سطح النموذج/البطاقة | `rgba(255,255,255,0.035)` + حد `rgba(255,255,255,0.08)` |
| خلفية الحقل | `rgba(255,255,255,0.05)` (أعمق قليلًا من السطح = تمييز) |
| نص الحقل | `var(--pi-ink)` `#E8EDF8` |
| placeholder | `var(--pi-ink-faint)` `#6B7899` (أهدأ بوضوح من النص) |
| حد الحقل | `rgba(255,255,255,0.10)` |
| التركيز | حد `rgba(96,165,250,0.55)` + هالة `0 0 0 3px rgba(96,165,250,0.14)` (نفس نمط الكتلة ⑩ المعتمدة في index.html:2890) |
| التسميات | `var(--pi-ink-dim)` `#9AA7C4` |
| العناوين | `var(--pi-ink)` + لكنة النموذج تُحفظ كأيقونة/شريط رفيع فقط (إزالة التدرجين الفاقعين في senior لصالح اللغة الموحدة) |
| الأزرار | أساسي: الأزرق المتدرج المعتمد · خطر: أحمر زجاجي شفاف · ثانوي: زجاجي (إيقاف شرائح coral-50 الفاتحة) |
| checkbox/radio | `accent-color: var(--pi-blue)` |
| الجداول | رأس `rgba(255,255,255,0.04)` + نص `pi-ink` + حدود `pi-line-soft` |
| الرسائل | زجاج دلالي: نجاح زمردي / تنبيه كهرماني / خطأ أحمر (لغة الشارات الحالية) |

## 6) الملفات التي ستتغير (بعد الاعتماد فقط)

| الملف | التغيير | الحجم |
|---|---|---|
| `public/css/smart-toolbar.css` | R1: `color:#1E293B` ← لون فاتح (يصلح 19 نافذة دفعة واحدة) · R2: جداول داكنة · R3: لون التسمية | 3 نقاط |
| `public/css/executive-theme.css` | R5: تصحيح التعليق الآكل · R4: تغليب داكن لشبكات formContent · R6: تغطية analytics-header وثلاثي controlModal · R7: تغطية datetime-edit · R9: accent-color · توحيد لغة الأزرار/العناوين داخل formsModal | إضافات طبقة |
| `public/index.html` | رفع `?v=` للورقتين فقط (كاش سنة) | رقمان |
| `scripts/forms-theme-test.js` (جديد) | حارس انحدار: صفر أسطح فاتحة في النوافذ الثلاث + نص حقول فاتح + placeholder متدرج + focus مرئي + فحص ساكن يمنع `*/` داخل التعليقات | اختبار صرف |

**لا يتغير:** `app.css` (محمي) · `app.js` · `forms/*.html` (بنية وحقول ومنطق) · الخريطة/RC-6/PI-10 · لا Redesign ولا إعادة ترتيب حقول.

## 7) ملاحظات صراحة

- إصلاح R1 يمس 19 نافذة دفعة واحدة — هذا هو المطلوب (توحيد)، والتغيير حرفي (لون نص خاطئ موضوعيًا على خلفية داكنة). النوافذ الثلاث المرقّعة في exec تبقى فائزة (نفس السبقية + تحميل لاحق) فلا تعارض.
- أثر R4 التخطيطي (auto-fill لشبكة التواقيع) يبقى كما هو في هذه الجولة (ثيم فقط) ويُحسم لاحقًا بقرار منفصل.
- تدرجا عناوين senior البنفسجي/الأزرق: اقتراح إزالتهما لصالح اللغة الموحدة — إن أردت إبقاء هوية لونية لكل نموذج تُحفظ كلكنة أيقونة فقط؛ **أنتظر قرارك في هذه النقطة تحديدًا**.

**الخلاصة:** النماذج الستة متقاربة بصريًا لكنها تتشارك 4 عيوب وظيفية (نص داكن ✗ · placeholder غير موحد ✗ · focus غائب ✗ · accent افتراضي ✗) جذرها قاعدة واحدة معيبة في smart-toolbar + ثغرات موثقة من المرحلة ①. الإصلاح كله في طبقتي Theme + حارس اختباري، بلا أي مساس بالكود المحمي أو منطق النماذج.
