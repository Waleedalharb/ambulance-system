# SCHEDULE-IMPORT-HISTORICAL-DIAGNOSIS — السبب الجذري العام لـ«الحفظ لا ينعكس»

**التاريخ:** 2026-08-30 · **النطاق:** تشخيص فقط — لا تعديل كود/بيانات، لا Commit/Push/Deploy
**بلا فرضية مسبقة:** لم يُفترض أن 409 هو السبب؛ تُبعت الدورة كاملة وفُحصت كل المسارات.
**الأدلة:** قراءة كود + إعادتا إنتاج معزولتان (10/10 + 6/6) + فحص آثار فعلية في القاعدة المحلية.

---

## ١. الخلاصة — يوجد **سببان جذريان عامان**، لا سبب واحد

> **G1 (مسار 409 — أصاب شهر 9):** بعد الـ409 تعيد الواجهة القراءة من القاعدة قبل قرار
> المستخدم، فتُمحى بيانات الشهر الجديد من المصفوفة، ويصل طلب «كتابة فوق» فارغًا من
> الشهر → «نجاح» بلا انعكاس. *(تفاصيله الكاملة في SCHEDULE-M9-DIAGNOSIS.md)*
>
> **G2 (الأخطر والأعم — يفسر تكرار الحادثة في 7 و8):** `syncFromSchedule` تشتق
> الفترات **حصريًا** من تواريخ مداخل الحمولة. إذا لم تحمل الحمولة أي مدخل بتاريخ
> صالح → `periods` فارغة → **`DELETE FROM shift_roster` بالكامل (كل الشهور)** ←
> والـendpoint يرجع **200 نجاح** (`rosterRows=0`). أي استيراد فشل تحليل أيامه
> يمسح الجداول كلها وهو «ناجح».
>
> **إصلاح toast + تثبيت الحمولة يعالج G1 فقط. G2 مستقل ويبقى مفتوحًا بعده.**

## ٢. خريطة الدورة الكاملة وكل الكتّاب

```
① المسارات الرسمية الجديدة (oiApply — official-import): تحليل __oi.parse → دمج
   → POST /api/schedule/official-import → JSON + syncFromSchedule (ذرّية)
② المسارات الكلاسيكية (ما زالت حية في نفس الصفحة!):
   - parseExcel (2316/2349) → extractEmployees → saveToServer → نفس المعالج
   - تحميل «الجدول المحفوظ» (2316) → extractEmployees → saveToServer
   - تحليل PDF (2722) → saveToServer
   - زر «مزامنة مع السيرفر» (syncBtn) → saveToServer
③ المسارات الجانبية التي تكتب shift_roster بلا JSON:
   تعديل خلية (11007) · صف يدوي · نقل يوم واحد (12238) · clear-range (11249)
④ المسارات الماحية: DELETE /api/schedule/employees (9707+syncFromSchedule([]))
   · clear-all (11220) — مقصودة وموثقة
⑤ لا توجد إعادة مزامنة لاحقة: الإقلاع لا يقرأ JSON → DB، والبث لا يكتب.
```

## ٣. إجابات الأسئلة بالأدلة

**هل يوجد أكثر من مصدر حقيقة؟** نعم، بانفصال مصمَّم: `shift_roster` (SQLite) هو SSOT
المعلن للعرض (3261-3268)؛ `schedule-employees.json` كاش سقوط **عام بلا مفتاح شهر**
(576, 2160). المسارات الجانبية ③ تكتب القاعدة بلا JSON — والاستيراد الرسمي بعد
`oiApply` يكتب JSON بمداخل شهر الاستيراد فقط (5482-5485). **الملفان لا يمثلان نفس
البيانات إطلاقًا بعد أول تعديل جانبي أو أول استيراد شهر ثانٍ** — والدليل الفعلي
محليًا: JSON يغطي `{"2026-09": 2324}` فقط بينما القاعدة تحمل 4 شهور.

**كيف يحدد syncFromSchedule الشهر؟** من تواريخ `schedule[].date` داخل الحمولة فقط
(303-318) — **لا يستقبل month/year ولا يقرأ متغيرًا آخر**. لا يوجد أي تحقق أن
الفترات المبنية هي الشهر الذي قصده المستخدم.

**هل يمكن أن يرجع نجاحًا بلا إنشاء roster للشهر؟** نعم — مُثبت مرتين:
G1 (الطلب الثاني الفارغ) وG2 (periods فارغة ← مسح شامل). كلاهما 200+success.

**هل توجد حالات حفظ JSON دون SQLite؟** المعالج المشترك لا (فشل المزامنة = 500).
لكن العكس يقع: المسارات الجانبية ③ تكتب SQLite بلا JSON — انفصال دائم الاتجاهين.

**هل عملية لاحقة تعيد الكتابة؟** لا يوجد boot-resync ولا watcher. لكن البث
`schedule_employees_updated` يدفع المتصفحات المفتوحة الأخرى للقراءة — وأي Save
لاحق من تبويب قديم يحمل بيانات قديمة يعيد كتابة القاعدة بها (قناة الحفظ لا تتحقق
من حداثة الحمولة).

**إعادة التحميل/اختيار الشهر؟** يعتمدان القاعدة حصرًا (`fetchEmployeesFromDBSilent`
+ `/api/shift-roster/months`) — الكاشات سقوط مُنحط بلا سلطة. لذلك «أعد فتح الصفحة»
يكشف الحقيقة فورًا: ما ليس في القاعدة لن يظهر أبدًا.

## ٤. كيف تولد حمولة G2 في الواقع (المسار الكلاسيكي)

- `extractEmployees` يستخرج الشهر من صفوف الملف الأولى (2422-2442): إن لم يجد
  رقم شهر صالحًا → `monthNum=0` → تواريخ `2026-00-XX` → `_validIsoDate` يرفضها
  كلها → `skippedEntries` → **periods فارغة → مسح شامل بنجاح**.
- إن لم يُستخرج أي موظف: `generateDemoData()` (2606، 2719) — **بيانات تجريبية
  وهمية** (تواريخ من 2026-06-29، جوالات عشوائية) تُرفع بـ saveToServer وتُبنى
  في القاعدة كأنها جدول حقيقي.
- موظف بلا أي خلية صالحة يُسقط من empMap (2565) → لا يرد في الحمولة →
  `syncFromSchedule` **يعطّله** (`is_active=0`, سطر 233-243).

## ٥. آثار الشهور في القاعدة المحلية (دليل فعلي، مع حدوده)

| الشهر | shift_roster محليًا | JSON العام | ملاحظة |
|---|---|---|---|
| 2026-05 | 2804 صف / 148 موظفًا | ✗ غير مغطى | JSON لا يغطي إلا آخر استيراد — ببالتصميم |
| 2026-06 | **غائب كليًا** | ✗ | سبب الغياب غير قابل للتحديد من هذه النسخة |
| 2026-07 | 2479 / 144 | ✗ | |
| 2026-08 | 2372 / 142 | ✗ | |
| 2026-09 | 2324 / 138 | ✓ 2324 مدخلًا (مطابق تمامًا) | JSON بتاريخ 18:10 UTC 29-8 — استيراد محلي ناجح بلا 409 (لا تعيينات يدوية محليًا) |

سجل التدقيق المحلي: **صفر** أحداث `import_overwrite_manual` — أي أن الاستيرادات
الناجحة محليًا لم تمر بمسار 409 إطلاقًا. **في الإنتاج** (حيث اليدوية موجودة)
الحادثة سلسلة G1. أما حادثتا 7/8 فلا آثار إنتاجية متاحة لديّ — لكن G2 يفسر
نمطهما («حُفظ ولم ينعكس/اختفى السابق») دون الحاجة لأي 409.

**حدود الدليل:** لا أملك قراءة قاعدة الإنتاج. الجدول النهائي الكامل للشهور
الثلاثة إنتاجيًا يتطلب إما وصولًا أو لقطات `/api/shift-roster/months` قبل/بعد.

## ٦. الجدول النهائي المطلوب (شهر 9 — الإنتاج، مدعوم بإعادة الإنتاج)

| الشهر | POST | JSON | syncFromSchedule | shift_roster | months API | الواجهة |
|---|---|---|---|---|---|---|
| 9 (طلب 1) | ✅ حمل الشهر → 409 | لم يُكتب | لم تُستدعَ | بلا تغيير | بلا 9 | مودال التعارض |
| 9 (طلب 2 — كتابة فوق) | ❌ **فارغ من 9** (G1) | كُتب بلا 9 | ✅ أعاد بناء القديم فقط | ❌ بلا 9 | ❌ بلا 9 | ❌ لا ينعكس |

## ٧. اتجاهات الإصلاح — بلا تنفيذ (لكلٍّ أمر منفصل)

1. **G1:** تثبيت حمولة الطلب الأول وإعادة إرسالها نفسها مع `confirmOverwriteManual` (أو تأجيل إعادة القراءة حتى نجاح نهائي).
2. **G2 (أولوية قصوى):** حارس سيرفري — ممنوع أن تكون `periods` فارغة مع حمولة غير
   فارغة إلا بمسار مسح صريح؛ المسح الشامل يبقى حصرًا لـ clear-all/DELETE. كسر
   الصمت: `skippedEntries>0` يجب أن يظهر في الرد والواجهة.
3. **تقييد/إطفاء المسارات الكلاسيكية ②** أو توجيهها عبر محلل `__oi` ذي الحُرّاس —
   فهي الطريق الوحيد الذي تولد فيه حمولات G2 والبيانات التجريبية.
4. **عزل JSON:** إما تفعيله بمفتاح شهر أو إعلانه أرشيفًا للقراءة فقط — لأنه اليوم
   يوحي بأنه مرآة القاعدة وهو ليس كذلك.

**الخلاصة للمالك:** شهر 9 = G1 (يعالجه تثبيت الحمولة). الشهران 7/8 = النمط الأعم
G2 على الأرجح (حارس periods الفارغة يغلقه). المشكلة «أكبر من شهر 9» — مؤكد.
