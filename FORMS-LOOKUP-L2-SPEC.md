# مواصفة L-2 التنفيذية — ربط نموذج E بـIncidentLookup

**التاريخ:** 2026-09-02 · **الحالة:** للمراجعة والاعتماد فقط — **ممنوع أي كود قبل توقيع المالك**
**المرجع:** تشخيص `FORMS-INCIDENT-LOOKUP-DIAGNOSIS.md` (§5) + L-1 المغلق (`FORMS-LOOKUP-L1-EXECUTION.md` — API 14/14 · UI 13/13)
**النطاق:** **نموذج E فقط** — بقية النماذج الخمسة لا تُمسّ، ولا مساس بأي جولة أخرى (PI-10 / الثيم / Place Identity / R1–R5).

---

## 1) الهدف

```text
نموذج E ← IncidentLookup (L-1) ← بيانات البلاغ المركزية
```

يصبح نموذج E: يُدخل الموظف رقم البلاغ ← يتحقق النظام ← تُعرض بيانات CAD **read-only** ← يختار الموظف **الفرقة الفعلية** من مشاركات البلاغ الحقيقية ← زمن الاستجابة يظهر **من الحساب المركزي كما خُزّن** ← يُحفظ النموذج بـ`incidentNumber` مُتحقق منه.

## 2) القرارات المحسومة (بما أثبته L-1)

| # | القرار | الحكم |
|---|---|---|
| 1 | `eResponseTime` اليدوي | **يُحذف من النموذج** — لا حقل إدخال لزمن الاستجابة إطلاقًا |
| 2 | زمن الاستجابة المعروض | `resp_arrival_min` المركزي **كما هو** — لا إعادة حساب في الواجهة ولا الخادم ولا النقطة |
| 3 | تعدد الفرق ⭐ | **الاختيار من الفرق الفعلية المحتسبة فقط** (التفصيل §3) |
| 4 | المشاركة الملغاة/المسحوبة | **ليست مشاركة فعلية:** تظهر موسومة (شفافية تشغيلية) لكنها **غير قابلة للاختيار** ولا يُعرض زمنها كقيمة النموذج |
| 5 | نقص البيانات | صريح دائمًا: «غير متاح — بيانات CAD ناقصة» — **لا صفر ولا قيمة وهمية ولا إخفاء** |
| 6 | الحفظ | يتطلب بلاغًا **مُتحققًا منه عبر Lookup** + فرقة فعلية مختارة؛ يُحفظ `incidentNumber` + `incidentUnit` + `responseTime` (المركزي) + `responseTimeSource: 'cad-central'` داخل `form_data` |
| 7 | بيانات CAD في النموذج | read-only ببنية DOM + وسم «من CAD» — نفس سلوك L-1 المثبت |
| 8 | بلاغ غير موجود | **لا حفظ** — رسالة «✗ لا يوجد بلاغ بهذا الرقم — نموذج E يرتبط ببلاغ CAD موثّق» |
| 9 | عرض وقت CAD في النموذج (حسم ملاحظة Bidi من L-1) | يُعرض **منسّقًا بتوقيت الرياض** عبر `TimeRiyadh` القائم من `cadCreatedAtTs`، والخام `cadCreatedAtRaw` يبقى في tooltip للتتبع |
| 10 | الخادم | **صفر تغيير:** `POST /api/e-cases` القائم يمرر الحقول الجديدة إلى `form_data` عبر `FormsService` دون أي تعديل |

## 3) ⭐ حسم تعدد الفرق (النقطة المطلوبة صراحةً)

L-1 أثبت أن `bestArrivalMin` قيمة **تحليلية** (أسرع فرقة)، بينما نموذج E طبي يرتبط بـ**الفرقة الفعلية للمريض**. الحكم:

```text
مشاركات البلاغ من Lookup
        ↓
المحتسبة فقط قابلة للاختيار (counted=true)
الملغاة/المسحوبة: موسومة + معطّلة
        ↓
┌─ فرقة محتسبة واحدة ← تُختار تلقائيًا وتُعرض بزمنها
├─ أكثر من فرقة ← قائمة اختيار إلزامية داخل بطاقة Lookup:
│    «أي فرقة باشرت الحالة فعليًا؟» — لا اختيار صامت ولا افتراض «الأسرع»
│    كل خيار يعرض زمنه المركزي بجانبه
└─ لا مشاركة محتسبة ← حالة نقص صريحة (§4)
        ↓
زمن الاستجابة في النموذج = resp_arrival_min للفرقة المختارة فقط
(bestArrivalMin التحليلي لا يدخل النموذج إطلاقًا)
```

- تغيير الاختيار يحدّث الزمن المعروض فورًا من نفس حمولة Lookup (لا طلب إضافي).
- زمن المباشرة `resp_mubashara_min` للفرقة المختارة يُعرض معلوماتيًا بجانب الاستجابة.

## 4) حالات النقص الصريحة

| الحالة | الواجهة | الحفظ |
|---|---|---|
| `timeCompleteness=complete` + فرقة مختارة لها زمن | بطاقة خضراء + الزمن | مسموح |
| فرقة مختارة بلا زمن (`respArrivalMin=null`) | «زمن الاستجابة غير متاح — بيانات CAD ناقصة» | مسموح، يُحفظ `responseTime: null` + `responseTimeSource: 'cad-unavailable'` — **لا صفر** |
| بلاغ بلا مشاركة محتسبة | «البلاغ موجود لكن لا فرقة محتسبة — راجع التوزيع» | **ممنوع** (لا فرقة فعلية = لا نموذج E) |
| `timeCompleteness=missing/partial` | شارة ⚠️ + الحقول الناقصة موسومة | مسموح فقط إن وُجدت فرقة محتسبة مختارة |
| بلاغ غير موجود | «✗ لا يوجد بلاغ بهذا الرقم» | ممنوع |

## 5) تغييرات النموذج بالضبط (`form-e.html` + `app.js`)

| العنصر | قبل | بعد |
|---|---|---|
| رأس «معلومات البلاغ» | `eReportNumber` نص حر + `eDateTime` يدوي | **مكوّن IncidentLookup** (بحث + بطاقة CAD + اختيار الفرقة) |
| `eDateTime` | إدخال يدوي | read-only من CAD (منسّق الرياض + الخام tooltip) |
| `eLocation` | إدخال يدوي | read-only من CAD (`address/district/street`) |
| `eUnit` | قائمة ثابتة «سريع 1–4 / جنوب 1–19» | **قائمة الفرق الفعلية للبلاغ** من Lookup (§3) |
| `eResponseTime` | حقل رقمي يدوي | **يُحذف** ← عرض محسوب read-only داخل البطاقة |
| `eAge / eGender / eHospital / eOutcome / eNotes` | يدوية | **تبقى يدوية كما هي — بلا أي تغيير** |
| الحفظ `saveE()` | يجمع حقولًا حرة | يتطلب Lookup محلولًا + فرقة مختارة؛ يرسل الحقول الجديدة |

- `index.html`: إضافة سطري `<link>/<script>` للمكوّن مع `?v=` جديد (أول شحن فعلي للمكوّن — L-1 لم يشحنه).
- صفر تغيير في `server.js` / `FormsService` / قاعدة البيانات / النماذج الأخرى.
- الثيم: بطاقة Lookup داخل النموذج تتبع نفس لوحة جولة الثيم (الملف `incident-lookup.css` القائم — لا ورقة جديدة).

## 6) التوافق الخلفي

- سجلات `e_case` القديمة (بلا `incidentNumber`) تبقى كما هي وتُعرض كما هي — **لا migration ولا لمس للتاريخ**.
- قائمة «الحالات المحفوظة» وواتساب: تُقرأ الحقول الجديدة إن وُجدت وإلا السلوك القائم (عرض `reportNumber` القديم) — لا كسر للعرض التاريخي.

## 7) اختبارات القبول (امتداد لنمط L-1)

### `scripts/e-form-lookup-test.js` (خادم معزول + Chrome CDP)

| # | الفحص |
|---|---|
| 1 | فتح نموذج E ← مكوّن Lookup موجود، وحقل `eResponseTime` **غير موجود في DOM** |
| 2 | بحث 1310627 ← `eDateTime` و`eLocation` معبّآن read-only من CAD ويحملان وسم «من CAD» |
| 3 | تعدد الفرق: فرقتان محتسبتان ظاهرتان، **لا اختيار تلقائي**، والزمن يتبع الاختيار (سريع 1 ← 6.8 · جنوب 14 ← 26) |
| 4 | بلاغ 1310420: الملغاة يدويًا **موسومة ومعطّلة** — لا يمكن اختيارها |
| 5 | فرقة واحدة محتسبة ← اختيار تلقائي موثق في الواجهة |
| 6 | 99900127: شارة النقص + «غير متاح» + **«0 دقيقة» لا تظهر** |
| 7 | 0000000: رسالة عدم الوجود + **زر الحفظ لا يحفظ** |
| 8 | حفظ فعلي: `POST /api/e-cases` يستلم `incidentNumber:'1310627'` + `incidentUnit` + `responseTime:6.8` + `responseTimeSource:'cad-central'` — يُثبت بقراءة `shift_forms` في temp DB |
| 9 | فرقة بلا زمن (عينة temp): الحفظ يمر بـ`responseTime:null` + `'cad-unavailable'` — لا صفر |
| 10 | عرض «الحالات المحفوظة» لسجل قديم بلا الحقول الجديدة لا ينكسر |
| 11 | انحدار: `incident-lookup-api-test` + `forms-theme-test` أخضران |
| 12 | لقطات: قبل البحث · بعد الربط · اختيار الفرقة · نقص · غير موجود |

## 8) الملفات والمخاطر والتراجع

| بند | التفصيل |
|---|---|
| **تُعدَّل** | `public/forms/form-e.html` · `public/js/app.js` (نقاط E فقط: التركيب + الحفظ + العرض) · `index.html` (سطرا تحميل + `?v=`) |
| **لا تُمسّ** | الخادم · قاعدة DB · النماذج الخمسة الأخرى · PI-10 · جولة الثيم · `incident-lookup.js/.css` (يُعاد استخدامهما كما اعتُمدا في L-1) |
| **مخاطر** | منخفضة-متوسطة: أول ربط فعلي بنموذج قائم — يُخفف بأن الحقول اليدوية المتبقية لا تتغير وأن الحفظ يمر عبر نفس النقطة القائمة |
| **التراجع** | إعادة `form-e.html` ومقطعَي `app.js` — لا بيانات ولا خادم يتغيران |
| **الفصل** | Commit مستقبلي يُفصل عن الثيم وPI-10 بنفس أسلوب الـpatch المعتمد |

## 9) معايير إغلاق L-2

1. الاختبار 12/12 أخضر على المعزول + الانحداران أخضران.
2. تقرير تنفيذ باللقطات الخمس + حمولة الحفظ المثبتة من temp DB.
3. تأكيد صريح: صفر تعديل على النماذج الأخرى · صفر تغيير خادم · صفر إعادة حساب أزمنة.
4. **لا Commit/Push/Deploy إلا بأمر صريح بعد المراجعة المحلية.**

---

**بانتظار توقيع المالك على هذه المواصفة قبل كتابة أي سطر.**
