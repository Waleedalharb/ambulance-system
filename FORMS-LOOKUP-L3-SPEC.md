# مواصفة L-3 التنفيذية — ربط نموذجَي حادث + تصعيد بـIncidentLookup

**الحالة:** للمراجعة فقط — **لا كود قبل الاعتماد**
**المرجع:** `FORMS-INCIDENT-LOOKUP-DIAGNOSIS.md` (مصفوفة الحقول §2.1/§2.3) · `FORMS-LOOKUP-L2-SPEC.md` (النمط المثبت) · `FORMS-LOOKUP-L2-EXECUTION.md` (16/16)

## 0) المبدأ الحاكم (المُثبت في L-1/L-2)

> **CAD يعبّئ ما يعرفه النظام → الموظف يكمل ما لا يعرفه النظام → لا إعادة إدخال لما هو موجود أصلًا → لا حسابات موازية → لا فقدان للتاريخ.**

- نفس مكوّن `IncidentLookup` (L-1) ونفس نقطة `GET /api/incidents/lookup` — **لا تُعدَّلان**.
- نفس نقاط الحفظ القائمة `POST /api/incidents` و`POST /api/escalations` عبر `FormsService` — **صفر تغيير في الخادم وقاعدة البيانات**.
- نفس آليات L-2 المعتمدة: `cadRawToLocalInput` (وقت CAD من الخام الجداري) · وسم «من CAD» · حارس الحمولة البالية · الملغاة موسومة معطّلة · `null` لا يتحول إلى صفر.

## 1) النطاق

| داخل L-3 | خارج L-3 |
|---|---|
| `form-incident.html` (بلاغ حادث) | النماذج الأربعة الأخرى (E أُغلق في L-2؛ جوي/يومي/كبار لاحقًا) |
| `form-escalation.html` (التصعيد) | الخادم · DB · PI-10 · جولة الثيم · `incident-lookup.js/.css` |
| دوال الحادث/التصعيد في `app.js` فقط | تغذية قوائم «المركز»/«المستشفى» من جداولها (توسيع مؤجل) |

## 2) مصفوفة الحقول — كل حقل ومصدره (من التشخيص، بلا افتراض)

### 2.1 نموذج بلاغ حادث

| الحقل | المصدر المحسوم | السلوك بعد L-3 |
|---|---|---|
| `incReportNumber` | مفتاح Lookup | **يُحذف** ← مكوّن البحث والتوثّق |
| `incDateTime` | CAD `cad_created_at` | read-only + وسم «من CAD» + الخام tooltip |
| `incLocation` | CAD `address/district/street/city` | read-only + وسم «من CAD» |
| `incUnit` الفرقة | CAD مشاركات البلاغ | **قائمة الفرق الفعلية المحتسبة فقط** — نفس منطق L-2: تعدد ← اختيار إلزامي «-- أي فرقة باشرت فعليًا؟ --»؛ مفردة ← تلقائي؛ ملغاة ← موسومة معطّلة. الأزمنة تظهر داخل بطاقة Lookup كما هي (النموذج لا يحفظ زمن استجابة — ليس حقلًا فيه أصلًا) |
| `incType` نوع الحادث | **يدوي يبقى يدويًا** | التشخيص: ترجمة `code` (MDS) للنوع **غير محسومة** ← لا اشتقاق. الموظف يقرأ الترميز الظاهر في بطاقة Lookup (مثال `26A02`) ويختار يدويًا. إلزامي كما هو |
| `incCenter` المركز | **يدوي يبقى يدويًا** | لا يوجد في بيانات CAD الواصلة (تشخيص §2.1). تغذيته من جدول `centers` توسيع مؤجل خارج الجولة |
| `incPatientName` / `incAge` / `incGender` | **يدوية تبقى يدوية** | لا تصل من CAD |
| `incDescription` وصف الحالة | CAD `description` كتعبئة مسبقة | **قابل للتحرير** — CAD مصدر البداية والموظف يستكمل (انظر §4 قرار التتبع) |
| `incActions` الإجراءات | **يدوي يبقى يدويًا** | |

### 2.2 نموذج التصعيد

| الحقل | المصدر المحسوم | السلوك بعد L-3 |
|---|---|---|
| `escReportNumber` | مفتاح Lookup | **يُحذف** ← مكوّن البحث والتوثّق |
| `escDateTime` | CAD `cad_created_at` | read-only + وسم «من CAD» |
| `escLocation` | CAD `address/district/street/city` | read-only + وسم «من CAD» |
| `escEventType` نوع الحدث | **يدوي يبقى يدويًا (14 خيارًا)** | التشخيص: **لا نظير له في CAD الواصل** — لا اشتقاق من `code`/`type` إطلاقًا. يبقى إلزاميًا يدويًا |
| `escInjuries` / `escDeaths` | **يدوية تبقى يدوية** | |
| `esc-agency` الجهات المشاركة | **يدوية تبقى يدوية (8 جهات)** | |
| `escDetails` التفاصيل | CAD `description` كتعبئة مسبقة | **قابل للتحرير** (نفس §4) |

**فرق جوهري عن E:** التصعيد **لا يحتوي حقل فرقة إطلاقًا** ← الحفظ يتطلب بلاغًا متحققًا فقط، ولا قائمة فرق ولا أزمنة.

## 3) تدفق الحفظ

```
بحث CAD → بلاغ متحقق (found:true, حارس الحمولة البالية)
   ↓
حادث: + فرقة فعلية مختارة (counted فقط) + نوع حادث يدوي
تصعيد: + نوع حدث يدوي
   ↓
POST /api/incidents | /api/escalations  ← نفس النقاط القائمة
   ↓
shift_forms (form_name: 'incident_report'/'escalation' كما هي)
```

**الحقول الجديدة في السجل المحفوظ:**
- حادث: `incidentNumber` (متحقق) · `incidentUnit` (الفرقة المختارة) · `cadDescription` (انظر §4) · بقية الحقول بأسمائها الحالية (`reportNumber` يبقى = رقم البلاغ لتوافق العرض والقوائم القديمة).
- تصعيد: `incidentNumber` · `cadDescription` · بقية الحقول كما هي.

**حالات الحجب (نفس L-2):**

| الحالة | حادث | تصعيد |
|---|---|---|
| بلاغ غير موجود / حمولة بالية | ممنوع | ممنوع |
| بلاغ بلا فرقة محتسبة | **ممنوع** (انظر §5 س1) | لا ينطبق — الحفظ مسموح |
| نوع الحادث/الحدث فارغ | ممنوع (يدوي إلزامي كما هو حاليًا) | ممنوع (كما هو حاليًا) |

## 4) قرار تتبع وصف CAD (يحتاج اعتمادك)

`incDescription`/`escDetails` تُعبأ من CAD وتبقى قابلة للتحرير — أي أن المحفوظ قد يختلف عن نص CAD. للحفاظ على «لا فقدان للتاريخ»:

**المقترح:** يُحفظ حقل إضافي `cadDescription` (نص CAD الخام كما ورد) بجانب الحقل المحرَّر، فتبقى بداية CAD قابلة للتتبع حتى لو عدّلها الموظف. لا يظهر `cadDescription` في الواجهة — تتبع فقط.

## 5) أسئلة تحتاج حسمك في الاعتماد

1. **حادث بلا فرقة محتسبة:** أقترح الحجب الكامل (اتساقًا مع E — توثيق الفرقة الفعلية إلزامي). البديل: السماح بالحفظ بلا فرقة مع وسم نقص. **قراري المقترح: حجب.**
2. **`cadDescription`** (§4): إضافته للسجل للتتبع — **مقترح: نعم.**
3. التصعيد ببلاغ `timeCompleteness=missing/partial`: مسموح بشارة ⚠️ (لا فرقة ولا زمن في النموذج أصلًا) — **مقترح: مسموح مع الشارة.**

## 6) تغييرات الملفات بالضبط

| الملف | التغيير |
|---|---|
| `public/forms/form-incident.html` | حذف `incReportNumber` · تركيب `#incIncidentLookup` · `incDateTime`/`incLocation` read-only بوسم «من CAD» · `incUnit` قائمة ديناميكية معطّلة حتى التوثّق (تُزال القائمة الثابتة سريع 1–4/جنوب 1–19) |
| `public/forms/form-escalation.html` | حذف `escReportNumber` · تركيب `#escIncidentLookup` · `escDateTime`/`escLocation` read-only بوسم «من CAD» |
| `public/js/app.js` | نطاق دوال الحادث (5763–5871) ودوال التصعيد (6821–6919) فقط: تركيب المكوّن · `fill*` من الحمولة · إعادة كتابة `saveIncident`/`saveEscalation`/`clear*`/`send*WhatsApp` بنفس بنية L-2 · **إعادة استخدام** `cadRawToLocalInput` القائمة (لا نسخة جديدة) |
| `index.html` | لا شيء — المكوّن مُحمّل أصلًا منذ L-2 |
| `scripts/incident-esc-lookup-test.js` | جديد — نفس بنية اختبار L-2 |

**لا تُمسّ:** الخادم · DB · النماذج الأربعة الأخرى · `form-e.html` ودوال E · `incident-lookup.js/.css` · PI-10 · ملفات الثيم.

## 7) اختبارات القبول — `scripts/incident-esc-lookup-test.js`

| # | الفحص |
|---|---|
| 1 | فتح حادث: Lookup مركّب + `incReportNumber` غائب من DOM + صفر أخطاء console |
| 2 | حادث 1310627: `incDateTime`/`incLocation` معبّآن read-only بوسم «من CAD» + `incDescription` مُعبأ مسبقًا من CAD |
| 3 | حادث 1310627: فرقتان محتسبتان + خيار إلزامي + **لا اختيار تلقائي** |
| 4 | حادث 1310420: الملغاتان موسومتان معطّلتان + «جنوب 5» تُختار تلقائيًا |
| 5 | حادث: `incType` و`incCenter` يبقيان فارغين يدويين بعد التوثّق — لا اشتقاق من `code` |
| 6 | حادث 99900127: شارة النقص + لا حفظ بلا فرقة محتسبة |
| 7 | حادث 0000000: «لا يوجد بلاغ» + حارس الحمولة البالية + لا POST |
| 8 | حادث — حفظ فعلي يُثبت من `shift_forms` في temp DB: `incidentNumber:'1310627'` + `incidentUnit:'جنوب 14'` (المختارة) + `type` اليدوي + `cadDescription` = نص CAD الخام + `description` = نص الموظف المعدَّل |
| 9 | فتح تصعيد: Lookup مركّب + `escReportNumber` غائب |
| 10 | تصعيد 1310627: تعبئة read-only + `escEventType` يبقى فارغًا يدويًا + `escDetails` مُعبأة مسبقًا قابلة للتحرير |
| 11 | تصعيد — حفظ فعلي **بلا فرقة** (لا حقل فرقة أصلًا) يُثبت من temp DB: `incidentNumber` + `eventType` اليدوي + `cadDescription` |
| 12 | تصعيد 0000000: الحفظ محظور |
| 13 | توافق خلفي: سجلّان قديمان (حادث + تصعيد) بلا الحقول الجديدة يُعرضان بلا كسر |
| 14 | انحدار أخضر: `e-form-lookup-test` + `incident-lookup-api-test` + `forms-theme-test` |
| 15 | لقطات: حادث (قبل · بعد الربط · اختيار فرقة) + تصعيد (قبل · بعد الربط) + نقص + غير موجود |

## 8) المخاطر والتراجع

| بند | التفصيل |
|---|---|
| مخاطر | منخفضة — نمط L-2 مثبت بحذافيره؛ الجديد الوحيد: التعبئة المسبقة القابلة للتحرير (§4) |
| التراجع | إعادة ملفّي النموذجين ومقطعَي `app.js` — لا بيانات ولا خادم يتغيران |
| الفصل | شجرة العمل تحمل تغييرات L-2 + الثيم + PI-10 — Commit مستقبلي يُفصل بالـpatch بنفس الأسلوب المعتمد |

## 9) معايير إغلاق L-3

1. الاختبار 15/15 أخضر على المعزول + الانحدارات الثلاثة خضراء.
2. تقرير تنفيذ باللقطات + حمولتا الحفظ مثبتتان من temp DB.
3. تأكيد صريح: صفر تعديل خادم/DB · صفر اشتقاق لنوع الحادث/الحدث · صفر مساس بـE وL-1.
4. **لا Commit/Push/Deploy إلا بأمر صريح بعد المراجعة المحلية.**
