# Release Audit — جرد ما قبل النشر (2026-08-29)

**الغرض:** مراجعة شاملة لكل التغييرات المحلية المتراكمة قبل أي قرار Commit/Push/Deploy.
**الحالة:** تقرير مراجعة فقط — **لم يُنفذ أي Commit / Push / Deploy**.

---

## ① نقطة الأساس

آخر commit: **`ba8bc29`** — «إصلاح دورة المستشفى الكاملة + حارس الجداول متعدد الشهور» (معتمد ومنشور في جولته).
**كل ما هو محلي غير مُلتزم الآن = جولات Place Intelligence الأربع (PI-1 ← PI-4) فقط.** لا يوجد أي تغيير عالق من جولات سابقة — إصلاح الجداول 8/9 ودورة المستشفى داخلة أصلًا في `ba8bc29`، فلا اختلاط.

## ② الجرد الكامل للتغييرات المحلية

### أ. ملفات متتبعة معدّلة (3)

| الملف | الحجم | المحتوى | الجولة |
|---|---|---|---|
| `server.js` | +31/−0 | حقن placeIntel + طابور الحسم بعد تسجيل CAD (PI-3) + سطر `response.places = sealed.places || null` (PI-4) — كلها additive صِرفة | PI-3 · PI-4 |
| `shift-archive-engine.js` | +29/−1 | `places: null` في التهيئة + الخطوة 19 + دالة `_getPlaces` بنمط hospital | PI-3 |
| `public/operations-dashboard.html` | +168/−0 | 3 حزم فقط: أنماط `.pl-*` + زر التبويب العاشر + قسم PI-4 المعلَّم (`PI-4 BEGIN/END`) مع الوصلة في `viewShift` — **تحقق آلي: كل سطر مضاف يخص PI-4** | PI-4 |

### ب. ملفات جديدة — كود إنتاجي (2)

| الملف | الوظيفة | الجولة |
|---|---|---|
| `services/place-intelligence-service.js` | محرك التعرف R1–R5 + المخازن الثلاثة + الاكتشاف + قسم الأرشيف (+ حقل `decision` من PI-4) | PI-1 ← PI-4 |
| `services/place-resolution-hook.js` | طابور الحسم التسلسلي للربط الحي السلبي | PI-3 |

### ج. ملفات جديدة — اختبارات (6)

`place-intelligence-test.js` (26) · `place-discovery-test.js` (13) · `place-archive-test.js` (13) · `place-archive-proof2-test.js` (6) · `place-archive-view-test.js` (16) · — كلها تكتب في مجلدات مؤقتة فقط.

### د. ملفات جديدة — أدوات تشغيلية (4)

`places-seed.js` (زرع السجل 21 موقعًا) · `place-discovery.js` (اكتشاف المرشحين) · `places-review.js` (المراجعة البشرية — أدوات PI-2) · `place-intelligence-historical-run.js` (التشغيل التاريخي) · `pi4-preview-gen.js` (مولّد معاينات — أداة تطوير).

### هـ. وثائق (7)

`PI-1..PI-4-REPORT.md` + `pi-1-directive-final.md` + `pi-1-review-checklist.md` + `place-intelligence-architecture.md`.

## ③ فحص الآثار الجانبية — بيانات الإنتاج

| الفحص | النتيجة |
|---|---|
| `/data/` في `.gitignore` (سطر 4) | ✅ لن تدخل git أصلًا |
| `data/places.json` | البذرة المعتمدة فقط: **21 موقعًا (12 مستشفى + 9 مراكز تشغيلية)، كلها active، placesVersion=1** |
| `data/place-resolutions.json` | **غير موجود محليًا** — الاختبارات كلها في مجلدات مؤقتة معزولة |
| `data/place-candidates.json` | **غير موجود محليًا** (الاكتشاف أنتج 0 مرشحين بصدق على البيانات التاريخية) |
| قاعدة البيانات / report_times / مخازن 1B | لم تُمس — إثبات: `cad-report-integration 89/89` + `cycle 53/53` بعد كل الجولات |
| `data/hospital-monitor.json` | آخر تعديل 2026-08-28 (جولته المعتمدة) — لم تلمسه جولات PI |

**خلاصة الجانبيات: صفر.** لا بيانات تشغيلية تغيّرت، ولا ملفات مؤقتة تسربت إلى data/.

## ④ البطارية الاختبارية الشاملة (أُعيدت كلها اليوم)

| الحزمة | النتيجة |
|---|---|
| place-intelligence (PI-1) | 26/26 ✅ |
| place-discovery (PI-2) | 13/13 ✅ |
| place-archive (PI-3) | 13/13 ✅ |
| place-archive-proof2 (إغلاق مراجعة PI-3) | 6/6 ✅ |
| place-archive-view (PI-4) | 16/16 ✅ |
| hospital-monitoring-lost | 48/48 ✅ |
| hospital-cycle | 53/53 ✅ |
| duplicate-detection-v2 | 13/13 ✅ |
| cad-report-integration | 89/89 ✅ |
| schedule-month-guard | 23/23 ✅ |
| multi-month | 7/7 ✅ |
| notification-hospital-ux | 49/49 ✅ |
| hospital-monitor-smoke | 16/16 ✅ |
| **الانحدار الشامل** | **201/213 — نفس الـ12 فشل البيئية المعروفة من baseline (W1-B/V-B/SR-1/SR-2)، بلا أي فشل جديد** |

## ⑤ ما يدخل الإصدار وما يبقى — مقترح للمالك

### يدخل الإصدار (كود + اختبارات)
- الملفات الثلاثة المعدّلة + الخدمتان الجديدتان.
- الاختبارات الستة (توثيق حي مانع للرجوع).
- أدوات PI-2 التشغيلية الثلاثة (seed/discovery/review) — **ضرورية للإنتاج** (انظر ⑥).

### قرار للمالك
- **الوثائق السبعة** (تقارير/أوامر/معمارية): تدخل git كتوثيق، أم تبقى خارج المستودع؟
- **`pi4-preview-gen.js`**: أداة تطوير للقطات — تدخل أم تُستبعد؟

### لا يدخل git إطلاقًا (محمي بـ.gitignore)
- `data/places.json` وأي مخازن مستقبلية — بيانات تشغيلية.

## ⑥ متطلبات نشر خاصة بهذا الإصدار (يجب أن تُدرج في أمر النشر)

1. **تشغيل البذرة على الإنتاج مرة واحدة:** `node scripts/places-seed.js` — لأن `/data/` خارج git، والسجل (21 موقعًا) لن ينتقل مع Deploy. دونه يعمل النظام لكن بلا أي جهة معروفة (كل البلاغات Unknown بصدق).
2. **لا Migration ولا إعادة توليد** لأي بيانات قائمة — المخازن الثلاثة تُنشأ ذاتيًا عند أول استخدام.
3. **التحقق الإنتاجي بعد النشر (مقترح):**
   - Health/Uptime سليم + نسخة المنصة الجديدة.
   - بلاغ CAD تجريبي ← يظهر `placeResolution` مخزنًا (مقنّعًا) ولا يؤثر على الاستجابة.
   - ختم مناوبة تجريبية ← اللقطة تحتوي `places` + التبويب العاشر يعرضها.
   - مناوبة قديمة (قبل PI-3) ← رسالة الغياب الهادئة بلا أخطاء.
   - عدم تأثر: report_times / الاحتساب / الجداول / المستشفيات / الخريطة / الإشعارات.
4. **قيد معروف موثق (من اعتماد PI-4):** بطاقة البلاغ تعرض «وقت الحسم» — وقت البلاغ نفسه غير محفوظ في بنية القسم؛ معالجته مرحلة مستقلة بأمر منفصل.

## ⑦ خطة الالتزام المقترحة (لا تُنفذ إلا بأمرك)

خياران:
- **أ.** Commit واحد مجمع: «Place Intelligence PI-1..PI-4 (اعتماد المالك 2026-08-29)» — أبسط، وكل الجولات معتمدة.
- **ب.** أربعة Commits متتابعة (PI-1 ← PI-4) — أدق تاريخيًا لكنه يتطلب تقسيمًا يدويًا للملفات المشتركة (server.js/shift-archive-engine.js فيهما جولتان).

**توصيتي: الخيار أ** — الملفات المشتركة لا تنقسم بشكل نظيف، والتقارير الأربعة توثق التسلسل.

---

**بانتظار قرار المالك: ما يدخل/يبقى ← ثم أمر النشر. لا شيء يُنفذ قبلها.**
